// shells/LearnShell.tsx — 學習文件 shell（group: learn）。
// LearnHub / ai-models hub / model-wishlist / my-brain / codex / teaching-archive / teams / feedback。
import { ShellFrame } from "./ShellFrame";

export function LearnShell() {
  return <ShellFrame shell="learn" />;
}

export default LearnShell;
