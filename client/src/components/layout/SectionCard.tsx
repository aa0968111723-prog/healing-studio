import type { ReactNode } from "react";
import { cn } from "@/lib/utils";
export function SectionCard({ title, description, icon, actions, children, variant = "default" }: { title: string; description?: string; icon?: ReactNode; actions?: ReactNode; children?: ReactNode; variant?: "default" | "primary" | "warning" | "muted"; }) { const variantClass = { default: "bg-card/40", primary: "bg-primary/5 border-primary/40", warning: "bg-amber-50/40 border-amber-300/50", muted: "bg-muted/30" }[variant]; return <section className={cn("rounded-xl border border-border/40 p-4 space-y-3", variantClass)}><div className="flex items-start justify-between gap-2"><div><h3 className="font-medium flex items-center gap-2">{icon}{title}</h3>{description ? <p className="text-sm text-muted-foreground">{description}</p> : null}</div>{actions}</div>{children}</section>; }
