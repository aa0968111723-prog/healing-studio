import type { ReactNode } from "react";
export function NextStepPanel({ title, description, status, reason, primaryAction, secondaryActions, warnings }: { title: string; description?: string; status?: ReactNode; reason?: string; primaryAction?: ReactNode; secondaryActions?: ReactNode; warnings?: ReactNode; }) { return <section className="rounded-xl border border-primary/30 bg-primary/5 p-4 space-y-2"><div className="flex items-start justify-between gap-3"><div><h2 className="text-base font-semibold">{title}</h2>{description ? <p className="text-sm text-muted-foreground">{description}</p> : null}</div>{status}</div>{reason ? <p className="text-xs text-muted-foreground">原因：{reason}</p> : null}{warnings ? <div className="text-xs text-amber-600">{warnings}</div> : null}<div className="flex flex-wrap gap-2">{primaryAction}{secondaryActions}</div></section>; }
