# healing-studio 網站細節深掘（程式碼 Ground-Truth・給 UI/UX 設計交接包 / Claude Design）

> 完成日：2026-06-06　·　模式：**全程唯讀**（未編輯任何檔、未跑 git、未動 working tree）
> 來源：真實 repo `C:\Users\User\healing-studio-dev`（branch `feat/4-shell-restructure`，另一 Code session 同時在優化／commit）
> 基準對照：`director.today_登入後內部盤點.md`、`AI-Director_開發計畫.md`
> 取證原則：**每項附真實檔案路徑＋符號名**；數量已實際 grep 計數者標「✅ 已驗」，僅由子代理推估者標「⚠️ 約／待核」；**不杜撰**。
>
> **本文補的是「盤點文件還沒完整捕捉」的後端真相**：25 精靈完整集合與視覺/觸發中繼資料、世界觀資料模型全欄位、導演 AI 真實 procedure 清單、以及提示詞庫／模型登錄／積分／生成管線／資產庫等子系統。

---

## 0. 先講三件最關鍵的「校正」（盤點文件 vs 真實程式碼）

| 項目 | 盤點/口傳值 | 程式碼真實值 | 證據 |
|---|---|---|---|
| 站內頁面數 | 「36 / 47 / 50」自相矛盾 | **`APP_PAGE_REGISTRY` 約 48–50 筆**（`supportsPageAgent:` 出現 50 次、`agentEntryPriority:` 48 次） | `shared/appRegistry.ts`（✅ 已驗 grep） |
| 精靈數 | 25 | **確為 25**（`SPIRITS` 陣列剛好 25 筆、`SPIRIT_COMMANDS` 25 筆、`AgentRole` union 也剛好 25 個成員） | `client/src/lib/spiritsVisual.ts:37`、`shared/slash-commands.ts:116`、`shared/orb-agent-roles.ts:19`（✅ 已驗） |
| 資料表數 | 「23 張表」(舊文件) | **82 張 `mysqlTable`** | `drizzle/schema.ts`（✅ 已驗 grep `= mysqlTable(` = 82） |
| 主動觸發 | 16 | **`SPIRIT_PROACTIVE_TRIGGERS` 約 16–17 筆** | `shared/orb-agent-roles.ts:2332`（⚠️ grep 計到 17，需逐筆核） |
| 提示詞庫條目 | — | **`PROMPT_REFERENCE_LIBRARY` 120 筆** | `shared/promptReferenceLibrary.ts`（✅ 已驗 `id:` = 120） |

> **設計含意**：頁面權威數請以 `appRegistry.ts` 為準（約 48–50），不是 codex 卡片上的 36/47。資料庫是 **MySQL + 大量 JSON 欄位**，不是「23 張表」。

---

## 1. 25 個「精靈」完整集合（全部列出，附三處交叉佐證）

精靈系統的「真相」散落三個檔，**三者必須一起看**：

| 檔案 | 符號 | 角色 |
|---|---|---|
| `shared/orb-agent-roles.ts:19` | `type AgentRole`（25 成員 union） | **權威 id 清單**＋關鍵字路由規則 `KEYWORD_RULES` |
| `shared/orb-agent-roles.ts:975` | `SPIRIT_NICKNAMES` | 每個 role 的暱稱＋別名陣列 |
| `shared/orb-agent-roles.ts:1114` | `SPIRIT_FAMILY` | 每個 role 屬哪個家族（specialist/role/proactive） |
| `shared/slash-commands.ts:116` | `SPIRIT_COMMANDS`（25 筆） | slash 指令字＋短描述 |
| `client/src/lib/spiritsVisual.ts:37` | `SPIRITS`（25 筆）＋ `SPIRIT_PROFILES`（395） | **視覺**：emoji／漸層／光圈／vibe／profile |
| `shared/spirit-chat-tools.ts` | `SPIRIT_CHAT_TOOLS` | 每精靈的對話「執行類型」 |
| `shared/orb-agent-roles.ts:2332` | `SPIRIT_PROACTIVE_TRIGGERS` | 主動觸發事件 |

### 1A. 三個家族（`spiritsVisual.ts:365` `SPIRIT_FAMILY_LABEL`，✅ 已驗 9+10+6=25）

- **specialist 專精精靈（9）**：圖 / 影 / 音 / 聲 / 訓 / 學 / 群 / 靈 / 體
- **role 通用同事（10）**：導 / 編 / 品 / 查 / 路 / 暖 / 總 / 記 / 細 / 步
- **proactive 主動精靈（6）**：財 / 巧 / 守 / 律 / 安 / 帶

### 1B. 全 25 精靈總表

> 「執行類型」來自 `shared/spirit-chat-tools.ts`（`SPIRIT_CHAT_TOOLS`）。視覺 emoji/漸層來自 `spiritsVisual.ts`（子代理回報，⚠️ 個別色碼建議交接時打開該檔核對）。

| # | id (`AgentRole`) | slash 指令 | 暱稱(主) | 家族 | emoji | 執行類型 | 用途／輸入→輸出 |
|---|---|---|---|---|---|---|---|
| 1 | `image-specialist` | `/image` | 圖圖 | specialist | 🎨 | `fal-generation`(≥6字) | 生圖/改圖/角色一致性。輸入提示詞→圖。工具見 §1D |
| 2 | `video-specialist` | `/video` | 影影 | specialist | 🎬 | `fal-generation` | 圖轉影/生影片/對嘴 |
| 3 | `music-specialist` | `/music` | 音音 | specialist | 🎵 | `fal-generation` | BGM/音效/混音/分軌 |
| 4 | `voice-specialist` | `/voice` | 聲聲 | specialist | 🎙️ | `fal-generation` | 配音/聲音克隆/TTS/轉錄 |
| 5 | `training-specialist` | `/train` | 練練 | specialist | 🧪 | `fal-generation` | LoRA/角色/風格訓練 |
| 6 | `learning-specialist` | `/learn` | 學學 | specialist | 📚 | `navigate`(→/learn-hub) | 教學中心/模型解說/學習路徑 |
| 7 | `community-manager` | `/community` | 群群 | specialist | 📣 | `llm-persona` | IG/TikTok/YT/小紅書策略＋貼文素材 |
| 8 | `inspiration-specialist` | `/inspire` | 靈靈 | specialist | 💡 | `llm-persona`＋靈感工具 | 靈感/mood board/prompt 變體 |
| 9 | `anatomy-specialist` | `/anatomy` | 體體 ⚠️ | specialist | 🫀 | `fal-generation` | 人體解剖/醫學插圖（**指令檔暱稱寫「解解」，見 §1F**） |
| 10 | `director` | `/director` | 導導 | role | 🎯 | `llm-persona` | 跨頁規劃拆步驟（與 /director 頁同名但不同物，§3） |
| 11 | `composer` | `/composer` | 編編 | role | ✍️ | `page-execution`(≥4字) | 當頁直接填提示/選模型/送出 |
| 12 | `critic` | `/critic` | 品品 | role | 🔎 | `llm-persona` | 成品檢查/給回饋 |
| 13 | `researcher` | `/research` | 查查 | role | 🧭 | `search`(≥3字) | 站內/網路查資料/比較 |
| 14 | `navigator` | `/nav-spirit` | 路路 | role | 🧳 | `navigate`(intentBased) | 帶你去對的頁 |
| 15 | `companion` | `/companion` | 暖暖 | role | 🌿 | `llm-persona` | 心情陪聊/緩衝（fallback role） |
| 16 | `chief-orchestrator` | `/orchestrator` | 總總 | role | 🎩 | `llm-persona` | 多代理團隊狀態總指揮 |
| 17 | `notes-curator` | `/notes` | 記記 | role | 📒 | `navigate`(→/notes) | 筆記/排程/素材庫 |
| 18 | `settings-detail` | `/settings-detail` | 細細 | role | ⚙️ | `llm-persona` | 偏好/設定細節微調 |
| 19 | `plan-executor` | `/execute` | 步步 ⚠️ | role | 🧩 | `agent-plan`(≥6字) | 規劃+跨頁一條龍執行（**指令檔暱稱寫「執執」，見 §1F**） |
| 20 | `accountant` | `/budget` | 財財 | proactive | 💰 | `llm-persona` | 預算/點數估算/省錢（主動） |
| 21 | `quality-coach` | `/quality` | 巧巧 | proactive | ✨ | `llm-persona` | 提示詞+品質教練（主動） |
| 22 | `inspector` | `/inspect` | 守守 ⚠️ | proactive | 🛡️ | `llm-persona` | 全站糾察（壞連結/a11y/perf）（**指令檔描述寫「內容審查/合規」，見 §1F**） |
| 23 | `legal-advisor` | `/legal` | 律律 | proactive | ⚖️ | `llm-persona` | 版權/商標/肖像權守門（主動） |
| 24 | `security-guard` | `/security` | 安安 | proactive | 🔒 | `llm-persona` | 金鑰/釣魚/帳號安全（主動） |
| 25 | `onboarding-coach` | `/onboarding` | 帶帶 | proactive | 🤝 | `llm-persona` | 卡關偵測/操作輔導（主動） |

> ✅ 此 25 筆與 `AgentRole` union（`orb-agent-roles.ts:19-44`）一一對應，無多無缺。

### 1C. 暱稱別名（`SPIRIT_NICKNAMES` @ `orb-agent-roles.ts:975`，供 `@暱稱` 呼叫）

每個 role 有 2–4 個暱稱，首位為主暱稱。重點別名（設計搜尋框時要支援）：

- 圖圖／阿圖；影影／阿影／小影；音音／哼哼／小音；聲聲／麥麥／小聲；練練／阿訓；學學／學長；導導／老導；編編；品品／嚴選；查查／研哥；路路／領航；暖暖；財財／精算師；巧巧／品質教練／提示詞教練；守守／糾察隊／巡邏員；律律／法務官；安安／盾盾；群群／社群經理；總總／總管；帶帶／操作教練；記記；細細；步步／執行精靈；靈靈／創意精靈；體體／醫學插圖精靈。

### 1D. 哪些精靈有「真實工具表」（`shared/orb-specialized-agents.ts` `SPECIALIZED_AGENT_CAPABILITIES`，✅ 已讀全檔）

並非 25 個都有獨家工具表；`SPECIALIZED_AGENT_CAPABILITIES` 只收了 **11 個** specialist-family（其餘走 generic skill / proactive trigger 路徑）：

| agentId | displayName | primaryTools（節錄真實工具名） |
|---|---|---|
| image-specialist | 圖像精靈 | `studio.generateImage` `studio.generate3D` `imageSpecialist.recommendModel/enhancePrompt/getModels/generate/edit/upscale/getTips`（8+） |
| video-specialist | 影像精靈 | `studio.generateVideo/enhanceVideo/animateSpeaker` |
| music-specialist | 音樂精靈 | 5×`studio.*`（generateAudio/generateSfx/separateStems/isolateAudio/mergeAudios）＋7×`musicSpecialist.*`（recommendEngine/buildPrompt/estimateCost/listEngines/getRecentAssets/getOptions/getTips）＝**12** |
| voice-specialist | 語音精靈 | 10×`voiceSpecialist.*`＋5×`studio.*`（generateVoice/cloneVoice/designVoice/changeVoice/transcribe） |
| training-specialist | 訓練精靈 | `studio.trainLora`＋6×`trainingSpecialist.*`（listMyModels/getModelStatus/recommendParams/analyzeDataset/estimateTraining/getTips） |
| learning-specialist | 學習精靈 | 9×`learningSpecialist.*`＋`inspiration.fetch` |
| community-manager | 社群精靈 | `research.deepSearch` `studio.generateImage` `studio.generateVideo` |
| inspiration-specialist | 靈感精靈 | `inspiration.fetch`＋8×`inspirationSpecialist.*`（searchTrends/getSuggestions/analyzeReference/getStyleMixing/buildMoodBoard/refinePromptVariants/rankStylesByIntent/getStyleAtlas） |
| composer | 編編 | 8×`page.*`（fillPrompt/setModel/setParam/setTab/setMode/applyPreset/submit/runWorkflow） |
| anatomy-specialist | 解剖精靈 | `studio.generateImage`（單一） |

> 註：檔頭註解明說「另有 8 位精靈（律律/安安/群群/總總/帶帶/記記/細細/步步）多數沒有自己的 fal 工具或 specialised modality」，只有 community-manager 被收進此 registry。設計交接時：**specialist 精靈＝會真的呼叫生成 API；proactive/role 精靈＝多半是 LLM 人格 + 導航/規劃**。

### 1E. 主動觸發事件（`SPIRIT_PROACTIVE_TRIGGERS` @ `orb-agent-roles.ts:2332`；⚠️ 約 16–17 筆，子代理回報下表，建議交接時逐筆核對文案）

| eventId | 精靈 | UI 層級 | 觸發條件（語意） |
|---|---|---|---|
| `monthly_spend_threshold` | 財財 | inline | 月額度超過閾值 |
| `expensive_op_about_to_run` | 財財 | **blocking** | 高成本操作前確認 |
| `low_quality_generation` | 巧巧 | inline | 偵測低品質結果 |
| `prompt_too_short` | 巧巧 | toast | 提示詞過短 |
| `site_error_detected` | 守守 | toast | API 4xx/5xx |
| `page_perf_bad` | 守守 | toast | 首屏/TTI 過慢 |
| `feature_not_used` | 守守 | toast | 方案功能長期未用 |
| `context_near_full` | 暖暖 | inline | 對話 token 接近壓縮上限 |
| `ip_risk_detected` | 律律 | **blocking** | 名人/品牌/版權素材偵測 |
| `credential_leak_detected` | 安安 | **blocking** | API key/token/password 字串 |
| `user_stuck_detected` | 帶帶 | inline | 連續失敗或長時間無動作 |
| `team_status_overview` | 總總 | toast | ≥3 並行任務 |
| `social_post_ready` | 群群 | inline | 內容生成+社群關鍵字 |
| `notes_capture_suggested` | 記記 | toast | 決策/想法偵測 |
| `settings_drift_detected` | 細細 | toast | 設定與行為不一致 |
| `multi_step_plan_ready` | 步步 | inline | ≥3 步計畫已批准 |

> 三種 UI surface：`blocking`（擋住、需確認，多為高風險：成本/版權/資安）、`inline`（嵌在對話流）、`toast`（角落輕提示）。設計交接需為這三層各做一套樣式。
> 客戶端事件匯流排：`client/src/lib/proactiveSpiritEvents.ts`（`ProactiveEventBus`、`useProactiveSpiritEvents()`）。

### 1F. ⚠️ 三個「真實程式碼內部不一致」（交接務必拍板統一）

1. **解解 vs 體體**（anatomy-specialist）：`slash-commands.ts:141` 寫 `nickname: "解解"`；但 `SPIRIT_NICKNAMES`（`orb-agent-roles.ts`）與 `spiritsVisual.ts` 主暱稱是 **體體**。→ 同一精靈兩個名。
2. **執執 vs 步步**（plan-executor）：`slash-commands.ts:139` 寫 `nickname: "執執"`；`SPIRIT_NICKNAMES`/`spiritsVisual` 主暱稱是 **步步**。
3. **守守職責**：`slash-commands.ts:131` 描述「內容審查／合規」；但 `orb-agent-roles.ts` 的 `KEYWORD_RULES`（inspector 規則 line 411）與 `spiritsVisual` 定位是「全站糾察（壞連結/404/500/a11y/perf）」。→ 兩種人設。

### 1G. 精靈小屋 UI（`client/src/components/orb-agent/SpiritHut.tsx`）

- 資料型別：`SpiritVisual`（id/emoji/label/nickname/vibe/greeting/prompt/gradient/ring/family）＋ `SpiritProfile`（skills[]/helpsYou[]/sampleAsks[]）。
- 展開名片三段：「我的看家本事」(skills)、「我能怎麼幫你」(helpsYou)、「你可以這樣叫我」(sampleAsks chips，點擊 → 預填輸入框 `onPrefill`)。
- CTA「邀請出門」→ `onInvite`（greeting toast）。視覺變體：classic（漸層暈染）/ minimalist（單色）。
- 交棒協議：`shared/spirit-handoff-protocol.ts`（`scoreProtocolHandoffs()`/`pickBestHandoff()`/`walkProtocolHandoffChain()`，預設走 4 步鏈）。

---

## 2. 世界觀系統（Worldview / Worldbuilding）資料模型——完整 schema

世界觀是 healing-studio 最深的資料結構。**儲存策略：MySQL 3 張表 + 大量 JSON 欄位**，強型別定義在 TS。

### 2A. 三張核心表（`drizzle/schema.ts`，✅ 行號已驗）

#### `worldbuilding_frameworks`（@ `drizzle/schema.ts:3165`）— 一個「世界」
| 欄位 | 型別 | Null | 預設 | 說明 |
|---|---|---|---|---|
| id | int PK autoinc | NO | | |
| userId | int | NO | | FK user |
| name | varchar(255) | NO | | 世界名 |
| description | text | YES | | |
| genre | varchar(128) | YES | | 類型（療癒/賽博龐克…，preset 見 §2D） |
| era | varchar(128) | YES | | 時代背景 |
| **charactersJson** | json | NO | | `WorldCharacter[]`（§2B 全欄位） |
| **scenesJson** | json | NO | | `WorldScene[]` |
| objectsJson | json | YES | | `WorldObject[]` |
| linkedModelIds | json `number[]` | YES | | 綁的 LoRA（`fine_tuned_models.id`） |
| **styleProfilesJson** | json | YES | | `WorldStyleProfile[]`（畫面風格） |
| **musicThemesJson** | json | YES | | `WorldMusicTheme[]`（配樂主題） |
| defaultStyleProfileId | varchar(64) | YES | | 預設風格 profile |
| globalNegativePrompt | text | YES | | 全世界負面提示詞 |
| productionTargetsJson | json | YES | | 製作目標（格式/時長/受眾/平台/里程碑/credits/master 規格） |
| researchEntriesJson | json | YES | | v4：世界研究資料庫 |
| soundLibraryJson | json | YES | | v4：音效庫 |
| uploadedAssetsJson | json | YES | | v4：上傳資產引用 |
| tags | json `string[]` | YES | | |
| isActive | boolean | NO | true | 軟刪旗標 |
| createdAt / updatedAt | timestamp | NO | now / onUpdate | |
索引：`wf_userId_idx`、`wf_userId_createdAt_idx`。型別匯出 `WorldbuildingFramework = $inferSelect`。

#### `world_storyboards`（@ `drizzle/schema.ts:3243`）— 動畫分鏡時間軸
| 欄位 | 型別 | Null | 預設 | 說明 |
|---|---|---|---|---|
| id | int PK | NO | | |
| userId | int | NO | | |
| worldId | int | NO | | → `worldbuilding_frameworks.id`（多對一） |
| name | varchar(255) | NO | | |
| totalDurationSec | int | NO | | 全片秒數 |
| fps | int | NO | 24 | |
| aspectRatio | varchar(16) | NO | "16:9" | |
| **scenesJson** | json | NO | | `StoryboardScene[]`（§2C） |
| sourceScriptId | varchar(64) | YES | | 連回腳本 |
| narration | text | YES | | 全片旁白 |
| productionStatus | varchar(32) | NO | "planning" | `planning→generating_frames→refining→rendering_video→composing_audio→final_compose→completed` |
| finalVideoUrl | varchar(2048) | YES | | |
| estimatedCostUsd | varchar(32) | YES | | |
| pipelinePlanJson | json | YES | | `AnimationPipelinePlan` 快照 |
| jobsJson | json | YES | | `Record<stepId,{status,output,error}>` |
索引：`ws_userId_idx`、`ws_worldId_idx`、`ws_userId_createdAt_idx`。

#### `creative_projects`（@ `drizzle/schema.ts:3297`）— 三向綁定的專案整合表（/create 駕駛艙的後端）
| 欄位 | 型別 | Null | 說明 |
|---|---|---|---|
| id / userId / title / description | | | |
| **directorSessionId** | int | YES | → `project_notes_calendar.id`（noteType=script, tag 含 director-session） |
| worldviewId / scriptId | int | YES | Phase 1 別名（可空，過渡用） |
| **worldFrameworkId** | int | YES | → `worldbuilding_frameworks.id` |
| **worldStoryboardId** | int | YES | → `world_storyboards.id` |
| status | mysqlEnum | NO（預設 concept） | `concept / production / review / complete` |
| coverImageUrl / tags / metadata | | YES | metadata 為未來擴充（設計案/品牌設定…） |
索引含 `cp_worldFrameworkId_idx`、`cp_worldStoryboardId_idx`。**三個 FK 皆 nullable、無硬約束**（允許建後再綁）。

> ⚠️ /create 頁顯示的「進度 42%／目前步驟」屬 UI 文案；programmatic 進度是由「三向綁定是否齊（director/world/storyboard）+ status enum」推導，不是單一 progress 欄位。

### 2B. `WorldCharacter` 全欄位（`shared/worldbuilding-types.ts:185`，✅ 已讀全 2695 行）

角色是世界觀裡最肥的型別，分 4 個版本層疊：

- **基礎**：id, name, role(`protagonist/supporting/antagonist/npc`), tagline, personality, likes[], interests[], outfit, appearance, signatureItems[], backstory, linkedModelId, triggerWord, notes
- **v2 動畫**：`threeViewSheet`（前/側/背/3-4 視圖＋參考圖）、`expressions[]`（表情包：name/imageUrl/promptKeywords/intensity/triggers）、`outfits[]`（多套服裝：occasion/season/palette/triggerWord）、`speechTone`（語氣/正式度/語速/口頭禪/自他稱呼/禁用詞）、`voiceProfile`（engine: elevenlabs/gemini/fal/minimax/custom + voiceId/languageCode/pitch/speed/emotion/clone）、`scriptRole`（archetype/arcType/defaultPosition/screenTimeRatio/relationships/signatureLines）、`body`（heightCm/ageStage/build/species/distinctiveFeatures）
- **v3 製作級**：`rigSpec`（live2d/spine_2d/rigged_3d/stop_motion/ai_only + boneCount/ikChains/blendShape/cloth/hair/eye）、`lipSyncSet`（preston_blair/rhubarb/ipa/custom + 9 音素圖）、`actingNotes`（emotionalRange/signatureGestures/walkCycleStyle/posture/gaze/tics/cameraPreference/squashStretch）、`ageVariants[]`、`soundProfile`（footsteps/breath/laugh/cry/shout/hurt/sigh 樣本）、`referenceLibrary[]`
- **v4 真實參考**：`realWorldRefs[]`（歷史人物/原型/聲優/時尚…+ citation）、`uploadedAssets[]`（`WorldAssetRef`：url/fileKey/mimeType/purpose）

### 2C. 分鏡型別（`shared/worldbuilding-animation.ts`）— 鏡號/S0X 的真相

> ⚠️ 真實程式碼用 **`StoryboardFrame.id` + `seed`** 當「鏡號」鍵；盤點/計畫文件講的 `S0X` 是慣例命名，對應到此 `frame.id`/HF dataset `A{N}-S0X`。程式碼本身欄位名是 `frames[]`，不是字面 `S0X` 欄位。

- `StoryboardScene`：id, sequenceIndex, startSec, endSec, title, worldSceneId, `characterBeats[]`, actionDescription, cameraDirection, transitionOut, `frames[]`, `audioClips[]`, styleProfileId, musicThemeId, status(`draft/in_review/approved/rendering/rendered/needs_revision`)
- `StoryboardFrame`（＝關鍵影格/鏡）：id, atSec, shotDescription, shotSize(特寫/中景/全景…), cameraMovement, prompt, negativePrompt, **三段管線輸出 `imageUrl`(t2i)→`refinedImageUrl`(精修)→`videoClipUrl`(i2v)**, modelHints{imageModel/refineModel/videoModel}, status(`queued/t2i_done/refined/i2v_done/failed`), **seed**
- `StoryboardCharacterBeat`：characterId, startOffsetSec, durationSec, outfitId, expressionId, pose, dialogue, innerThought, goal, interactionTags[]
- `StoryboardAudioClip`：kind(`music/voiceover/sfx`), startOffsetSec, durationSec, characterId, text, musicThemeId, sfxDescription, volume, fadeIn/Out, audioUrl, status

### 2D. 世界觀在 /video 流程怎麼被用（procedure 層）

- `server/routers/worldbuilding.ts`：`list/get/create/update/delete`、`linkableModels`（可綁的 ready LoRA）、`linkableVoices`、**`summarizeForPrompt`**（把世界壓成 LLM 文字＋每角色/場景的一致性 prefix＋globalNegativePrompt）、**`queryEntities`**（跨角色/場景/物件/研究全文檢索，回 top-K）
- `server/routers/worldStoryboard.ts`：`list/listByWorld/get/create/update/delete`、`seedSkeleton`（從 framework 自動長出空白分鏡）、`validate`（時間軸不重疊）、**`planPipeline`**（編出 t2i→refine→i2v 的 `AnimationPipelinePlan`，含拓樸排序 steps + 估時/估成本）、`updateJob`、`summarizeForPrompt`、**`createFromSegments`**（從腳本 segment 建分鏡）
- `AnimationPipelineStep.spirit` 直接指派給精靈：`image-specialist / video-specialist / music-specialist / voice-specialist / video-compositor`（即世界觀→分鏡→生成是「派工給精靈」的真實接點）。
- 前端世界觀互動在 `/director` 頁的「世界觀」分頁（§3），不是壞掉的 `/animation`。

### 2E. 前端 preset 常數（`worldbuilding-types.ts` 尾段，給 UI chip 直接用）

`GENRE_PRESETS`(17)、`ERA_PRESETS`(12)、`PERSONALITY_TRAIT_PRESETS`(20)、`SCENE_MOOD_PRESETS`、`SCENE_LIGHTING_PRESETS`、`EXPRESSION_PRESETS`(27)、`OUTFIT_OCCASION_PRESETS`、`VOICE_TONE_BASE_PRESETS`、`ART_STYLE_PRESETS`(21)、`MUSIC_INSTRUMENT_PRESETS`、`CAMERA_MOVEMENT_PRESETS`、`TRANSITION_PRESETS`、`CHARACTER_ARCHETYPE_PRESETS`、`RIG_TYPE_PRESETS`、`LIP_SYNC_SYSTEM_PRESETS`、`ANIMATION_SCHOOL_PRESETS`、`COMPOSITING_PASS_PRESETS`、`SHADING_MODEL_PRESETS`、`COLOR_SPACE_PRESETS`、`MASTER_RESOLUTION_PRESETS`、`PRODUCTION_ROLE_PRESETS`(21) 等——**這些是現成的下拉/標籤資料，設計時直接對映即可**。

---

## 3. 導演 AI（Director AI）——真實能力／模式／procedure

> ⚠️ 重要區分：「導演 AI」是 **兩個不同的東西**：①`/director` **頁面**（一條龍工作室，後端 `server/routers/director.ts`）；②精靈「導導」（`director` role，純 LLM 人格切片，§1）。本節講①。

### 3A. Router：`server/routers/director.ts`（✅ 2697 行，procedure 全用 `brainProcedure`，行號已驗）

完整 procedure 清單（top-level 32 + `preferences` 子 router 2 = **約 34**）：

| 群組 | procedure（行號） | 作用 |
|---|---|---|
| 對話 | `chat`(122) / `refineScript`(154) | 雙引擎 CO-STAR 對話（含 personality）/ 改寫腳本 |
| Session | `saveSession`(259) / `listSessions`(308) / `loadSession`(321) / `deleteSession`(341) | 存/列/載/刪導演對話（gzip+base64 壓縮存 `project_notes_calendar`） |
| 偏好 | `preferences.get`(358) / `preferences.update`(361) | personality/preferredFormat/customSystemPrompt |
| 模板 | `templates`(250) | 預設創作模板 |
| 腳本分析 | `importScript`(382) / `discussSegment`(532) / `quickActions`(661) / `exportScript`(666) | 解析長腳本成 segment / 逐段討論（多模態）/ 快速動作 / 匯出(json/csv/md/fdx/srt) |
| 影片腳本 | `generateVideoScript`(433) / `videoScriptTypes`(521) | 從 brief 生結構化腳本（可吃 worldFrameworkId）；類型 `short_video/long_video/social/animation/deep_discussion` |
| CO-STAR | `generateSegmentCostar`(737) / `batchGenerateCostar`(903) / `analyzeScriptOverview`(1043) | 單段/批次 CO-STAR；整體分析（主題/角色/地點/節奏） |
| 模型/估價 | `generationModels`(1214) / `estimateSegmentCost`(1273) | 列各模態模型+腦預設引擎 / 估每段點數 |
| 生成管線 | `autoGenerateFromSegments`(1589) / `executeGenerationTask`(1956) / `pollGenerationTask`(2211) | 批次備工 / 送單一 fal 任務（扣點+建 background job）/ 輪詢結果 |
| 規劃模式 | `planningDiscuss`(1309) / `planningAnalyzeDepth`(1414) / `savePlanningSession`(1474) / `listPlanningSessions`(1509) / `loadPlanningSession`(1524) / `deletePlanningSession`(1538) / `planningCreateMilestones`(1548) | 5 階段長片規劃迴圈＋情感深度分析＋轉里程碑 |
| 工作室橋接 | `askForStudioPlan`(2497) | 回 `AgentAction[]`＋rationale（接到 studio 分頁） |
| 靈感/查證 | `fetchTrendingInspiration`(2627) / `perplexityThrottleStatus`(2673) | Perplexity Sonar 趨勢靈感（含 citations）/ 配額狀態 |

### 3B. 四個模式（前端 `client/src/pages/DirectorAI.tsx`，⚠️ 路徑由子代理回報）

- **對話模式 (chat)**：AIChatBox + 右側 Storyboard 面板；雙引擎 RAG（事實研究 + 創意編排）+ 記憶注入 + personality。
- **腳本分析 (script)**：上傳/貼長腳本 → 解析 segment → 逐段討論 → CO-STAR → 整體分析 → 多格式匯出。狀態軌 `pending→draft→refined→approved`。
- **規劃模式 (planning)**：5 階段 `concept → outline → scene-planning → emotional-depth → schedule`，每階段強調「情感溫度/療癒」，產 `ScriptPlanningSession`，可轉行事曆里程碑。
- **世界觀 (worldbuilding)**：嵌世界觀編輯；生腳本時 `worldFrameworkId` 把世界 context 注入 system prompt。

### 3C. 人格（prompt 結構，✅ 已驗）

- `shared/director-personality-prompts.ts`：`type DirectorPersonality = "calm"|"creative"|"technical"`；`DIRECTOR_PERSONALITY_SYSTEM_PROMPTS`（共用切片）+ `DIRECTOR_PERSONALITY_LABELS`（沉穩型/創意型/技術型）。
- `server/services/director/personality.ts`：擴充 4 層（researchStyle / directorStyle / proactiveHint / systemPreamble）。
- 服務檔（✅ 已驗存在 7 支）：`costarService.ts`(`runDirectorAI`)、`scriptAnalysisService.ts`、`scriptGenerationService.ts`、`planningService.ts`、`exportFormats.ts`、`templates.ts`、`personality.ts`。

### 3D. 它編排什麼、產出什麼

腳本→生成一條龍（真實 pipeline）：
`importScript`/`generateVideoScript` → `ScriptSegment[]`（含 storyboard + 空 costar）→ `discussSegment`（逐段對話累積 insight）→ `generateSegmentCostar`（CO-STAR：context/situation/task/action/result/visualPrompt/audioScript/musicVibe/proactiveQuestion）→ `autoGenerateFromSegments`/`executeGenerationTask`（送 fal、扣點、建 background job）→ `pollGenerationTask` → 產出存 `generationHistory`。
產出資料型別（`shared/types.ts`）：`ScriptSegment` / `CoStarScript` / `ScriptOverview` / `ScriptPlanningSession`。

---

## 4. 其他子系統掃描（補盤點缺口）

### 4A. 提示詞庫（Prompt Library）— 真相是「一物多源聚合」

- `shared/promptReferenceLibrary.ts`：`PROMPT_REFERENCE_LIBRARY`（✅ **120 筆**），entry = `{id, modality, title, summary, prompt, negativePrompt?, modelHint?, difficulty, tags[], language, example?, references?}`。`PROMPT_MODALITY_META` 定義 **10 模態**（image/image-edit/video/audio/voice/3d/agent/system/workflow/multimodal），各帶 emoji + Tailwind 色。
- `shared/site-prompt-catalog.ts`：`getSitePromptCatalog()` **聚合 8 來源**（agent_role 25 精靈系統 prompt / proactive_trigger / director_personality 3 / model_template / image_studio / video_studio / pro_studio / prompt_reference 120）。entry = `SitePromptEntry{sourceType, sourceRef, title, content, category, tags[]}`。→ **這就是盤點看到「提示詞庫一物三放」的根因：它本來就設計成一個聚合器吃多源**。
- `shared/visual-inspiration-library.ts`：`buildVisualInspirationLibrary()` 從世界觀資料萃取視覺靈感（worldbuilding_asset/character_reference/scene_reference/style_reference）。

### 4B. 模型登錄（Model Registries）— 14 支登錄檔（`shared/`）

| 檔案 | 模態 | 數量 |
|---|---|---|
| `textToImageModelRegistry.ts` | 文生圖 | ✅ **5**（FLUX Pro 1.1 / FLUX Schnell / SD 3.5 Large / SeeDream v4 / Imagen4 preview） |
| `imageToImageModelRegistry.ts` | 圖生圖/編輯/control | ⚠️ 約 7+ |
| `imageToVideoModelRegistry.ts` | 圖生影 | ⚠️ 約 5+ |
| `imageUpscaleModelRegistry.ts` | 放大 | ⚠️ 約 4+ |
| `skeletalModelRegistry.ts` | 骨骼/姿態 | ⚠️ 約 3+ |
| `threeDModelRegistry.ts` | 3D | ⚠️ 約 3+ |
| `voiceModelRegistry.ts` | 語音/TTS | ⚠️ 約 8+ |
| `audioModelRegistry.ts` | 音樂/音效 | ⚠️ 約 10+ |
| `v2vModelRegistry.ts` | 影生影 | ⚠️ 約 4+ |
| `videoModelCatalog.ts` | 影片 router IDs | T2V 7 / I2V 7 / V2V 3（子代理回報） |
| `fineTuneModelRegistry.ts` | 微調 | ⚠️ 約 3+ |
| `unifiedModelRegistry.ts` | 統一聚合（798 行） | 吃以上全部 |
| `aiModelsCatalog.ts` | AI 模型情報（/ai-models-hub） | 策展 + 自動研究 |
| `falModelCapabilities.ts` | fal 模型能力表 | — |

> ⚠️ 除 textToImage(5) 為實際 grep 計數，其餘「+」數字為子代理估計，交接時請打開該檔以 `modelId:`/`label:` 計數核實。盤點頁面看到的「23 個圖像模型／21 個影片模型」是 **UI 分頁加總**，與單一 registry 不直接相等。

### 4C. 積分 / Credits / Points

- 估價：`shared/orb-cost-governor.ts`（`CostRiskTier = free/cheap/medium/expensive/premium`；`DEFAULT_COST_TABLE` 每工具 credits/durationMs/tier，如 generateImage=8、generateVideo=60、trainLora=200、director.suggestPlan=1）。
- 真實單價：`config/pricing-table.json`（fal_ai/gemini/elevenlabs/suno 各 unitType+costPerUnit）。
- 成本分析：`server/services/costAnalytics.ts`（summarizeByCategory/Endpoint/User/Status、latencyStats、calculateWasteCost、projectMonthlyCost、compareCatalogVsActual、suggestSavings）。
- 帳本表（`drizzle/schema.ts`）：`api_usage_logs`（requestType/apiProvider/costCredits/durationMs/responseStatus）、`aiUsageEvents`（provider/endpoint/units/costUsd/latencyMs）、`costAggregations`（每日 rollup）。`users` 表存 `quotaJson{image/video/audio/voice}`、`autoCreditEnabled/Amount/IntervalDays`、`remainingGenerations`。
- 賺取規則（與盤點頁一致）：註冊+50、首次分享資產+2、首次分享模型+3、管理員手動加分。**先扣後生成、失敗全額退、最小1pt、安全上限500pt**（此邏輯在退點/原子扣款測試 `server/atomic-deduction.test.ts`、`points-billing-audit.test.ts` 有覆蓋）。

### 4D. 生成管線（Generation Pipeline）

- Adapter registry：`server/services/ai-adapters/`（`bootstrap.ts`/`registry.ts`/`types.ts`）。
- 背景工：`server/jobs/`（`modelTrainingWorker.ts`、`apiUsageAlertJob.ts`、`providerSnapshotJob.ts`、`circuitBreaker.ts`、`apiHealthMonitor.ts`）。fal 佇列：`fal-queue-dispatcher`/`fal-queue-awaiter`（見同名 test）。
- 表：`background_jobs`（✅ 真實欄位見 §7，狀態 `queued→processing→completed/failed/cancelled`）、`generationHistory`（prompt/modelId/generationType/status/costCredits/durationMs）。
- Providers：FAL（主）+ Gemini / ElevenLabs / Suno adapters。
- 接縫（開發計畫 §2 對應）：`server/subsystems/commander`、`server/subsystems/contextPackets`、`server/subsystems/projectContext`、`server/subsystems/trainingTrack` 已有目錄（✅ 已驗存在）。

### 4E. 資產庫 / 一致性保險庫

- `digitalAssetLibrary` 表：assetType(`image/video/audio/voice/script/zip_bundle`)、fileUrl/fileKey/thumbnailUrl、visibility(`private/team_shared`)、rewardCredits、sourceStudio(`image-studio/video-studio/pro-studio`)、modelId、provider(`s3/r2/gcs`)、archivedAt/expiresAt/archivalChecksum。多組索引含 hot-path「未封存」索引。
- `consistencyVault` 表（一致性保險庫）：itemType(`character/scene`)、imageUrl/fileKey、tags、metadata。索引 (userId)、(userId,itemType)。
- assets router：`listAssets`(filter assetType/category/sourceStudio/visibility)、`uploadAsset`、`tagAsset`、`setVisibility`(private↔team_shared)、`archiveAsset`、`vaultItem.add/list`、`getAssetsBySource`。

### 4F. appRegistry（頁面導覽真相，✅ 已驗）

- `shared/appRegistry.ts`：`APP_PAGE_REGISTRY` **約 48–50 筆**。entry = `AppPageRegistryItem{id, label, path, routeAliases?, group, description, aliases[], showInSidebar, sidebarOrder?, sidebarGroup?, sidebarIcon?, showInAgentHome, agentEntryPriority, supportsPageAgent, quickActions[], orbHints[], supportedActions[]}`。
- `AppPageGroupId`（8）：`orb / create / train / project / assets / learn / settings / admin`。
- `SidebarGroupId`（4）：`creation-hub / six-systems / model-playground / management`。
- `supportedActions` 列舉：`setModality/setModel/fillPrompt/setParam/submit/reset/navigate/focusElement/setTab/search/openDialog/applyPreset`（這是精靈「編編」能在每頁做的動作集，§1D `page.*` 工具的後盾）。

---

## 5. 給 UI/UX 設計 / Claude Design 的「拍板待辦」（從程式碼真相導出）

1. **頁面權威清單**：以 `appRegistry.ts`（~48–50 筆）為單一真相，逐筆對 4-shell 落點；codex 卡片的 36/47 是 UI 統計，別當權威。
2. **25 精靈三層視覺**：specialist（會生成）/ role（人格+導航+規劃）/ proactive（主動，三種 surface：blocking/inline/toast）——每層各一套樣式。
3. **統一三個內部不一致**（§1F）：解解/體體、執執/步步、守守職責。**設計稿出去前先請工程拍板**，否則名片會自相矛盾。
4. **世界觀是巨型表單**：角色光是 v1–v4 就有數十欄位群（三視圖/表情/穿衣/語氣/語音/rig/lipsync/演技/年齡變體/聲音庫/真實參考）——需「漸進揭露」(progressive disclosure) 設計，不能一次攤平。preset 常數（§2E）可直接做成 chip。
5. **分鏡鏡號**：UI 顯示用 `S0X` 慣例即可，但要知道後端鍵是 `frame.id`+`seed`，三段輸出 imageUrl→refinedImageUrl→videoClipUrl 對應三個狀態徽章。
6. **導演 AI 四模式 + 三人格**：規劃模式是 5 階段精靈引導（情感溫度導向）——值得做成 stepper；人格切換（沉穩/創意/技術）影響語氣。
7. **提示詞庫一物多源**（§4A）：是「聚合器吃 8 來源」的刻意設計，重構時保留聚合、收斂入口即可，別重做三份。
8. **積分透明度**：先扣後生成/失敗退款/估價→確認的流程已在後端（cost-governor + pricing-table），UI 要把 `estimatedCostUsd`「先估再確認」做成一致門。

---

## 附：本文已實際驗證 / 仍待核對清單

**✅ 已 grep/讀檔驗證**：25 精靈集合（3 處交叉）、AgentRole=25、family 9/10/6、82 張表、worldview 3 表行號與欄位、worldbuilding-types 全 2695 行型別、director.ts 34 procedure 名與行號、director 7 服務檔存在、director 3 人格符號、appRegistry ~48–50、promptReferenceLibrary=120、textToImage=5、三個內部不一致。

**⚠️ 子代理回報、建議交接時打開原檔再核**：各模型 registry 的「+」數量、SPIRIT_PROACTIVE_TRIGGERS 精確筆數（16 vs 17）與文案、`spiritsVisual.ts` 個別 emoji/漸層色碼、`DirectorAI.tsx` 與 `SpiritHut.tsx` 的精確行號、各子 router（assets/creativeProject）procedure 是否還有未列出者。

---

# 【第二輪加碼深掘】背景任務／個人資料庫／各 Studio／提示詞庫 schema／模型登錄／未整理區＋RBAC

> 同樣唯讀、附真實檔案路徑＋符號、不杜撰。✅＝已 grep/讀檔驗證；⚠️＝子代理回報待核。

## 6. 背景任務（Background Tasks）

### 6A. 表 `background_jobs`（✅ `drizzle/schema.ts:281`，已逐欄驗證）

| 欄位 | 型別 | Null | 預設 |
|---|---|---|---|
| id | int PK autoinc | NO | |
| userId | int | NO | |
| **jobType** | mysqlEnum | NO | — |
| **status** | mysqlEnum | NO | `"queued"` |
| progress | int | NO | 0（0–100） |
| progressMessage | text | YES | |
| resultJson | json | YES | 通用結果 payload |
| errorMessage | text | YES | |
| createdAt / updatedAt | timestamp | NO | now / onUpdate |

- **jobType 8 值**（✅）：`image / video / audio / voice / zip_export / multimodal / model_training / teaching_archive_ingestion`
- **status 5 值**（✅）：`queued / processing / completed / failed / cancelled`
- 索引：`userId_status_idx`(userId,status)、`userId_createdAt_idx`(userId,createdAt)。型別 `BackgroundJob`。
- ⚠️ **更正第一輪文件**：之前 §4D 把 jobType 寫成 `image_generation/lora_training/voice_clone`、status 含 `pending` 與 `retryCount/resultUrl` 欄——**那是錯的**；真實如上（無 pending、無 retryCount 欄、結果存 `resultJson` 非 resultUrl）。

### 6B. 狀態機 / 續傳 / 重試（worker 端）

- 訓練 worker：`server/jobs/modelTrainingWorker.ts`。流程：`getQueuedJobsByType` 撿 `queued` → 標 `processing`（idempotent）→ fire-and-forget `runLoraTrainingJob` → 成功 upsert `resultJson`+`completed` / 失敗 `errorMessage`+`failed`。
- **卡住任務收割（Reaper）**：`recoverStuckTrainingJobs()`（同檔，⚠️ 約 line 178+）——`processing` 超過 **15 分鐘** 視為卡住，最多重試 3 次，靠 `getReplicateTrainingStatus()` 對 Replicate 核實。
- **斷路器**：`server/jobs/circuitBreaker.ts` `CircuitBreaker`（CLOSED→OPEN→HALF_OPEN，預設 3 連敗開路、冷卻 10 分）。
- 排程：背景 worker 每 5 分鐘（node-cron），`isWorkerRunning` 旗標防重疊。
- 前端續傳：`BackgroundTasksContext`（client）背景輪詢，切頁回來進度仍在；完成以 toast 通知。

### 6C. procedure（⚠️ 在 `server/routers.ts`，子代理回報行號）

- `generate.myJobs`(query, ~2891)：列使用者所有 job。
- `generate.activeJobs`(query, ~3684)：近 24h + 進行中。
- `generate.submitMultimodalAsync`(mutation, ~2910)：排入非同步生成，回 `{jobId, requestId}`。
- ⚠️ 未見明確的 cancel/retry top-level procedure（重試由 reaper 自動處理）；交接時可再確認是否要前台手動重試鈕。

### 6D. 告警表

- `orb_system_alerts`（⚠️ ~3113）：alertType `health_critical/health_warning/cost_spike/performance_degradation/error_rate_high/system_overload`。
- `alertConfigs`（⚠️ ~2067）：alertType `budget/quota/anomaly`。
- 背景工另含：`apiUsageAlertJob.ts`、`providerSnapshotJob.ts`、`apiHealthMonitor.ts`。

## 7. 個人資料庫（Personal DB＝RAG 記憶 + Context Packet + 教學典藏）

「個人資料庫系統」分頁實際是 **三件事的合體**：向量記憶（Pinecone）、Context Packet（壓縮上下文）、教學典藏（個人知識文件）。

### 7A. `context_packets`（✅ `drizzle/schema.ts:3421`）

| 欄位 | 型別 | 說明 |
|---|---|---|
| id / projectId / teamId? / userId | | |
| **scope** | enum `project/team/user` | 存取邊界 |
| sourceRefsJson | json | `ContextSourceRef[]` |
| summaryMarkdown | mediumtext | 壓好的摘要 |
| tokenEstimate | int | token 估算 |
| createdByModel | varchar(64) | 哪個模型壓的（deterministic 時為 null） |
| expiresAt | timestamp | TTL 快取失效 |
索引含 `cp_expiresAt_idx`（TTL 清理）。

### 7B. 向量記憶 `server/services/ragMemory.ts`（⚠️ 子代理回報）

- Pinecone index `ai-director-memories`，維度 **3072**（gemini-embedding-001），每 user 一個 namespace `user-{id}`，cosine。
- `MemoryRecord{userId, generationId, prompt, generationType, resultSummary?, vibeCardIds?, rating?}`。
- 函式：`upsertMemory` / `queryMemories(topK=3)` / `buildMemoryContext` / `queryRagMemory`（供 orb planner）。
- → **與素材的關係**：每次生成（generationHistory）會 `upsertMemory`，把 prompt+結果摘要嵌入，未來生成時做相似檢索注入 system prompt。**這是「光球記得我什麼」的後端**。

### 7C. 教學典藏 `server/routers/teachingArchive.ts`（個人知識文件，⚠️ 子代理回報）

- mediaType（7）：text/pdf/document/image/video/audio/presentation。
- visibility（3）：private/team_shared/public_disciples。
- sourceType（7）：discourse/group_practice/class/ceremony/publication/interview/other。
- 欄位含 textContent、transcriptionStatus（PDF/音/影進 OCR/ASR）、lineage、tags。
- procedure：`list/get/logView/create`；PDF/音/影 create 時 `enqueueTeachingIngestion()` 起 OCR/ASR。

### 7D. Context Packet / 資料來源 procedure（`server/subsystems/contextPackets/contextPacketRouter.ts`，⚠️）

- `contextPacket.compileProject`(mutation：projectId/mode/query?/forceRefresh?)、`getLatest`(query)。
- compile modes（4）：`create / director / video / assets`。
- `teamData.setProjectAccessRules/listProjectAccessRules`（owner/admin 限定）。
- `dataConnections.create/list/test/setStatus`——外部來源 `data_source_connections`（✅ `schema.ts:3504`）kind `cloud/notes/mcp/external_api`，provider google_drive/notion，auth oauth/api_key/none。
- accessLevel（4）：`none / summary_only / chunk_access / full_reference`（`project_data_access_rules` ✅ `schema.ts:3458`）。

> **與筆記/提示詞/素材的關係**：個人資料庫不是單一表，而是 RAG 把 generationHistory（素材）+ teaching_materials（文件）+ notes 聚合進 context_packet 餵 LLM。提示詞庫（§9）是獨立兩張表，**無 FK 直連**素材（設計上「生成當下快照 prompt」而非引用 live prompt）。

## 8. 創意工作室與各 Studio（資產 `sourceStudio` 的真實來源）

### 8A. 共用派工 `server/services/postGenActions.ts`（⚠️ 子代理回報，關鍵樞紐）

- `doPostGenComplete(params)`（~120）：一次寫 4 處——`prompt_library`（prompt≥4 字才存）、`digital_asset_library`（含 **sourceStudio**）、`generation_history`（parameterSnapshot 內含 sourceStudio）、監控 log；並 `enqueueMediaArchivalTask` 封存。
- `runPostGenForJob(jobId)`（~320）：webhook/輪詢回來時，讀 `background_job.resultJson` 取 `{studioType, modelId, prompt, resultUrl, sourceStudio, costPoints}` → `sourceStudio = meta.sourceStudio ?? studioType`。
- `digital_asset_library.sourceStudio`（varchar32）真實可能值：`image / video / audio / pro / creative / director / background / webhook / suno / replicate / unknown`；索引 `dal_userId_sourceStudio_idx`（✅ §4E 已記表）。

### 8B. 各 Studio 入口（⚠️ 子代理回報 router/page，交接時再核行號）

| Studio | 路由 | client | server router | 主要生成入口 | sourceStudio |
|---|---|---|---|---|---|
| 創作工作室（統一） | /studio | `Studio.tsx` | `routers.ts` | `generate.submitMultimodalAsync` | = generationType |
| 圖片創作室 | /image-studio | `ImageStudio.tsx` | `server/routers/imageStudio.ts` | 各模型 mutation + `checkImageStatus` | `"image"` |
| 影片創作室 | /video-studio | `VideoStudio.tsx` | `server/routers/videoStudio.ts` | 各模型 mutation + `checkVideoStatus` | `"video"` |
| 音樂配音創作室 | /pro-studio | `ProStudio.tsx` | `server/routers/proStudio.ts` | textToMusic/soundEffects/elevenLabsTTS… | `"pro"` |

- **統一 studio 參數**（`submitMultimodalAsync` zod）：prompt、generationType `image/video/audio/voice`、mode `lightning/deep_precision`、aspectRatio、negativePrompt、styleReferenceUrl、vibeReferenceUrl、videoDurationSeconds、firstFrameUrl/lastFrameUrl、characterRefUrl、cameraMotion{pan/zoom/tilt}、musicStyle/isInstrumental/audioDuration、voiceModelId/voiceText/voiceSpeed/voiceStability/voiceEmotionType/voiceEmotionIntensity、seed、overrideModelId、modelParams。
- **imageStudio procedure（按分頁）**：文生圖 `nanoBanana2 / nanoBananaPro / seedreamV4 / imagen4`；圖片編輯 9 個（nanoBananaProEdit/fluxKontext/grokEdit/gptImage15Edit…）；放大 `seedVRUpscale`；姿態 `dwPose`；SD `stableDiffusion35/fastSdxl/sdLora`；圖轉 3D `trellis2/sam3dObjects/hunyuan3d/rodin3d/hunyuanWorld`。
- **videoStudio procedure**：T2V 6（kling/wan/minimax/veo3/veo3Pro/ltx，sora→自動替代 veo3）；I2V（kling/klingPro/wan/runway/pixverse/minimax/ltx）；V2V（wan/kling）；增強（videoUpscale/frameInterpolation/topaz，皆失敗自動替代 wan v2v）；進階（camMaster/animateDiff/depthCrafter/viduReference）。多數 procedure 帶 `model_requested/model_used/degraded_reason` 替代中繼資料。
- **proStudio procedure**：音樂 textToMusic（sonauto/ace-step/stable-audio/musicgen，吃 brain 預設引擎）；soundEffects；TTS elevenLabsTTS（flash-v2.5→自動替代 turbo）/qwenTTS；克隆 qwenCloneVoice/qwenCloneAndSpeak/qwenVoiceDesign/diaTTSVoiceClone/elevenLabsVoiceClone；處理 demucs（分軌）/audioIsolation/mergeAudios；變聲 voiceChanger；ASR speechToText（NVIDIA，須 queue 輪詢）；klingCreateVoice。

## 9. 提示詞庫——完整 schema（✅ 兩張真實表已驗）

### 9A. `prompt_library`（✅ `drizzle/schema.ts:1759`）— 個人提示詞收藏

欄位：id, userId, title(varchar256), content(text), category(general/image/video/audio/voice/system, 預設 general), tags(json≤10), isFavorite(bool), isPublic(bool, 公開廣場), useCount(int), modelHint(varchar128), language(varchar8 預設 zh), generationMode(lightning/deep_precision/null), createdAt/updatedAt。索引 userId/category/generationMode。型別 `PromptLibraryItem`。

### 9B. `prompt_collection`（✅ `drizzle/schema.ts:1792`，migration 0063）— 站內提示詞「收藏夾」

欄位：id, userId, title, content, category, tags, notes(收藏理由), **sourceType**(agent_role/proactive_trigger/model_template/image_studio/video_studio/pro_studio/director_personality/prompt_reference/site_prompt/manual), **sourceRef**(去重鍵), sourceLabel(來源顯示如「圖圖 image-specialist」), visibility(private/team_shared), teamId, isFavorite, useCount。唯一索引 `(userId, sourceType, sourceRef)`。型別 `PromptCollectionItem`。

### 9C. 與 asset 的關聯

⚠️ **無外鍵直連**：`prompt_library`/`prompt_collection` 不 FK 到 `digital_asset_library` 或 `generationHistory`。`digital_asset_library.promptUsed`(text) 與 `generationHistory.prompt/compiledPrompt`(text) 是**生成當下的字串快照**。設計意圖：提示詞可獨立演進，生成紀錄保留當時實際用的 prompt。→ **設計含意**：若要做「這張圖用了哪個收藏提示詞」反查，目前只能字串比對，沒有 id 關聯。

### 9D. procedure

- `server/routers/promptLibrary.ts`：`list`(category/tags/search/generationMode 分頁) / `listPublic` / `getById` / `create` / `update` / `delete` / `toggleFavorite` / `incrementUseCount` / `adminSeed`(admin)。
- `server/routers/promptCollection.ts`：`siteCatalog`(回 `getSitePromptCatalog()`) / `listMine` / `listTeam` / `collect` / `update` / `setVisibility`(private↔team_shared) / `remove`。

## 10. 模型登錄 / 廠商清單——那 115 模型從哪來

### 10A. `shared/aiModelsCatalog.ts`（✅ 已驗）

- `AI_MODELS_CATALOG: AIModelEntry[]`（`:751`）= **靜態手工策展陣列，剛好 115 筆**（✅ grep `id:` = 115）。**不是 DB 表**——這是 /ai-models-hub「模型總數 115」的真實來源。
- `type ModelProvider`（`:38`）＝廠商 union，⚠️ 約 **35 家**（Anthropic/OpenAI/Google/Meta/Mistral/DeepSeek/xAI/Alibaba/Stability/Midjourney/Black Forest Labs/Runway/Pika/Luma/ElevenLabs/Suno/Udio/Microsoft/Moonshot/Zhipu/Tencent/Kling/Cohere/Voyage/MiniMax/Ideogram/Recraft/NVIDIA/GitHub/Cognition/Replit/Cursor/Manus/Perplexity/LangChain）。廠商數由 `new Set(m.provider)`（`:5312`）動態算。
- `AIModelEntry` 欄位：核心（id/name/apiId?/provider/modality `text/image/video/audio/multimodal/embedding/agent`/tier `frontier/balanced/lightweight/open-source`/releaseDate/tagline/description/strengths[]/limitations[]/useCases[]/contextWindow?/openWeight/tags[]/featured?/officialUrl?）＋ 自動研究 enrich（pricing/benchmarks[]/latestUpdates[]/availability/factCheck{status,checkedAt,provider,sources[]}/researchKeywords）。
- factCheck status：`verified/auto-checked/pending/stale/error`。`PROVIDER_STYLE`（`:372`）給每家廠商配色。

### 10B. 資訊型 catalog vs 功能型 registry（兩套，刻意分離）

- **資訊型**＝`aiModelsCatalog.ts`（純 metadata，給 /ai-models-hub 展示＋每日自動研究 `researchAndFactCheckAllModels()` via Perplexity）。
- **功能型**＝§4B 的 14 支 registry（textToImage/video/audio/voice…，含真實 fal modelId、推論端點）。
- ⚠️ 兩者**無 query-time 依賴**，新增模型需兩邊各自更新（潛在不同步風險，設計交接可提議統一）。

## 11. 未整理區域（/unorganized）＋ 管理後台 RBAC

### 11A. /unorganized 機制（`shared/appRegistry.ts`，⚠️ 子代理回報行號）

- registry 條目 `id:"unorganized"`、`showInSidebar:false`、`supportsPageAgent:false`。
- 填充邏輯：`getDockPages()` 只顯示 `VISIBLE_DOCK_PAGE_IDS` 白名單（`create / assets / director / teaching-archive`）；其餘 `showInSidebar:true` 但不在白名單的頁 → 落入 /unorganized fallback。⚠️ 約 30 頁被收在這裡（暫不在主選單、但仍可達，不 404）。

### 11B. RBAC（✅ 角色 enum 已驗）

- `users.role`（✅ `schema.ts:35`）= **`["user", "leader", "admin"]`**（注意有中間層 **leader**，不只 user/admin）。
- orb 對話訊息另有 `role`（✅ `:2455`）= `["user","orb"]`（不同語意）。
- admin 閘控：`adminProcedure`（`server/_core/trpc.ts`，檢查 `ctx.user.role === "admin"`），分散在各 router；集中的 `adminRouter.ts` 至少有 `runAgentEval`。⚠️ admin 端點散落多 router，非單一模組，交接時建議整併視角（與盤點「admin 應為閘控視角」呼應）。

### 11C. 團隊 ACL（team_shared 權限模型，✅ 表已驗）

- `teams`（✅ `:3757`）：id/name/description/ownerId。
- `teamMemberships`（✅ `:3776`）：teamId/userId/**role `["owner","admin","member"]`**(預設 member)/invitedBy/joinedAt，唯一索引 (teamId,userId)。
  - owner：轉移/解散；admin：邀請/移除成員、編全團隊素材；member：讀寫 team_shared、public_disciples 唯讀。
- 可見度模型（套用於 `digital_asset_library` / `fineTunedModels` / `prompt_collection`）：`visibility = private | team_shared`；team_shared 必設 teamId；private 時 teamId 為 NULL。**ACL 在應用層強制**（非 DB 約束）。
- `project_data_access_rules`（✅ `:3458`）：細到「每專案/團隊對某教學文件的 accessLevel」＋ allowedModesJson。
- `teaching_material_access_log`（⚠️ ~3811）：稽核 view/download/search_hit/reingest/update/delete。

## 12. 第二輪「待拍板 / 設計含意」補充

1. **背景任務 UI**：狀態 5 態（queued/processing/completed/failed/cancelled）＋ progress 0–100＋progressMessage；失敗只有 errorMessage、重試靠 15 分 reaper（無 retryCount 欄）。前台若要「手動重試」鈕需新增 procedure。
2. **個人資料庫不是一張表**：是「向量記憶＋Context Packet＋教學典藏」三合一，UI 要分三區呈現（不是單一列表）。
3. **提示詞 ↔ 素材無 id 關聯**：反查只能靠 prompt 字串快照——若 UX 想做「此提示詞產出的所有作品」需工程加關聯表。
4. **115 模型是手工策展靜態陣列＋每日自動查證**：UI 的「已自動查核 0/115、上次研究」對應 factCheck.status 與研究 job。
5. **三層角色**（user/leader/admin）＋ 團隊三角色（owner/admin/member）：權限 UI 要區分「站台角色」與「團隊角色」兩套。
6. **sourceStudio 11 值**是資產庫篩選的真實列舉（image/video/audio/pro/creative/director/background/webhook/suno/replicate/unknown），設計篩選器直接對映。

---

> 全程唯讀完成：未編輯任何檔案、未執行任何 git 指令、未改動 working tree、未安裝/建置。
