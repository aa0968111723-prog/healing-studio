# 啟動順序 — Bruce 授權後照這個跑

> **這是什麼**：把 P0 包 → 契約凍結 → 推分支 → GitNexus 索引 → 派 Codex/Antigravity → 三軌並行 → Claude 整合，串成一條**可照抄的施工序**。
> **誰跑**：標 **[Bruce]** 的由你授權／拍板；標 **[Claude-Code]** 的由 Claude 的 Code 工作階段執行（取得你授權後）；標 **[Codex]**／**[Antigravity]** 的是把對應簡報貼給它們後它們自走。
> **事實基準**：`main` HEAD `2888a36`。**全程旗標 OFF＝零行為改變**；任何一步可回滾。
> **狀態註記（2026-06-07）**：①–③ 已完成（umbrella 已推、**PR #852 已開，head `500a4e4d`**）；④ GitNexus 索引待跑；⑤ 派工改用 `啟動提示詞_給三代理.md`（含新增的 Claude Design 軌）。

---

## 📦 Downloads 目前實際有什麼（先對清單）

| 包 | 內容 | 狀態 |
|---|---|---|
| `AI-Director-P0補丁/` | **P0 路由 4-shell 骨架＋P1 SpineProvider＋5 接縫 adapter**（24 新檔＋5 編輯，旗標 OFF）＋`APPLY_GUIDE.md` | ✅ 可套用 |
| `AI-Director-模擬/` | sim 原型（mock）：`client/src/shells/{video,social,learn,settings}`、`adapters/types.ts`、`spine/`、`components/chrome` | ✅ 移植源 |
| `AI-Director-Supabase遷移/` | **P3**：82→86 表 parity＋additive（含 M3 `video_generation_sessions`/`video_segment_jobs`、`asset_generation_events`、`rag_embeddings`）＋`MIGRATION_GUIDE.md` | ✅ 待金鑰 |
| `AI-Director-整合包/` | `整合指南.md`・`adapter對應表.md`・`PR說明範本.md`・`風險與相容清單.md` | ✅ 參考 |
| `AI-Director-交接包/` | **本包**：`00_總交接_START_HERE.md`・`COORDINATION.md`・三份交接簡報・`啟動提示詞_給三代理.md`・`啟動順序.md`・`開源選型/` | ✅ 本次交付 |

> **⚠ 關於「P1P2 /video 補丁」**：**P1（脊椎）已含在 P0 包**裡（見 `APPLY_GUIDE.md §10`——SpineProvider＋5 adapter 一併進 P0）。所以「套 P0」＝同時落地 P0＋P1。**「P2 /video」不是一份預打包補丁，而是 Codex 在 `codex/4shell-video` 上、以 sim（`AI-Director-模擬`）為移植源建出來的**。下面 ② 即反映此事實。若你另有一份預打包的 `/video` 補丁要我用確切資料夾名引用，告訴我，我併進序列。

---

## 一張圖看懂順序

```
[相位 A｜Claude 單人，解鎖另兩方]
  ① 套 P0 包（含 P1 脊椎＋5 adapter）  ② 凍結契約 v1＋放 COORDINATION.md  ③ 推 umbrella＋P0 子分支
  ④ gitnexus analyze .＋複製 MCP 設定到 Codex/Antigravity
        │（契約凍結＝解鎖）
        ▼
[相位 B｜三軌並行，各守資料夾]
  ⑤ 派工：把 啟動提示詞_給三代理.md 對應段貼給對應 agent
     ├─ [Claude Design] claude/4shell-ui-design：D1 token＋D2 chrome＋D3 共用元件 → 四 shell
     ├─ [Codex]       codex/4shell-video：P2 cockpit＋確認門＋Mock 生成（零金鑰）
     ├─ [Antigravity] antigravity/4shell-social-learn：P5 /social＋P6 /learn 研究＋Gemini
     └─ [Claude]      P3 Supabase parity＋/settings＋E-QA（與上軌並行）
        │
        ▼
[相位 C｜Claude 會合]
  ⑥ P3 合入 → 解鎖 Codex P4 真實生成（HF/Fal，兩表已在 P3 additive schema）
  ⑦ Claude 整合閘：rebase → 依相依序合子分支 → 回歸三件套 → 對 main 開 PR → [Bruce] 拍板 push
```

---

## ① [Claude-Code] 套用 P0 包（含 P1 脊椎）

> 純前端、只加不刪、旗標 OFF＝行為 == 線上。**未碰 `server/`／`drizzle/`**。完整步驟見 `AI-Director-P0補丁/APPLY_GUIDE.md`。

```bash
cd /path/to/healing-studio
git fetch origin && git switch main && git pull            # 確認基準仍是 main HEAD 2888a36
git switch -c feat/4-shell-restructure                      # umbrella 分支
npm ci

# 取基準綠燈快照（「沒弄壞」的對照）
npm run check:routes | tee /tmp/baseline-routes.txt
npm run check:smoke  | tee /tmp/baseline-smoke.txt
npm run check:navigation | tee /tmp/baseline-nav.txt

PKG=/path/to/Downloads/AI-Director-P0補丁
# (1) 24 個新檔（new-files/ 鏡射 client/src/）
cp -R "$PKG/new-files/client/src/." client/src/
# (2) 5 個既有檔編輯（patch；先 --check 再套）
git apply --check "$PKG"/edits/{App.tsx,DashboardLayout.tsx,PersonalSettingsContext.tsx,useMobile.tsx,env.example}.diff
git apply         "$PKG"/edits/{App.tsx,DashboardLayout.tsx,PersonalSettingsContext.tsx,useMobile.tsx,env.example}.diff
# (3) parity 測試＋掃描腳本
mkdir -p client/src/__tests__ scripts
cp "$PKG/tests/route-parity.test.tsx" client/src/__tests__/route-parity.test.tsx
cp "$PKG/tests/scan-shell-routes.mjs" scripts/scan-shell-routes.mjs

# (4) 驗收（DoD）
node scripts/scan-shell-routes.mjs .     # 期望：✓ parity 全綠、零路由遺失
npm run test -- route-parity             # 旗標預設 OFF、收編無死轉址
npm run typecheck && npm run build
npm run check:routes && npm run check:smoke && npm run check:navigation   # 對照 baseline 不退步
git add -A && git commit -m "feat(spine): P0 4-shell 路由骨架 + SpineProvider + 5 接縫 adapter（旗標 OFF）"
```

**驗收門檻**：回歸三件套全綠且不退步；`scan-shell-routes` 全綠（21 redirects 全命中）；`route-parity` 通過；`typecheck`/`build` 通過；`git diff --stat` 僅見 client + `.env.example` + tests（未動 `server/`、`drizzle/`）。

---

## ② [Claude-Code] 凍結契約 v1 ＋ 放 COORDINATION.md

```bash
# 把本交接包的 COORDINATION.md 放進 repo 根（與 .mcp.json、AGENTS.md 同層）
cp /path/to/Downloads/AI-Director-交接包/COORDINATION.md ./COORDINATION.md
```
- 打開 `COORDINATION.md §0`，填 **契約版本 v1.0 ＋ 凍結日**（＝5 介面 `client/src/adapters/types.ts` 此刻定稿、不再隨意改）。
- 確認 §2 看板各列、§9 狀態總表正確。
- 提醒：**P1 脊椎已隨 P0 包落地**（SpineProvider＋5 adapter）；契約凍結＝廣播「介面定稿、可開工」。
```bash
git add COORDINATION.md && git commit -m "chore(coord): 放置 COORDINATION 訊息板 + 凍結接縫契約 v1.0"
```

---

## ③ [Claude-Code] 推 umbrella ＋ P0 子分支（Bruce 授權才 push）

```bash
# P0 落在 umbrella；如要保留可獨立 review 的 P0 子分支，另開一條指到同一 commit：
git branch claude/4shell-p0-routing
git push -u origin feat/4-shell-restructure        # ← [Bruce] 授權這一步
git push -u origin claude/4shell-p0-routing
```
> **只有 Claude 推真實 repo，且這一步要 Bruce 拍板**。Codex／Antigravity 之後從 `feat/4-shell-restructure` 開自己的子分支。
> **狀態**：本步已完成——**PR #852 已開（head `500a4e4d`）、未合併**。

---

## ④ [Claude-Code] GitNexus 建索引 ＋ 複製 MCP 設定

```bash
cd /path/to/healing-studio
npm install -g gitnexus                 # 或每次 npx -y gitnexus@latest <cmd>
gitnexus analyze .                       # 全 repo 一次索引（本機開啟語意嵌入/FTS）
gitnexus status                          # 確認索引完成
```
- **Claude Code**：repo 根 `.mcp.json` 已掛 gitnexus（開箱即連，首次按「信任」）。
- **[Codex]**：在 `~/.codex/config.toml` 加 `[mcp_servers.gitnexus]`（見 `交接_給Codex.md §6`）。
- **[Antigravity]**：Settings → MCP / Tools 貼 `mcpServers` JSON（見 `交接_給Antigravity.md §6`）。
> ⚠ MCP server 只查、不建索引——**先 analyze 一次**，否則工具回「Repository not indexed」。

---

## ⑤ [Bruce] 派工：把啟動提示詞貼給對應 agent

- 用 **`啟動提示詞_給三代理.md`**：§1 貼 **Claude Design**、§2 貼 **Codex**、§3 貼 **Antigravity**（或各貼整份 `交接_給○○.md`）。
- 三者**開工先讀 `COORDINATION.md` + `00_總交接_START_HERE.md`、先查 GitNexus**；卡住在 §2 看板寫「blocked 於誰」。

---

## ⑥ 相位 B — 多軌並行（各守資料夾、互不阻塞）

| 軌 | Agent | 分支 | 做什麼 | 等誰 |
|---|---|---|---|---|
| UI/UX | **Claude Design** | `claude/4shell-ui-design` | D1 token＋D2 chrome＋D3 共用元件 → D4–D8 四 shell | D1/D2 不等人；D3 起等 ② 契約凍結＋cockpit 骨架 |
| /video | **Codex** | `codex/4shell-p2-cockpit-gate` | P2 三欄 cockpit＋確認門狀態機＋Mock 生成（零金鑰） | 僅等 ② 契約凍結 |
| /social+/learn | **Antigravity** | `antigravity/4shell-p5-social`＋`…p6-learn-research` | P5 /social（零新表）＋P6 /learn 研究＋Gemini/Sonar adapter | 僅等 ② 契約凍結（P5 不需等 P2） |
| 後端/治理 | **Claude** | `claude/4shell-p3-supabase`＋`claude/4shell-settings-qa` | P3 Supabase parity＋/settings＋E-QA harness | 與上軌並行 |

> 各軌**寫不同資料夾→檔案層級零重疊→不可能 merge 衝突**。各軌成果掛自己的 flag、預設 OFF。

**P3 上線（Claude，需金鑰）**：照 `AI-Director-Supabase遷移/MIGRATION_GUIDE.md` —— 建 Supabase 專案 → `psql -f 0001_enable_pgvector.sql` → `drizzle-kit migrate --config drizzle.config.pg.ts`（對 Direct 5432）→ 雙寫/回填/校驗 → **`DATABASE_URL` 換 Pooler 字串＋`RAG_BACKEND=pgvector`** 即 live。

---

## ⑦ 相位 C — Claude 會合（整合閘）

1. **解鎖 Codex P4**：Claude 的 **P3 parity 合入 umbrella 後**，Codex 接 `codex/4shell-p4-director-video`（真實 HF/Fal；兩表 `video_generation_sessions`/`video_segment_jobs` 已在 P3 additive schema，落狀態機即可）。
2. **[Claude-Code] 依相依序合子分支**（每合一條跑回歸三件套＋契約測試，重建 GitNexus 索引）：
   ```
   p0-routing → p1-spine(契約) → ui-design → p2-cockpit-gate → p3-supabase
   → p5-social → p6-learn-research → p4-director-video → settings-qa / p6-mcp-harden
   ```
   - 生成選擇器 `server/services/generation/index.ts` 由 **Claude 獨佔**：把 Codex 的 `Mock/Hf/Fal` 與 Antigravity 的 `Gemini` 各掛一行接入（兩方都不改選擇器）。
   - 合併衝突多半只在 Claude 自己擁有的 `App.tsx`／`appRegistry.ts`，Claude 一手解。
3. **[Claude-Code] 對 `main` 開 PR**（用 `PR說明範本.md`，DoD 全綠）——**本步已完成：PR #852（head `500a4e4d`）**；後續為持續 rebase＋合子分支＋更新同一 PR：
   ```bash
   git switch feat/4-shell-restructure && git fetch origin && git rebase origin/main
   npm run check:routes && npm run check:smoke && npm run check:navigation
   npm run typecheck && npm run build && npm run test
   ```
4. **[Bruce] 最後拍板 push／合 PR**。整包合進 main 後仍 flag OFF＝線上零行為改變；逐軌開 flag 內測。

---

## 回滾（任一步可逆）

- **最快**：拿掉/設 0 對應 flag（`VITE_ENABLE_4SHELL`、`videoSessions`…）重新 build → 立即回線上行為。
- **逐接縫**：任一 adapter 出狀況，設該接縫 env=`mock` 隔離，不影響其餘。
- **還原碼**：`git revert <commit>` 或 `git switch main`（所有重構都在 `feat/4-shell-restructure`，未動 main）。
- **P3 DB**：把 `DATABASE_URL` 改回 `LEGACY_DATABASE_URL`（MySQL）＋`RAG_BACKEND=pinecone`、重部署。

---

## 一句話檢查表（Bruce 授權前確認）

- [x] P0 包套用後回歸三件套全綠、未動 `server/`/`drizzle/`、旗標 OFF == 線上。
- [ ] `COORDINATION.md` 已放 repo 根、§0 契約 v1 已凍結並填日期。
- [x] `feat/4-shell-restructure` 已推、**PR #852 已開（head `500a4e4d`）**。
- [ ] `gitnexus analyze .` 跑過、`gitnexus status` 綠；MCP 設定已複製到 Codex/Antigravity。
- [ ] `啟動提示詞_給三代理.md` 三段已分別貼給 Claude Design／Codex／Antigravity。
- [ ] P4 真實生成等 P3 parity 先合；最終對 main 的 PR 三件套全綠後 Bruce 才 push。

---

*本序列對齊 `AI-Director_多代理分工.md`（§2.1 三相位・§3.1 分支）、`AI-Director-P0補丁/APPLY_GUIDE.md`、`AI-Director-Supabase遷移/MIGRATION_GUIDE.md`、`AI-Director_GitNexus深度整合分析.md §G`。配套：`00_總交接_START_HERE.md`・`COORDINATION.md`・三份交接簡報・`啟動提示詞_給三代理.md`。*
