# aidv-optimize 第 2 輪審計 — 2026-06-27

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：4 模態 harvest（audit docs、Jira 看板、UI/UX 活碼、全端活碼）→ 對 origin/main 實證驗證 → 對看板去重 → 寫卡。
> **規模**：harvest 候選 ~18 → 驗證後 **新建 1 卡｜路由 2 筆｜丟 ~12 筆（已修/重複/待議）**。
> **基準**：HEAD（origin/main，2026-06-27 驗證）。

---

## 結論（一句話）

**最有價值的發現是「`orbFeatureDiscovery` 功能推薦引擎全為空殼」**（generateRecommendations 永遠回 []、所有核心方法皆 stub/TODO），已開成 [AIDV-548]（Medium，待議 A/B/C 實作策略）；第 1 輪 AIDV-161/162/163 誤報（稿件 12 commits behind main）已藉 HEAD 實證確認已修，本輪不重複產卡；drop 率高（~12/18）主因大量候選在 origin/main 已修或已有追蹤卡。

---

## §1 本輪新建卡（已對 origin/main 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-548** | Medium | opt-card, backend, 待議, decision | `server/services/orbFeatureDiscovery.ts`：`generateRecommendations()` 回 []、`recordRecommendationInteraction()` 無 DB 寫入、`findSimilarUsers()` 未實作、`aggregateFeatureAnalytics()` 未實作、`searchFeatures()` 未實作。碰推薦演算法選型→待 Bruce 拍板。 |

---

## §2 路由回擁有卡（已留言）

| 發現 | 路由到 | 內容 |
|---|---|---|
| Migration 0033 duplicate tag 仍在 | **AIDV-170**（已留言）| `drizzle/meta/_journal.json` idx=33 與 idx=73 均用 `0033_*` 標籤；兩支 SQL 皆存在。HEAD 2026-06-27 確認未修。 |
| orbLongTermMemory 語意搜尋 TODO | **AIDV-19**（已留言）| `server/services/orbLongTermMemory.ts:198` TODO：pgvector halfvec 上線後需對接向量相似度查詢，取代現行 LIKE 回退。 |

---

## §3 丟棄（已修／重複／不可動，**staleness 過濾的價值所在**）

| 候選 | 原因（origin/main 實證） |
|---|---|
| ThoughtChain post-hoc timestamps（ISSUE-01）| 舊路徑 `routers.ts:505-512` 已不存在；4-shell 重構後架構異動，無法驗證，丟棄。 |
| Cross-modal parameter inheritance（ISSUE-02）| **已修**：`client/src/lib/send-to-studio.ts` 含 `parameterSnapshot` 欄位。 |
| Voice preset EmotionProfile hardcoded | **已修**：AIDV-216 完成，`parseVoiceBlockPrompt()` 已實作。 |
| Compiler schema SSML validation | AIDV-172 已完成，不重複。 |
| costAnalytics 廢棄迴路 | AIDV-436 已完成，不重複。 |
| Stripe webhook event handlers 空殼 | **AIDV-167 已追蹤**（Backlog），不重複產卡。 |
| dispatch_task Edge Function fallback 未接 | **AIDV-522 進行中**，不重複產卡。 |
| providerSnapshotJob Fal/GCP billing TODOs | 外部 provider API 依賴，非程式碼缺陷，不可動，丟棄。 |
| projectGateway.ts `as any` / AIDV-162 | **已修**：origin/main `projectGateway.ts:35` 已有「取代 as any」標記。 |
| 缺 CI workflow | **已修**：`.github/workflows/pr-gate.yml` 存在，覆蓋 tsc + routes + navigation + vitest。 |
| AIDV-161 跨專案資料混入 | origin/main 確認已修（`listByWorld` 已套用）。 |
| AIDV-163 健壯性 4 連 | 上輪已建卡，本輪不重複。 |

---

## §4 Phase 2 / 待 Bruce 拍板

- **AIDV-548**：orbFeatureDiscovery 推薦引擎實作策略（A. 規則式標籤配對 / B. 協同過濾 / C. LLM 摘要推薦）—碰演算法選型，待 Bruce。
- **AIDV-19**（blocked/needs-key）：pgvector halfvec 維度確認後，接回 orbLongTermMemory.search() 向量查詢。

---

## §5 本輪校準紀錄

- 驗證基準：`git fetch origin main` + `git log origin/main --oneline -10`（2026-06-27 執行）。
- 第 1 輪誤報（AIDV-161/162/163）成因：本地副本落後 origin/main 12 commits；本輪全程以 `git show origin/main:<path>` 驗證，無重蹈。

— 智能助手 🤖
