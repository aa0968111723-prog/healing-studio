# aidv-optimize 審計 — 2026-07-03（Figma 搬遷 UI/UX）

> **方法**：多代理 harvest（Figma 搬遷稽核文件 + 現場 UI/UX 掃描 + design-kit token 深掘）→ 對 origin/main HEAD 實證 → 對看板 100 卡 + 近輪 opt-cycle 去重 → 已驗證卡規格。主迴圈親自建卡收口。
> **規模**：新建 **5 卡** ｜ 折疊/丟棄傘卡 3 ｜ 連結擁有軌 4。
> **特別**：依 Bruce 指示，本批**全為研究導向（`research`＋`待議`＋`decision`）**，不進自動開發，待一起討論。

## 結論（一句話）

本輪把 Figma 搬遷稽核的 UI/UX 缺陷收斂成 5 張 HEAD 實證卡，最高價值為 **AIDV-971（design-kit `.aidv-kit` 同名不同義 token 衝突）**——它是 1:1 token→Figma 遷移的**前置阻擋項**：全域與 `.aidv-kit` 對 `--muted`/`--surface-2`/`--surface-3` 三個名字賦予不同語意，不先正名，任何從 `:root` 建的 Figma 變數套到 design-kit 元件都會解析到錯的顏色。

## §1 本輪新建卡（對 HEAD 實證）

| 卡 | 嚴重 | 標籤 | 證據（file:line） |
|---|---|---|---|
| **AIDV-971** | High | opt-card, design-system, research, 待議 | `design-kit.css:21,22,28`（`--surface-2/-3`、`--muted:#897f70`）vs `index.css:105,184,185`（不同義）；56 使用端 |
| **AIDV-972** | Medium | opt-card, uiux, a11y, research, 待議 | 9px 層 269 處（`text-[9px]`257+`text-3xs`12，index.css:69）；10px 層 1453 處 |
| **AIDV-973** | Medium | opt-card, design-system, research, 待議 | `ManusDialog.tsx:55,70,74,83`、`PortfolioDetailDialog.tsx:86-104`、`IntelBentoGrid`/`ShowcaseMasonry` 重複 mood map |
| **AIDV-974** | Medium | opt-card, a11y, research, 待議 | icon-only 缺 aria-label 9 處：`FocusFlowPage.tsx:228,247,378,397`、`TeamsPage.tsx:427`、`TeachingArchive.tsx:1356`、`ProStudio.tsx:2851,2980,2119` |
| **AIDV-975** | Medium | opt-card, a11y, research, 待議 | 107 檔 import framer-motion、僅 30 守 useReducedMotion（74 缺）；`RippleTransition.tsx:103` 等 |

## §2 連結擁有軌（Relates）

- AIDV-971 → AIDV-195（design-token 收斂軌主卡）
- AIDV-973 → AIDV-195（同軌，批 II）
- AIDV-974 → AIDV-559（a11y 收斂軌）
- AIDV-975 → AIDV-827（a11y 動效軌）

## §3 折疊 / 丟棄

- **折疊**：sub-12px「~200 檔」傘卡 → 併入 AIDV-972（精確 9px/10px 分層）；hex「~30 檔」傘卡 → 併入 AIDV-973（plain-UI）＋其 chart palette 子範圍。
- **丟棄**：登入 `.login-cosmic` carve-out — 非缺陷，AIDV-91（完成）已把 token「全站套用（登入除外）」文件化，是既有可接受邊界。
- **丟棄**：`.aidv-kit` 其他別名（`--surface`/`--text`/`--shadow-sm`/`--line`）經對抗確認**非衝突**（新別名或刻意同值），把 AIDV-971 範圍鎖定在真正衝突的 3 個 token。

## §4 待 Bruce 討論（研究導向）

全部 5 張皆待討論。建議討論序：**AIDV-971（遷移前置、最高價值）→ AIDV-972（字級底線牽動視覺密度，需設計拍板）→ AIDV-973（ManusDialog token 化會使其響應暗色＝可見變化）→ AIDV-974 / AIDV-975（a11y，範圍明確，較低爭議）**。

## 完成指標

- 新卡 5：AIDV-971（High）、AIDV-972/973/974/975（Medium），全 Backlog、全 `research`＋`待議`。
- 連結 4；hub 彙總留言 AIDV-102。
- 來源稽核：`docs/figma-migration/decisions-and-audit.md`、`README.md`；搬遷 runbook `import-runbook.md`（見其頂部修正紀錄，尚待訂正後採用）。

— 智能助手 🤖
