# aidv-optimize 第 2 輪審計 — 2026-06-29

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：多代理 Workflow（4 模態 harvest → 2 視角對 HEAD 驗證＋對看板去重）。本檔＝該輪完整記錄，亦為下一輪去重錨點。
> **規模**：harvest 40 候選（docs:8 jira:8 uiux:10 fullstack:14）→ 驗證後 **新建 1 卡｜路由 3｜丟 ~25（已修/重複/已決策）**。
> **基準**：HEAD（`origin/main`，2026-06-29，50 commits ahead of 2026-06-21 基準）。

---

## 結論（一句話）

**本輪最有價值的發現是「`skillRegistryService.ts` 已完整實作但完全未串 tRPC Router，Wave S Admin 管理介面缺口」**（AIDV-664）；其餘三筆 a11y/成本/migration 殘留均路由回既有擁有卡；~25 個候選經對 `origin/main` HEAD 實證後丟棄（大量已在 50 個 commit 內修掉）。

---

## §1 本輪新建卡（已對 HEAD 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-664** | Medium | opt-card, backend, feature-gap, 待議, decision | `skillRegistryService.ts` 已建 6 個方法（seedOfficialSkills/installSkill/listSkills/getSkillById/updateSkillTrust/setSkillStatus），但 `server/routers/` 無任何 tRPC Router 串接、`client/` 零引用；Wave S Admin 管理介面（AIDV-129）無法使用此服務層。後端決策→待 Bruce 拍板後再開工。 |

---

## §2 路由回擁有卡（一軌一主，已留言）

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-597** | `costAnalytics.detectRetryChains()`（`costAnalytics.ts:693`）已實作但僅掛 admin dashboard（`apiUsage.ts`）；未連 `orbCostGuard.ts` 作自動降級決策。已留言附 file:line 與建議，歸此卡追蹤。 |
| **AIDV-559** | ① `CommandPalette.tsx` 缺 FocusTrap + `aria-modal` + `role="dialog"` + `aria-label`（AIDV-136 設計卡完成但未實作到碼）。② `AmbientVideo.tsx` 自動播放未檢查 `useReducedMotion`。已留言附 file 與修復建議，歸此卡 a11y 追蹤。 |
| **AIDV-170** | migration 0085 `ON UPDATE (now())` 語法錯誤（`drizzle/0085_skill_registry.sql:31`）已由 commit `949d98c` 修正，CI 守衛已在 `server/migration-prod-pending-block.test.ts` 加固。已留言確認修復狀態與後續 DB 驗證指令（`SELECT id, hash FROM __drizzle_migrations ORDER BY id DESC LIMIT 5`），歸此卡追蹤。 |

---

## §3 丟棄（已修／重複／已決策，staleness 過濾的價值所在）

| 候選 | 原因（HEAD 實證） |
|---|---|
| AIDV-572 快取無效化 | **已修**：commit `59f2eeb` 在 `promptLibrary.ts` 加入 `revalidateTag` 呼叫；HEAD grep 確認存在。 |
| `requestExport` dead 按鈕 | **已修**：commit `83ffbbc` 已實作 export pipeline（`exportService.ts` + router）；HEAD 有完整 export 流程。 |
| migration 0085 語法錯誤 | **已修**：commit `949d98c` 修正 `ON UPDATE CURRENT_TIMESTAMP` + 守衛測試；已路由 AIDV-170。 |
| Three event buses（EventEmitter / BroadcastChannel / WS） | **已完成**：AIDV-173 狀態完成（事件匯流整合），`origin/main` 已有統一出口。 |
| Director temperature 0.7→0.4 | **已修並 push**（上輪已確認，重複候選直接丟）。 |
| Auth 維持自建 JWT | **已決策**：AIDV-28 decision-resolved，無新可交付。 |
| `voiceCompiler` EmotionProfile 硬編 | **已實作**：`parseVoiceBlockPrompt()` + `resolvedCustomProfile` 機制存在（`voiceCompiler.ts:853`），非靜態寫死；Workflow 代理誤判。 |
| `AppleDockItem` 缺 aria-label | **已存在**：`AppleDockItem.tsx:47` 已有 `aria-label={label}`；grep 確認；Workflow 代理誤判。 |
| `audioCompiler.ts` 缺 schema 驗證 | **已存在**：`validateCompilerInput("audio", input, AudioCompilerInputSchema)` 在 `audioCompiler.ts:487`；非缺口。 |
| `FrameTimeline.tsx` 缺四態（loading/empty/error/success） | **非適用**：FrameTimeline 是上傳槽元件，非資料載入元件，既有 per-slot `isUploading` 狀態即符需求；不適用傳統四態設計模式。 |
| Fal 派發碎片化 | **已路由**：AIDV-164（Backlog/待議）是現任擁有卡，不重複開卡。 |
| Supabase parity 金鑰未到不切換 | **已路由**：AIDV-19（blocked/needs-key）重疊，不重複開卡。 |
| pgvector halfvec 維度 | **已路由**：AIDV-19 歸屬，不重複開卡。 |
| 孤兒頁清理 | **已路由**：AIDV-133 歸屬，不重複開卡。 |
| multi-step chain 改 xstate | **已路由**：AIDV-22 歸屬，不重複開卡。 |
| background_jobs 無 FK/TTL | **待議**：AIDV-164 已含此子項（#3 描述）；路由已完整，不重複。 |
| `promptLibrary.list` search 只 LIKE title | **上輪已記錄**（§2 提示詞庫搜尋）；本輪依一軌一主不重建卡。 |
| FALLBACK_CHAINS 無 compile-time 檢查 | **已路由**：AIDV-104（Backlog）歸屬，已留言；本輪確認仍 Backlog 未動，不重複留言。 |
| 大型重構 useGenerationTask hook | **已路由**：AIDV-71 歸屬；四 studio 複製問題已追蹤，不重複。 |
| a11y 對比實測（CommandPalette 外） | **上輪已路由 AIDV-118**（已完成）；CommandPalette 缺口已本輪路由 AIDV-559，不重複。 |
| `projectGateway.ts` 跨專案 loadProject | **已修**：AIDV-161/162 本輪初查均標 retracted/completed；HEAD grep 確認 `worldStoryboard.list` 已改用 `listByWorld`。 |
| design-kit token 漂移（硬 hex） | **已路由**：AIDV-92（design-kit ShotCard/載入態統一）歸屬；本輪 grep 未見新未追蹤漂移。 |
| `/social` 實 UI | **已路由**：AIDV-96 歸屬，Backlog 中，不重複。 |
| M2/M3 表（video_generation_sessions 等） | **已路由**：AIDV-18 歸屬，Backlog 中，不重複。 |
| AIDISC 收件匣重複候選（7 筆已完成卡） | **已完成**：比對看板快照，7 筆 Jira 候選對應卡均在完成欄，全部丟棄。 |

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 拍板（AIDV-664）**：skillRegistryService 已實作的 6 個方法是否要立即串 tRPC Router？作法有三：① 直接在 admin shell 加 `skillRegistry` router（低風險，後端純讀）② 等 Wave S 正式啟動再統一處理（同 AIDV-129 工作范圍）③ 只先加 `listSkills` + `getSkillById` 唯讀端點（最小介入）。描述工作表已在卡中；拍板後可立即開工（後端中風險，建議在 dev 環境先測）。
- **Phase 2 人工走查**：AmbientVideo `useReducedMotion` 的實際效果需真機確認（`prefers-reduced-motion` media query 在 autoplay 情境）；CommandPalette focus-lock 鍵盤焦點捕獲需手動 Tab 測試（均屬 AIDV-559 Phase 2 走查項）。
- **DB 確認（有 DB 存取時）**：AIDV-170 修復後確認 `__drizzle_migrations` 水位是否推進過 0085 到 0097。

---

## 完成指標

- 新卡 AIDV-664：Backlog（Medium，待議，labels: opt-card backend feature-gap 待議 decision）
- 路由留言：AIDV-597（costAnalytics gap）、AIDV-559（CommandPalette + AmbientVideo a11y）、AIDV-170（migration 0085 修復確認）
- 看板 hub（AIDV-102）本輪產出彙總留言已寫
- 本輪 issue link：AIDV-664 Relates to AIDV-129

— 智能助手 🤖
