# aidv-optimize 第 2 輪審計 — 2026-06-29

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：多代理 Workflow（4 模態 harvest → 2 視角對 origin/main 驗證＋對看板去重）。本檔＝該輪完整記錄，亦為下一輪去重錨點。
> **規模**：harvest ~60 候選 → 驗證後 **新建 7 卡｜路由 ~13 張既有卡｜丟 10（已修/重複/已完成）**。
> **基準**：origin/main HEAD `ea1925c`（fix AIDV-581 assets.myAssets）。

---

## 結論（一句話）

**最有價值的發現是「ai.chat 未讀 brain 推理槽設定造成光球代理模型選擇與 AI 大腦組態完全脫鉤」（AIDV-593，High/待議）以及「myAssets 硬上限 200 筆無游標分頁」（AIDV-595，Medium，PR #1067 遺留缺口）**；stripeWebhook 5 個事件仍為 TODO skeleton（路由 AIDV-167）、providerSnapshotJob 永久寫假資料（路由 AIDV-574）為重要接力信號；其餘 10 個候選（含 0085 ON UPDATE、fallback chain split、agentCapabilityRouter security 等）均已實證修好或卡已完成，過濾正確。

---

## §1 本輪新建卡（已對 origin/main 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-593** | Medium | opt-card, backend, brain, 待議, decision | `ai.chat` 使用 `preferEngine:'auto'`，完全不讀 brain 推理槽設定（`server/routers/ai.ts:427`）；光球代理模型選擇與 AI 大腦組態脫鉤。 |
| **AIDV-594** | Low | opt-card, backend, brain, testing | `brain.getHealthStatus` 在取不到模型時仍樂觀回 `{status:'healthy'}`（`server/routers/brain.ts`）；`ai-brain-settings.test.ts` 僅驗字串存在不對照 catalog。 |
| **AIDV-595** | Medium | opt-card, backend, frontend, perf | `assets.myAssets` 硬上限 `LIMIT 200`、無游標分頁（`server/routers/assets.ts`）；PR #1067 遺留缺口，第 201 筆資產對用戶隱形。 |
| **AIDV-596** | Low | opt-card, backend, error-handling | `history.ts` list / bookmarked：DB 錯誤 `catch { return [] }` 靜默回空，創作者誤以為生成歷史全空（`server/routers/history.ts`）。 |
| **AIDV-597** | Low | opt-card, backend, cost, 待議, decision | `costAnalytics.detectRetryChains()` 只用在 admin panel，未接 `orbCostGuard`；重試風暴不自動觸發降級（`server/services/costAnalytics.ts`）。 |
| **AIDV-598** | Low | opt-card, housekeeping, testing | `server/global-orb-executor.test.ts` 放在 `/server` 目錄（屬前端 orb 整合測試），測試歸類錯誤。 |
| **AIDV-599** | Low | opt-card, backend, migrations | `drizzle/0096_api_keys.sql`：INDEX 定義內嵌在 `CREATE TABLE`（違反三鐵則；應用 `information_schema` guard 分離）。 |

## §2 路由回擁有卡（一軌一主，已留言）

| 群 | 路由到 | 本輪新信號 |
|---|---|---|
| a11y / touch / token / 四態 | **AIDV-559** | 9 項：icon aria-label、button 44pt、空狀態 vs 錯誤、low-opacity 文字對比、token 顯示精度等（前一輪已開卡，本輪補全細節） |
| orb stubs + compiler schema | **AIDV-561** | `orbLongTermMemory` 3 方法 stub、`orbConversationEnhancer.enhance()` stub、`officialSkillAdapters.runCutRough` stub、scheduleItem/taskCard `z.any()` 驗證缺口 |
| 型別邊界 | **AIDV-570** | 四大工作室殼 `as any` 轉換、`notes.ts` `z.any().optional()` 過寬驗證 |
| 靜默失敗 / N+1 | **AIDV-563** | `assets.ts teamShared` N+1、`studio.ts` recipes/versions `catch {}`、`generate.ts` incrementModelUsage `catch (()=>{})` |
| 孤兒頁清理 | **AIDV-133** | 8 個孤兒頁（OrbPlayground / ComponentLibrary / DebugPanel / StyleGuide / TestPage / MigrationTest / FeatureFlags / ApiDocs）確認仍未掛路由 |
| useGenerationTask 抽 hook | **AIDV-71** | VideoStudio 確認未接 useGenerationTask，是四工作室中最後缺口 |
| 預扣款回退 | **AIDV-160** | `chargeForFalTask` 先扣後送仍存在（proStudio / imageStudio / videoStudio），失敗路徑 catch 不完整 |
| orbFeatureDiscovery stub | **AIDV-548** | `generateRecommendations` / `searchFeatures` / `getLeaderboard` 全為 stub，永遠回空陣列 |
| providerSnapshotJob fake data | **AIDV-574** | Fal.ai / GCP Gemini polling 均為 TODO，cron 每小時覆蓋寫假資料入 DB |
| stripeWebhook TODO | **AIDV-167** | 5 個 Stripe 事件（checkout / invoice.paid / invoice.fail / subscription.updated / subscription.deleted）均為 TODO skeleton |
| Progress UI 統一 | **AIDV-92** | 5 處獨立管理進度 UI（ImageStudio / VideoStudio / ProStudio / BackgroundTasks / Studio），確認未統一 |
| 重複 migration 號 + relations | **AIDV-170** | 0008/0033/0067 三號重複；`relations.ts` 只覆蓋 3 張表（DB ~30 張） |
| background_jobs TTL | **AIDV-169** | 無 TTL 欄位、無清理 cron，確認仍未修 |

## §3 丟棄（已修／重複／已完成，**staleness 過濾的價值所在**）

| 候選 | 原因（origin/main 實證） |
|---|---|
| migration 0085 `ON UPDATE (now())` | **已修**：commit `949d98c`（PR #1060），`ON UPDATE CURRENT_TIMESTAMP` 已就位 |
| FALLBACK_CHAINS 無 compile-time 檢查 | **已修**：`server/_core/fallbackPolicy.ts` 統一 fallback 鏈 |
| brain.upsert 缺 enum 白名單 | **已修**：`REASONING_MODEL_ALLOWLIST` 已存在並啟用 |
| ImageStudio / FAL_MODEL_CATALOG 不一致 | **已修**：catalog 已對齊 |
| Fallback 鏈分散兩處 | **已修**：統一至 `fallbackPolicy.ts` |
| prompt_library↔digital_asset junction 缺失 | **已修**：`prompt_assets` 表存在，已在 AIDV-8 追蹤 |
| AIDV-573 toast.success 競態 | **正常**：sonner 4s 預設，非 bug，不重複開卡 |
| skill_registry UI 缺 | **非 bug**：為後台管理頁，設計如此 |
| agentCapabilityRouter 安全缺口 | **AIDV-380 已完成**（狀態：完成），不重複 |
| CI workflows 缺失 | **AIDV-56 已完成**（狀態：完成），不重複 |

## §4 Phase 2 / 待 Bruce

- **待 Bruce 拍板**：
  - AIDV-593（ai.chat 未讀 brain 槽）：改法影響全站推理路由，需確認 brain 槽優先權語義
  - AIDV-597（detectRetryChains 未接 orbCostGuard）：涉及成本控制策略，需確認觸發閾值
  - AIDV-574（providerSnapshotJob fake data）：需 Fal.ai / Gemini API 規格確認
  - AIDV-167（stripeWebhook TODOs）：計費核心，需 Bruce 定義 webhook 處理流程
  - AIDV-160（chargeForFalTask 先扣後送）：生成關鍵路徑，需 hold/commit 架構決策

- **Phase 2 人工走查**：
  - AIDV-133（孤兒頁）：確認無深連結 / analytics / E2E 依賴後再刪
  - AIDV-169（background_jobs TTL）：確認資料保留政策後定 TTL 天數

---

— 智能助手 🤖
