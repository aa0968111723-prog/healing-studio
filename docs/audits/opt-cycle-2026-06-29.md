# aidv-optimize 第 2 輪審計 — 2026-06-29

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：多代理 Workflow（4 模態 harvest → 2 視角對 HEAD 驗證＋對看板去重）。本檔＝該輪完整記錄，亦為下一輪去重錨點。
> **規模**：harvest 40 候選（docs:8 jira:8 uiux:10 fullstack:14）→ 驗證後 **新建 6 卡｜路由 10｜丟 ~25（已修/重複/已決策）**。
> **基準**：HEAD（`origin/main`，2026-06-29，50 commits ahead of 2026-06-21 基準）。

---

## 結論（一句話）

**本輪最有價值的發現是「`skillRegistryService.ts` 已完整實作但完全未串 tRPC Router（AIDV-664）」與「ConsistencyVault VaultGrid API 錯誤靜默降級（AIDV-674）」**；另新建 4 卡含三 SSE 事件匯流需後端決策（AIDV-675）、四 Compiler 無共同介面（AIDV-676）、VoiceCompiler dead 欄位（AIDV-677）、imageStudio 26 個 `as any`（AIDV-678）；~25 個候選經 HEAD 實證後丟棄；9 張既有卡收到路由留言。

---

## §1 本輪新建卡（已對 HEAD 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-664** | Medium | opt-card, backend, feature-gap, 待議, decision | `skillRegistryService.ts` 已建 6 個方法（seedOfficialSkills/installSkill/listSkills/getSkillById/updateSkillTrust/setSkillStatus），但 `server/routers/` 無任何 tRPC Router 串接、`client/` 零引用；Wave S Admin 管理介面（AIDV-129）無法使用此服務層。後端決策→待 Bruce 拍板後再開工。 |
| **AIDV-674** | High | opt-card, frontend, error-handling | `ConsistencyVault.tsx:380` 的 `vaultQuery` 缺 `isError` 分支——API 失敗時元件靜默渲染空格狀態，使用者無錯誤反饋；加 `isError` guard + error UI 即可（auto-pass，純前端）。 |
| **AIDV-675** | Medium | opt-card, backend, 待議, decision | 三條並行 SSE 事件匯流（`generationBus` / `orbChatProgress` / `agentEventBus`）各自維護 client 映射，無統一路由層；需決策：合併為單一 `/api/events` SSE multiplexer 或保持三條但加 typed envelope。碰後端→待 Bruce 拍板。 |
| **AIDV-676** | Medium | opt-card, fullstack, type-safety | 四個 Compiler（`audioCompiler.ts:31`、`voiceCompiler.ts:79`、`videoCompiler.ts:32`、imageCompiler）各自宣告獨立 Input interface，無共同 base；Director 呼叫時依賴隱性型別相容，`compile()` 簽名不統一；加 `CompilerInput` / `CompilerOutput` 共同介面（auto-pass）。 |
| **AIDV-677** | Medium | opt-card, backend, dead-code | `VoiceCompilerInput.MoodBlock.emotionOverride` 欄位宣告於 schema 但 `resolveEmotion()`（`voiceCompiler.ts:769`）從未讀取；voice preset 系統透過 `resolvedCustomProfile` 機制覆蓋情緒，`emotionOverride` 為 dead field——移除或接線（auto-pass）。 |
| **AIDV-678** | Medium | opt-card, backend, type-safety | `imageStudio` router 26 個 `as any` 包住 fal.ai 回傳型別（不同於已完成的 AIDV-175 面板 any-cast）；fal schema 有公開型別定義可直接替換，消除靜默失敗風險（auto-pass）。 |

---

## §2 路由回擁有卡（一軌一主，已留言）

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-597** | `costAnalytics.detectRetryChains()`（`costAnalytics.ts:693`）已實作但僅掛 admin dashboard（`apiUsage.ts`）；未連 `orbCostGuard.ts` 作自動降級決策。已留言附 file:line 與建議，歸此卡追蹤。 |
| **AIDV-559** | ① `CommandPalette.tsx` 缺 FocusTrap + `aria-modal` + `role="dialog"` + `aria-label`（AIDV-136 設計卡完成但未實作到碼）。② `AmbientVideo.tsx` 自動播放未檢查 `useReducedMotion`。③ 本輪新增批次：`BackgroundTasksDrawer` 缺 `aria-expanded`；`VaultItemCard`/`UploadPanel`/`VaultDropzone` 多個 icon-only 按鈕缺 `aria-label`；`FrameTimeline` 清除按鈕 24px、`CharacterPanel` lock toggle 21px、`FocusFlowMini` 刪除按鈕 14px（均低於 44pt）；`ThoughtIslandChain` D3 動畫 + `FocusFlowMini` SVG 計時器未守 `prefers-reduced-motion`；`ThoughtIslandChain.STATUS_COLORS` 硬編 hex 未走 design-kit token。已留言附 file:line。 |
| **AIDV-170** | migration 0085 `ON UPDATE (now())` 語法錯誤（`drizzle/0085_skill_registry.sql:31`）已由 commit `949d98c` 修正，CI 守衛已在 `server/migration-prod-pending-block.test.ts` 加固。本輪補充：migration 編號 0008/0033/0067 疑有重複；`drizzle/relations.ts` 僅 3 條關聯定義（80+ 表）。已留言確認修復狀態與後續確認指令，歸此卡追蹤。 |
| **AIDV-650** | `ProStudio.tsx` 的 `submitStudioJob` 呼叫缺 `costPoints` 欄位——後端 schema 定義此欄但前端未傳入，導致成本計算靜默失敗。已留言附 file 與建議修法。 |
| **AIDV-646** | `requestExport` 後端（`exportService.ts` + router，commit `83ffbbc`）完整但前端 `client/` 零引用；匯出按鈕仍為 dead onClick / 「即將推出」。已留言附相關卡 AIDV-623 + AIDV-347。 |
| **AIDV-592** | `BackgroundTasksDrawer` 同時存在 tRPC 定時輪詢（`refetchInterval`）與 SSE 推播——兩條管線並存造成冗餘 DB 查詢（N×users 壓力）；部分空 `catch(() => {})` 構成 `unhandledRejection` DoS 向量。已留言附建議（移除輪詢，保留 SSE）。 |
| **AIDV-167** | `server/webhooks/stripe.ts` 5 個事件處理器（`payment_intent.succeeded/failed`、`customer.subscription.created/updated/deleted`）全為空 TODO stub——付款成功不增點、訂閱狀態不同步。已留言標 High 業務缺口、建議 needs-bruce。 |
| **AIDV-631** | Director `knowledge_base` prompt 插入欄位與 DB migration 定義無自動型別守衛（漂移風險）；四 Compiler 呼叫契約無 runtime 驗證（與 AIDV-676 互補，此條針對 Director↔Compiler 介面）。已留言附 zod parse 建議。 |
| **AIDV-548** | `orbLongTermMemory.ts` 搜尋仍用 SQL `LIKE '%keyword%'`（AIDV-435 完成的是 UI stub，非此搜尋品質問題）；待 AIDV-19（pgvector halfvec）解鎖後改 cosine similarity。已留言確認依賴關係。 |
| **AIDV-655** | `background_jobs` 表無 TTL / 清理機制仍確認存在（HEAD 驗證）；與本卡既有描述完全重疊，補確認留言，不重複開卡。 |

---

## §3 丟棄（已修／重複／已決策，staleness 過濾的價值所在）

| 候選 | 原因（HEAD 實證） |
|---|---|
| AIDV-572 快取無效化 | **已修**：commit `59f2eeb` 在 `promptLibrary.ts` 加入 `revalidateTag` 呼叫；HEAD grep 確認存在。 |
| `requestExport` dead 按鈕 | **已修（後端）**：commit `83ffbbc` 已實作 export pipeline；前端未接線已路由 AIDV-646。 |
| migration 0085 語法錯誤 | **已修**：commit `949d98c` 修正 `ON UPDATE CURRENT_TIMESTAMP` + 守衛測試；已路由 AIDV-170。 |
| Three event buses（EventEmitter / BroadcastChannel / WS） | **已完成**：AIDV-173 狀態完成（事件匯流整合），`origin/main` 已有統一出口。（SSE 匯流問題為不同層次，已建 AIDV-675。） |
| Director temperature 0.7→0.4 | **已修並 push**（上輪已確認，重複候選直接丟）。 |
| Auth 維持自建 JWT | **已決策**：AIDV-28 decision-resolved，無新可交付。 |
| `voiceCompiler` EmotionProfile 硬編 | **已實作**：`parseVoiceBlockPrompt()` + `resolvedCustomProfile` 機制存在（`voiceCompiler.ts:853`），非靜態寫死；dead field 問題已建 AIDV-677。 |
| `AppleDockItem` 缺 aria-label | **已存在**：`AppleDockItem.tsx:47` 已有 `aria-label={label}`；grep 確認；Workflow 代理誤判。 |
| `audioCompiler.ts` 缺 schema 驗證 | **已存在**：`validateCompilerInput("audio", input, AudioCompilerInputSchema)` 在 `audioCompiler.ts:487`；非缺口。 |
| `FrameTimeline.tsx` 缺四態 | **非適用**：FrameTimeline 是上傳槽元件，非資料載入元件，既有 per-slot `isUploading` 狀態即符需求；不適用傳統四態設計模式。 |
| Fal 派發碎片化 | **已路由**：AIDV-164（Backlog/待議）是現任擁有卡，不重複開卡。 |
| Supabase parity 金鑰未到不切換 | **已路由**：AIDV-19（blocked/needs-key）重疊，不重複開卡。 |
| pgvector halfvec 維度 | **已路由**：AIDV-19 歸屬，不重複開卡。 |
| 孤兒頁清理 | **已路由**：AIDV-133 歸屬，不重複開卡。 |
| multi-step chain 改 xstate | **已路由**：AIDV-22 歸屬，不重複開卡。 |
| background_jobs 無 FK/TTL | **已路由**：AIDV-655 歸屬（本輪補留言確認），不重複開卡。 |
| `promptLibrary.list` search 只 LIKE title | **上輪已記錄**（§2 提示詞庫搜尋）；本輪依一軌一主不重建卡。 |
| FALLBACK_CHAINS 無 compile-time 檢查 | **已路由**：AIDV-104（Backlog）歸屬，已留言；本輪確認仍 Backlog 未動，不重複留言。 |
| 大型重構 useGenerationTask hook | **已路由**：AIDV-71 歸屬；四 studio 複製問題已追蹤，不重複。 |
| a11y 對比實測（CommandPalette 外） | **上輪已路由 AIDV-118**（已完成）；CommandPalette + 新批次缺口已本輪路由 AIDV-559，不重複。 |
| `projectGateway.ts` 跨專案 loadProject | **已修**：AIDV-161/162 本輪初查均標 retracted/completed；HEAD grep 確認 `worldStoryboard.list` 已改用 `listByWorld`。 |
| design-kit token 漂移（硬 hex，非 ThoughtIslandChain） | **已路由**：AIDV-92（design-kit ShotCard/載入態統一）歸屬；本輪 grep 未見其他新未追蹤漂移。 |
| `/social` 實 UI | **已路由**：AIDV-96 歸屬，Backlog 中，不重複。 |
| M2/M3 表（video_generation_sessions 等） | **已路由**：AIDV-18 歸屬，Backlog 中，不重複。 |
| AIDISC 收件匣重複候選（7 筆已完成卡） | **已完成**：比對看板快照，7 筆 Jira 候選對應卡均在完成欄，全部丟棄。 |

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 拍板（AIDV-664）**：skillRegistryService 已實作的 6 個方法是否要立即串 tRPC Router？作法有三：① 直接在 admin shell 加 `skillRegistry` router（低風險，後端純讀）② 等 Wave S 正式啟動再統一處理（同 AIDV-129 工作范圍）③ 只先加 `listSkills` + `getSkillById` 唯讀端點（最小介入）。
- **待 Bruce 拍板（AIDV-675）**：三條 SSE 事件匯流是否合併為單一 multiplexer？影響後端 subscription 管理架構，碰關鍵路徑。
- **待 Bruce 拍板（AIDV-167）**：Stripe Webhook 5 個空 handler 的實作優先序——付款閉環缺失，建議列 High 並排入下一 Wave。
- **Phase 2 人工走查**：AmbientVideo `useReducedMotion` 的實際效果需真機確認；CommandPalette focus-lock 需手動 Tab 測試；`ThoughtIslandChain` D3 動畫 + `FocusFlowMini` SVG 計時器的 reduced-motion 行為需真機驗證（均屬 AIDV-559 Phase 2 走查項）。
- **DB 確認（有 DB 存取時）**：AIDV-170 修復後確認 `__drizzle_migrations` 水位是否推進過 0085（直到 0097）；確認 migration 編號重複是否真實存在（0008/0033/0067）。

---

## 完成指標

- 新卡 6 張：AIDV-664（Medium，backend，待議）、AIDV-674（High，frontend，auto-pass）、AIDV-675（Medium，backend，待議）、AIDV-676（Medium，fullstack，auto-pass）、AIDV-677（Medium，backend，auto-pass）、AIDV-678（Medium，backend，auto-pass）
- 路由留言 10 張：AIDV-597、AIDV-559（含批次 a11y）、AIDV-170（含 migration 重複）、AIDV-650、AIDV-646、AIDV-592、AIDV-167、AIDV-631、AIDV-548、AIDV-655
- 丟棄 ~25 個候選（已修/重複/已決策，經 HEAD 實證）
- 本輪 issue link：AIDV-664 Relates to AIDV-129；AIDV-675 Relates to AIDV-173；AIDV-676 Relates to AIDV-631；AIDV-678 Relates to AIDV-164

— 智能助手 🤖
