# aidv-optimize 第 2 輪審計 — 2026-06-29

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：4 模態 harvest（docs/jira/uiux/fullstack） → 對 origin/main HEAD 驗證 → 看板去重 → 產卡/路由/丟棄。
> **規模**：harvest 53 候選 → 驗證後 **新建 6 卡｜路由 11 則｜丟 8（已修/重複/已追蹤）**
> **基準**：HEAD commit `7ccd4c1`（2026-06-29），PR#1058-1074 均已合入 main。
> **前輪錨點**：`docs/audits/opt-cycle-2026-06-21.md`（AIDV-161/162/163 已撤回，AIDV-164 仍 Backlog/待議）

---

## 結論（一句話）

**本輪最重要的發現是三個資金/記帳安全缺口：Suno webhook 缺請求簽章（AIDV-615）、chargeForFalTask 先扣後送 submit 失敗點數永久損失（AIDV-620）、以及 notes/creativeProject 全量拉取 OOM 風險（AIDV-616/617）**；其餘健壯性、效能、UX 各開一卡；路由 11 則補齊現有卡的新發現點。

---

## §1 本輪新建卡（已對 HEAD 7ccd4c1 實證）

| 卡號 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-615** | High | opt-card, security, 待議, decision | Suno webhook 缺請求簽章驗證：`webhookSuno.ts` 只做 token DB lookup，不做 HMAC/Ed25519 body 簽章（對比 fal=Ed25519、Stripe=HMAC）；攻擊者若取得 webhook URL token 可偽造回調。待議：Suno 是否提供 signing secret。 |
| **AIDV-616** | High | opt-card, backend, perf | notes.list/summary/exportIcs 全量載入：三個 procedure 均呼叫 `getProjectNotesByUser()` 無 LIMIT，重度用戶累積千筆筆記時可能 OOM（Railway 512MB）。 |
| **AIDV-617** | Medium | opt-card, backend, perf, frontend | creativeProject.list 全量載入：`listPaginated` 已存在但 8 callsite 仍用無 limit 的 `list`。 |
| **AIDV-618** | Medium | opt-card, frontend, perf, backend | Admin 面板 UsersCreditsTab+AuditTab 無虛擬化/分頁：`usersWithCredits` 無 limit 全量 + `auditLogs limit:100` 全渲染，用戶增多後 Admin 卡頓。 |
| **AIDV-619** | Medium | opt-card, frontend, uiux | 全站操作「改變有感」UX optimistic update 缺失：筆記儲存、角色設定更新、專案重命名等操作均為 server-first；落地 Bruce AIDV-89 指示。 |
| **AIDV-620** | High | opt-card, backend, billing, 待議, decision | chargeForFalTask 預扣競態：先扣點再 fal submit，transient submit 失敗（fal 5xx）造成用戶永久點數損失；`refundJobIfBilled` 只覆蓋 webhook 失敗路徑，不覆蓋 submit 失敗。待議：選項 A（hold）/ B（submit-first）/ C（catch rollback）。 |

---

## §2 路由補充（已留言到擁有卡）

| 擁有卡 | 新增發現摘要 |
|---|---|
| **AIDV-559** | FeedbackDialog/AuthExpiredModal 缺 focus-trap+Escape；CreditsUsagePanel 缺 isError；PipelineCanvas/ThoughtIslandChain/OrbCreationStage 硬編碼 HEX；TemplatePicker 缺 focus-visible；AidvShellChrome credits 載入空白（6 項，8 發現合計） |
| **AIDV-582** | OrbUnifiedAssistant.tsx 5 處 + WorldbuildingInlineEditor + CommandPalette 仍直接 `toast.error(e.message)`，未用 PR#1074 的 `toastError` helper |
| **AIDV-592** | 複合 DoS 風險：AIDV-583（3s 輪詢）＋本卡（無速率限制）複合效應；建議先落地 583 再設閾值 |
| **AIDV-580** | OAuth CSRF prod flag 待 Bruce 在 Railway Variables 確認激活 |
| **AIDV-572** | brain.upsert category z.string() 無 enum 白名單（另路由 AIDV-594）；z.any() 殘點待評估 |
| **AIDV-561** | orbClarificationEngine.predictClarificationNeed:747-770 永遠回傳固定值、零 callsite → dead stub |
| **AIDV-594** | brain.upsert 缺 category enum 白名單；GENERATION_MODALITIES_KNOWLEDGE ↔ falModels.ts 缺一致性快照測試；falDispatcher.test.ts stale model slug |
| **AIDV-170** | drizzle/relations.ts 空白（無 Drizzle relations 定義）；migration 號碼 0008/0067 各重複兩次 |
| **AIDV-19** | orbLongTermMemory.ts:198 TODO LIKE→向量搜索，需與 pgvector 遷移一起落地 |
| **AIDV-565** | promptLibrary.list search 只搜 title，忽略 content 欄位 |
| **AIDV-601** | teamAssets catch{return[]} HEAD 7ccd4c1 確認仍存在，屬本卡 |

---

## §3 丟棄（已修／已追蹤，**staleness 過濾的價值所在**）

| 候選 | 原因（HEAD 實證） |
|---|---|
| migration 0085 ON UPDATE (now()) | **已修**：PR#1060（commit `949d98c`）`ON UPDATE CURRENT_TIMESTAMP` ✅ |
| fal dispatch fragmentation | **已追蹤**：AIDV-164 Backlog/待議，非重複 |
| three event buses | **已完成**：AIDV-173 完成，PR#1064 unified bus ✅ |
| Voice EmotionProfile hardcoded | **已完成**：AIDV-197 完成，voiceCompiler 現支援 customBlocks ✅ |
| CI workflows (.github/workflows 缺失) | **已修**：`.github/workflows/pr-gate.yml` 存在 ✅ |
| db:push 未接入 deploy flow | **已接入**：startup migration runner 已啟動時自動執行 ✅ |
| wouter postinstall 未自動 | **已修**：`package.json` 有 `"postinstall": "patch-package"` ✅ |
| @assets tsconfig alias 缺失 | **已修**：tsconfig.json 有 `"@assets/*": ["./attached_assets/*"]` ✅ |

---

## §4 待 Bruce 決策

- **AIDV-615**（Suno webhook 簽章）：Suno 是否有 signing secret API？若有則直接實作；若無則評估現行 token 機制的安全性充分性。
- **AIDV-620**（chargeForFalTask hold/escrow）：三個修法選項請 Bruce 指定方向後開工。AIDV-160 同題先決策再合批執行。
- **AIDV-617 migration**：`creativeProject.list` 廢棄後 8 callsite 需前端 changeset，建議 Bruce 安排一個 sprint。

---

## §5 本輪品質對帳

- **驗證方法**：`git show origin/main:<file>` + `grep origin/main` 對每個候選實證，確保非本地工作副本
- **去重錨點**：上輪（opt-cycle-2026-06-21.md） + searchJiraIssuesUsingJql 查重各候選 summary 關鍵詞
- **鐵則遵守**：只建 Backlog 卡、未自動合碼/轉欄/改既有卡狀態、待議卡加 label `待議,decision`

— 智能助手 🤖
