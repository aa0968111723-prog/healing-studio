# aidv-optimize 第 2 輪審計 — 2026-06-25

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：對 origin/main 直接驗證（HEAD: 47f07a9），聚焦 2026-06-21 以來合併的新代碼（AIDV-323/331/358 等）。
> **規模**：harvest 13 候選 → 驗證後 **新建 2 卡｜路由 1｜丟 8（已修/已追蹤）**。
> **基準**：origin/main commit 47f07a9（feat(multi-agent): AIDV-331）。

---

## 結論（一句話）

**本輪最重要的發現是 AIDV-331 新合併的多代理代碼引入兩個資安缺口**：SSE 訂閱端點的 IDOR（AIDV-379，可 auto-pass 修復）和代理能力登記無歸屬驗證（AIDV-380，需 Bruce 決策身份授權模型）；C13 安全報告的所有問題已被 AIDV-377（H14 autodev）在本輪前修復完畢，全數丟棄不重複產卡。

---

## §1 本輪新建卡（已對 origin/main 實證）

| 卡 | 優先 | 標籤 | 設計門 | 一句話 |
|---|---|---|---|---|
| **AIDV-379** | High | opt-card, security, idor, multi-agent | AUTO-PASS | `server/agentEventsRoute.ts:46` 身份驗證通過但未查 collaborationId 歸屬，任意登入用戶可訂閱他人代理事件流。修復：加一行 DB WHERE 查詢。 |
| **AIDV-380** | High | opt-card, security, multi-agent, 待議, decision | needs-bruce | `agentCapabilityRouter.ts` register/heartbeat 無 agentId 歸屬驗證，惡意用戶可搶注系統 agentId 或毒害心跳路由。需 Bruce 選定授權模型（Option A/B/C）。 |

---

## §2 路由留言（一軌一主）

| 擁有卡 | 內容 |
|---|---|
| **AIDV-326** | AIDV-331 已實作 `allowedEndpoints` scope 守門（commit 47f07a9），建議 Bruce 確認是否滿足驗收可標完成。留言已送出。 |

---

## §3 丟棄（已修／已追蹤，staleness 過濾的價值所在）

| 候選 | 原因（origin/main 實證） |
|---|---|
| C13: `agent_task_dlq` SECURITY DEFINER VIEW | **已修**：AIDV-377 H14 migration `h14_p0a_*` 已部署，Supabase Advisor 0 findings |
| C13: `set_updated_at` mutable search_path | **已修**：AIDV-377 P1-B `ALTER FUNCTION ... SET search_path` 已執行 |
| C13: 3 RLS per-row `auth.uid()` | **已修**：AIDV-377 P1-C `(SELECT auth.uid())` subquery 已套用 |
| C13: 3 unindexed FKs (agent_tasks/video_projects/video_segments) | **已修**：AIDV-377 P1-D 索引已建 |
| C13: 2 unused indexes | **已修**：AIDV-377 P1-D 已清除 |
| Agent Simulator / cron heartbeat | **已追蹤**：AIDV-378 [Backlog] 待 Bruce 批准，完全重複 |
| Realtime subscription 缺失 | **已追蹤**：AIDV-341/344/370 已追蹤，需 Bruce 後台操作 |
| AIDV-326 Agent Scope 缺口 | **已實作**：AIDV-331 `allowedEndpoints` 覆蓋，路由留言已送確認 |

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 決策（優先）**：
  - AIDV-380：代理身份授權模型（Option A 系統帳號白名單 / B userId 前綴 / C scope 守門），決策後 autodev 可立即執行
  - AIDV-326：確認 allowedEndpoints 是否滿足驗收條件
- **持續等待 Bruce 後台操作**：
  - AIDV-378（Agent Simulator cron）
  - AIDV-341/344/370（Realtime subscription 修復）

---

## §5 下輪建議聚焦

下輪（~90 分後）建議重點掃描：
1. `server/routers/agentCapabilityRouter.ts` 的 `assign` 端點是否也有 IDOR（分派到他人 project 的 agent 任務）
2. `server/services/agentCollaborationOrchestrator.ts` 的 in-memory `activeSessions` 重啟後丟失問題（若 AIDV-345 尚未涵蓋）
3. `server/services/priorityQueue.ts` 的 `QUEUE_KEY` 是否需要 per-project 隔離（目前 global queue，low priority）

— 智能助手 🤖
