# aidv-optimize 第 6 輪審計 — 2026-06-30（R6）

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：直接 code 分析（origin/main HEAD aead242）—— 聚焦 R5 基準（b777394）→ aead242 之間 17 commits 引入的新缺口，特別是 7dc3769（代理安全基礎設施 DLQ/Quota/ScopeGuard）與 AIDV-864（feedback router）。
> **規模**：直接分析 → 驗證後 **新建 7 卡｜路由 1｜丟 0**。
> **基準**：HEAD（`origin/main`，2026-06-30 aead242，以同日第 5 輪 b777394 為去重錨點）。
> **聚焦**：aead242 新引入的代理安全基礎設施（agentDlq、agentCreatorQuota、agentScopeGuard）接線缺失，以及 feedback router 新增的速率限制與 ownership 驗證缺口。

---

## 結論（一句話）

**本輪最重要的發現是三個 High 接線缺失：`agentDlq.insertDlqEntry`（AIDV-877）、`agentCreatorQuota.tryAcquireCreatorSlot`（AIDV-878）、`agentScopeGuard.assertAgentScope`（AIDV-879）均在 commit 7dc3769 實作完畢且有測試，但在 production dispatch 路徑中從未被呼叫，致使整套代理安全基礎設施在執行期完全無效；另建 2 Medium（feedback router 安全缺口）＋ 2 Low（VoiceAmbientCanvas isError、skillRegistry Admin UI 後續），路由 1 筆至 AIDV-675（orbChatProgress Bus 2 缺席），丟棄 0 筆。**

---

## §1 本輪新建卡（已對 origin/main HEAD aead242 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-877** | High | opt-card, backend, tooling, agent-infra | `server/services/agentDlq.ts` 的 `insertDlqEntry` / `pollDlq` 從未在任何 production handler 呼叫（`grep -r "insertDlqEntry" server/ --include="*.ts" -l` 只回傳 agentDlq.ts 自身）；commit 7dc3769 建立 DLQ 服務但未接線，agent 失敗完全不進 DLQ。修法：在 `validationGateRouter.ts:routeValidationFailure()` 呼叫 `insertDlqEntry`，並在 `server/_core/index.ts` 排程 `pollDlq()`。 |
| **AIDV-878** | High | opt-card, backend, agent-infra, perf | `server/_core/agentCreatorQuota.ts` 的 `tryAcquireCreatorSlot` / `releaseCreatorSlot` 從未被呼叫（grep 只回傳自身）；`ENABLE_AGENT_QUOTA` feature flag 存在但 task dispatch handler 從未查詢；每人並發配額在 runtime 零強制。修法：在 task dispatch 入口 guard 加 `if (!tryAcquireCreatorSlot(userId, MAX_CONCURRENT)) throw TRPCError(...)`。 |
| **AIDV-879** | High | opt-card, backend, security, agent-infra, 待議, decision | `server/_core/agentScopeGuard.ts` 的 `assertAgentScope` / `checkAgentScope` 從未被呼叫（grep 只回傳自身）；16 個 scope action（read:project, write:training, publish:project…）的角色邊界在 runtime 零強制，任何代理可執行任意 action。**需 Bruce 決策**：各代理 role 指派確認後才能啟用，否則誤封鎖正常操作。 |
| **AIDV-880** | Medium | opt-card, backend, security | `server/routers/feedback.ts:27-75` `create` mutation 使用 `protectedProcedure` 但**無速率限制**；已登入使用者可無限提交，致 DB 爆量。既有先例：`chatRouter.ts` 以 `tryConsumeChatToken` 限流。修法：新增 `feedbackToken` bucket（每使用者每小時 20 次），超限回 `TOO_MANY_REQUESTS`。 |
| **AIDV-881** | Medium | opt-card, backend, security, 待議, decision | `server/routers/feedback.ts:43` `screenshotKey` 直接由 client 傳入寫 DB，無 ownership 驗證；攻擊者可把他人截圖 key 附在自己的回饋上。**需 Bruce 決策**：方案①DB記錄`(key,userId)` / ②key 格式加 userId 前綴 / ③純 client 端不存 key。建議方案①。 |
| **AIDV-882** | Low | opt-card, frontend, error-handling | `client/src/shells/video/canvas/VoiceAmbientCanvas.tsx:100` AIDV-860 新增的 fal 健康指示器只處理 `isLoading`＋`data` 兩態，`isError` 時整塊消失（`providerStatusQ.data` 為 undefined → 條件 false）。修法：加 `providerStatusQ.isError &&` 分支顯示錯誤提示。**Relates to AIDV-860**。 |
| **AIDV-883** | Low | opt-card, frontend, backend | `server/routers/skillRegistryRouter.ts` 只暴露 `listSkills`/`getSkillById`；`skillRegistryService.ts` 的 `installSkill`/`updateSkillTrust`/`setSkillStatus` 未接入 tRPC；`AdminPage.tsx:2189` SkillRegistryTab 只有唯讀表格，無寫操作 UI。**Relates to AIDV-848**。 |

---

## §2 路由留言

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-675** | `server/services/sseRouter.ts` 整合 Bus 1（generationBus）✓ Bus 3（agentEventBus）✓，但 Bus 2（orbChatProgress）仍缺席；`docs/sse-routing-contract.md` 記錄 Bus 2 用 in-memory Map + polling（TTL 60s），非 EventEmitter。建議評估改 EventEmitter 或加 polling adapter 完成三路統一；已留言。 |

---

## §3 丟棄

本輪無丟棄候選（所有發現均為 aead242 新引入缺口，已在 HEAD 實證）。

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 決策（AIDV-879 role 指派）**：啟用 `assertAgentScope` 前需確認各代理（orchestrator/sub-agent/tool-agent）的 role 對應；建議 Bruce 先列出現行代理類型清單，再對照 ROLE_SCOPES 矩陣確認 coverage 後開啟 flag。
- **待 Bruce 決策（AIDV-881 screenshotKey 方案）**：需確認截圖上傳 endpoint 位置（本輪 codebase 未找到）再評估工作量；建議先確認是否有 R2/S3 上傳路由。
- **Phase 2 人工走查**：AIDV-877/878 接線完成後需 e2e 測試代理失敗路徑（DLQ entry 出現、配額達上限後被拒）；AIDV-879 開啟後需在 staging 測試各代理正常操作不受誤封鎖。

---

## 完成指標

- 新卡 7 張：AIDV-877（High，backend/agent-infra，auto-pass）、AIDV-878（High，backend/agent-infra，auto-pass）、AIDV-879（High，backend/security，needs-bruce）、AIDV-880（Medium，backend/security，auto-pass）、AIDV-881（Medium，backend/security，needs-bruce）、AIDV-882（Low，frontend/error-handling，auto-pass）、AIDV-883（Low，frontend/backend，auto-pass）
- 路由留言 1 筆：AIDV-675（orbChatProgress Bus 2 未合入 sseRouter）
- 丟棄 0 筆
- 本輪 issue links：AIDV-877 Relates to AIDV-346；AIDV-878 Relates to AIDV-348；AIDV-882 Relates to AIDV-860；AIDV-883 Relates to AIDV-848
- 彙總留言：AIDV-102（2026-06-30 16:18）
- 驗證基準：`origin/main` HEAD aead242（2026-06-30）

— 智能助手 🤖

---

## 附錄 A：Workflow 多模態 Harvest 補充卡（AIDV-884～907）

> **基準**：Workflow wfb4sqk3v 4 模態 harvest（docs/jira/uiux/fullstack）→ verify+dedupe → 2026-06-30 aead242 HEAD 實證。
> 本附錄涵蓋 R6 直接分析後，由 Workflow 補充驗證並建立的 24 張卡（含 2 Highest、3 High、12 Medium、7 Low）。

### 新建卡彙整

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-884** | Highest | opt-card, backend, security, idor | `server/routers/ai.ts:3065` `orbTask.approve` 缺 ownership 驗證，任何已登入使用者可核准他人 orb 任務（同卡 cancel:3102 已有 guard，approve:3065 缺）。 |
| **AIDV-885** | Highest | opt-card, backend, security, idor | `server/routers/ai.ts:3225` `orbTask.updateStepStatus` 缺 ownership 驗證，任何已登入使用者可改寫他人 orb 任務步驟狀態（completeStep:3203 已有 guard，updateStepStatus:3225 缺）。 |
| **AIDV-886** | High | opt-card, backend, reliability, agent-infra | `server/jobs/providerHealthProbeJob.ts:160` `const ok = res.status < 500` 把 401/403 視為 healthy，ANTHROPIC_API_KEY 過期時 healthProbe 持續回報正常，UI 無降級警告。 |
| **AIDV-887** | High | opt-card, backend, bug, llm | `server/_core/llm.ts:1997` 無條件送出 tools 陣列，`freellmapi`（llmRouter.ts:802 `supportsToolCalling: false`）收到含 tools 的請求後工具呼叫靜默失敗。 |
| **AIDV-888** | High | opt-card, frontend, a11y | `client/src/shells/video/console/ContextSidecar.tsx:419` VersionHistoryPanel 回退按鈕 `h-5`（20px），低於 WCAG 2.1 SC 2.5.5 最小 44pt 命中區。 |
| **AIDV-889** | Medium | opt-card, backend, security | `server/routers/feedback.ts:27` `pageContext.url: z.string().optional()` 無格式驗證，可存入 `javascript:` scheme，Admin UI 若以 `<a href>` 渲染將觸發 XSS。 |
| **AIDV-890** | Medium | opt-card, backend, bug, agent-infra | `server/services/orbTaskStateMachine.ts:493` `retryOrbAgentTask` 無 `'paused'` case；`resumeOrbAgentTask` 函式不存在，paused 任務永久卡住。 |
| **AIDV-891** | Medium | opt-card, frontend, a11y | `MusicCanvas.tsx:86` / `VoiceAmbientCanvas.tsx` provider 健康指示器缺 `role="status"`、`aria-label`、`aria-live`，螢幕閱讀器無法感知狀態變化。 |
| **AIDV-892** | Medium | opt-card, frontend, a11y | `AdminPage.tsx:53` framer-motion 動畫未守 `useReducedMotion`，違反 WCAG 2.1 SC 2.3.3。 |
| **AIDV-893** | Medium | opt-card, frontend, a11y | `AdminPage.tsx:2243` SkillRegistryTab CircleCheck/CircleX icon-only 缺 `aria-label`，違反 WCAG 2.1 SC 1.1.1。 |
| **AIDV-894** | Medium | opt-card, frontend, a11y | `OrbOnboardingDialog.tsx:116` 步驟進度指示器缺 `role="progressbar"`、`aria-valuenow`，螢幕閱讀器無法感知問卷進度。 |
| **AIDV-895** | Medium | opt-card, frontend, design-system | `AdminPage.tsx:1017` 活動日誌狀態標籤用硬編 hex（#22c55e/#ef4444/#f59e0b），未走 Tailwind design-kit token。 |
| **AIDV-896** | Medium | opt-card, backend, agent-infra, perf | `server/services/costAnalytics.ts:693` `detectRetryChains()` 未連接 `orbCostGuard`，代理可無限重試累積 token 費用超限。 |
| **AIDV-897** | Medium | opt-card, backend, 待議, decision | `server/jobs/postGenActions.ts:37` `ENABLE_PROMPT_ASSET_LINKS` 預設 OFF，生成後資產不自動關聯 prompt，PromptHistory 資產列表為空。**待 Bruce 決策**。 |
| **AIDV-898** | Medium | opt-card, backend, devops, 待議, decision | `.github/workflows/` 缺 Railway pre-deploy migration 步驟與 Playwright e2e，部署前 DB 遷移不自動執行，無 e2e 回歸保護。**待 Bruce 決策 CI 策略**。 |
| **AIDV-899** | Medium | opt-card, frontend, refactor, tech-debt | VideoStudio/ProStudio 未使用 `useGenerationTask` hook，生成邏輯四份拷貝（~19K LOC），bug 修一處其餘三處未修。 |
| **AIDV-900** | Low | opt-card, frontend, tech-debt | `client/src/pages/` 約 8 個孤立頁面未掛入任何路由，dead code 持續累積，仍參與 Vite build。 |
| **AIDV-901** | Low | opt-card, backend, tech-debt | `drizzle/relations.ts` 只聲明 3 個 relations，多數 FK 關係未聲明，影響 Drizzle relational query API（`with:` 語法）。 |
| **AIDV-902** | Low | opt-card, frontend, a11y | `OnboardingFlow.tsx:669` 生成結果圖 `alt="Your first creation"` 硬編英文，介面已中文化但 alt text 未同步。 |
| **AIDV-903** | Low | opt-card, backend, perf | `drizzle/0103_feedback_context.sql` 新增 `feature_area`/`screenshot_key` 欄位無索引，篩選查詢將全表掃描。 |
| **AIDV-904** | Low | opt-card, frontend, a11y | `BeginnerPathPanel.tsx:188` 文件連結按鈕缺 `type="button"` 與 `aria-label`，表單內預設 submit 且無無障礙說明。 |
| **AIDV-905** | Low | opt-card, frontend, a11y | `MusicCanvas.tsx:167` `ENABLE_VIDEO_GATE_KIT=OFF` 分支 CircleCheck 缺 `aria-hidden="true"` 且用硬編 `text-emerald-500`。 |
| **AIDV-906** | Low | opt-card, backend, agent-infra, 待議, decision | `agentCreatorQuota.ts:36` 使用 process-local Map，多副本部署下 per-user 配額不共享，橫向擴展後限制失效。**待 Bruce 決策（Redis 或降級方案）。** |
| **AIDV-907** | Low | opt-card, backend, agent-infra, 待議, decision | `server/services/ragMemory.ts:25` 有 TODO，RAG Pinecone/pgvector 後端尚未選型，向量搜尋功能暫時缺失。**待 Bruce 決策**。 |

### 路由留言補充

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-164** | imageStudio.ts 的 falQueueSubmit local wrapper 仍在 aead242，建議與 fal dispatch 統一同批處理；已留言。 |
| **AIDV-771** | chargeForFalTask 預扣款競態三處確認，建議改 optimistic-credit 或 catch 即退款；已留言。 |
| **AIDV-864** | feedback router 後端（PR①）已合入，前端 FeedbackDialog 仍缺（PR② 未實作）；AIDV-880/881/889 依賴 PR② 完成；已留言。 |
| **AIDV-847** | worldbuilding mockSuggestions 仍在，已留言請 Bruce 選擇修法方案（①AI生成 / ②mock fallback / ③coming soon）。 |
| **AIDV-170** | drizzle migration 衝突仍在，提醒確認 0103 連續性；與 AIDV-903 可同批修入；已留言。 |

### R6 完整完成指標

- 新卡 **31 張**（直接分析 7 + Workflow 補充 24）：AIDV-877～907
  - Highest：AIDV-884、AIDV-885（IDOR P0 安全漏洞）
  - High：AIDV-877、AIDV-878、AIDV-879、AIDV-886、AIDV-887、AIDV-888
  - Medium：AIDV-880、AIDV-881、AIDV-889、AIDV-890、AIDV-891、AIDV-892、AIDV-893、AIDV-894、AIDV-895、AIDV-896、AIDV-897、AIDV-898、AIDV-899
  - Low：AIDV-882、AIDV-883、AIDV-900、AIDV-901、AIDV-902、AIDV-903、AIDV-904、AIDV-905、AIDV-906、AIDV-907
- 路由留言：AIDV-675（R6 direct）+ AIDV-164、AIDV-771、AIDV-864、AIDV-847、AIDV-170（R6 Workflow）
- 待 Bruce 決策：AIDV-879（scope role 指派）、AIDV-881（screenshotKey 方案）、AIDV-897（ENABLE_PROMPT_ASSET_LINKS）、AIDV-898（CI 策略）、AIDV-906（quota 分散式方案）、AIDV-907（RAG 後端選型）
- 驗證基準：`origin/main` HEAD aead242（2026-06-30）

---

## 附錄 B：直接分析驗證細節

### DLQ 接線缺失（AIDV-877）驗證步驟

```
grep -r "insertDlqEntry\|pollDlq\|resolveDlqEntry" server/ --include="*.ts" -l
# 輸出：server/services/agentDlq.ts（僅自身）
grep -r "ENABLE_AGENT_DLQ" server/ --include="*.ts"
# 輸出：agentDlq.ts 內部 flag 定義，無外部引用
```

### Quota 接線缺失（AIDV-878）驗證步驟

```
grep -r "tryAcquireCreatorSlot\|releaseCreatorSlot\|ENABLE_AGENT_QUOTA" server/ --include="*.ts" -l
# 輸出：server/_core/agentCreatorQuota.ts（僅自身）
```

### ScopeGuard 接線缺失（AIDV-879）驗證步驟

```
grep -r "assertAgentScope\|checkAgentScope" server/ --include="*.ts" -l
# 輸出：server/_core/agentScopeGuard.ts（僅自身）
```

### feedback.create 缺 rate limit（AIDV-880）驗證步驟

```
grep -n "rateLimiter\|tryConsume\|TOO_MANY_REQUESTS" server/routers/feedback.ts
# 輸出：（無）
grep -n "tryConsumeChatToken" server/routers/chatRouter.ts
# 輸出：有，確認既有先例
```

### screenshotKey 缺 ownership 驗證（AIDV-881）驗證步驟

```
grep -n "screenshotKey\|screenshot_key" server/routers/feedback.ts
# 輸出：line 43 screenshotKey: z.string().max(512).optional()
grep -rn "screenshotKey" server/ --include="*.ts"
# 輸出：feedback.ts 43,65（無 ownership 驗證邏輯）
```

### VoiceAmbientCanvas isError（AIDV-882）驗證步驟

```
grep -n "isError\|isLoading\|providerStatusQ" client/src/shells/video/canvas/VoiceAmbientCanvas.tsx
# 輸出：isLoading ✓，data ✓，isError ✗
```
