# aidv-optimize 第 7 輪審計 — 2026-06-30（R7）

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：直接 code 分析（origin/main HEAD 5dfe7a9）—— 聚焦 R6 基準（aead242）→ 5dfe7a9 之間 8 commits 引入的新缺口，特別是 AIDV-864 PR②（QuickFeedbackButton / useElementPicker）與 AIDV-659 後續（ADP-2 falRecoveryPolicy 接線覆蓋率）。
> **規模**：直接分析 → 驗證後 **新建 4 卡｜路由 2｜丟 0**。
> **基準**：HEAD（`origin/main`，2026-06-30 5dfe7a9，以 R6 aead242 為去重錨點）。
> **聚焦**：AIDV-864 PR②（QuickFeedbackButton + useElementPicker）引入的 error handling / a11y 缺口，以及 ADP-2 三層恢復策略的實際覆蓋率缺口（只接 image/video，audio/sfx/3D 未升級）。

---

## 結論（一句話）

**本輪最重要的發現是 ADP-2 三層恢復策略的覆蓋缺口（AIDV-914，Medium）：`runFalWithRecovery` 只接線了 `studio.generateImage`/`generateVideo`，而 `studio.generateAudio`/`generateSfx`/`generate3D` 仍走舊版單層 transient 重試，遇到內容審核拒絕或模型下線時直接失敗；同等重要的 AIDV-913（Medium）發現 `QuickFeedbackButton.handleSubmit` 缺 try/catch，feedback 送出失敗時使用者無感知；另建 2 張 Low（useElementPicker 未守 reduced-motion、chips 缺 type="button"），路由 2 筆（AIDV-906 擴展覆蓋、AIDV-881 再確認），丟棄 0 筆。**

---

## §1 本輪新建卡（已對 origin/main HEAD 5dfe7a9 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-913** | Medium | opt-card, frontend, error-handling | `client/src/components/QuickFeedbackButton.tsx:100` `handleSubmit` 中 `await create.mutateAsync(...)` 無 try/catch；送出失敗（429/5xx/網路中斷）時 `setSubmitted(true)` 與 `toast.success` 不執行，使用者無任何錯誤提示。修法：包 try/catch + `toast.error("送出失敗，請稍後再試")`。 |
| **AIDV-914** | Medium | opt-card, backend, reliability, perf | `server/services/agentToolExecutor.ts` 中 `studio.generateAudio`（:1414）、`studio.generateSfx`（:1607）、`studio.generate3D`（:1249）仍走舊 `dispatchFalQueueTask + awaitFalForOrb` 單層路徑，未使用 AIDV-659 新建的 `runFalWithRecovery` 三層恢復；content 拒絕（審核）與 hard 錯誤（模型下線）無恢復機制。修法：仿 generateImage 包入 `runFalWithRecovery`。 |
| **AIDV-915** | Low | opt-card, a11y, frontend | `client/src/hooks/useElementPicker.ts:35` hover 高亮 overlay CSS inline style 含 `transition:left 60ms,...`，使用 inline style 無法被 CSS `@media (prefers-reduced-motion: reduce)` 覆寫，違反 WCAG 2.1 SC 2.3.3。修法：讀 `window.matchMedia` 偏好後條件加 transition。 |
| **AIDV-916** | Low | opt-card, frontend, robustness | `client/src/components/QuickFeedbackButton.tsx` CATEGORY_CHIPS map（~183）與 landmark 移除按鈕（~229）共 5 個 `<button>` 缺 `type="button"`，防禦性修法（同 AIDV-854 模式）。 |

---

## §2 路由留言

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-906** | `rateLimiter.ts` 新增的 `_feedbackRateLimitStore`（AIDV-880 修復）與 `_chatRateLimitStore` 採用與 agentCreatorQuota 相同的 in-process Map；Railway 多副本部署下限流乘以副本數失效。建議本卡修法同時覆蓋三個 in-process Map；已留言。 |
| **AIDV-881** | AIDV-903（migration 0104）只補 `ufr_feature_area_idx`/`ufr_screenshot_key_idx` 查詢索引，`server/routers/feedback.ts:52` 的 `screenshotKey` ownership 驗證仍缺，安全疑慮未解；已留言。 |

---

## §3 丟棄

本輪無丟棄候選（所有發現均為 aead242→5dfe7a9 新引入缺口，已在 HEAD 實證）。

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 關注（AIDV-881 screenshotKey）**：AIDV-903 不解決安全問題，需在 AIDV-881 方案選擇後開工（三方案：DB 記錄 key+userId / key 格式含 userId 前綴 / 純 client 端不存 key）。
- **Phase 2 人工走查**：AIDV-913 修復後需在瀏覽器模擬 429 並確認 `toast.error` 出現；AIDV-914 需在 staging 測試 audio/sfx 生成的 429 重試與 content 拒絕恢復；AIDV-915 需在 `prefers-reduced-motion: reduce` 環境確認 overlay 無動畫。

---

## 完成指標

- 新卡 4 張：AIDV-913（Medium, frontend/error-handling, auto-pass）、AIDV-914（Medium, backend/reliability, auto-pass）、AIDV-915（Low, a11y, auto-pass）、AIDV-916（Low, frontend/robustness, auto-pass）
- 路由留言 2 筆：AIDV-906（rateLimiter in-process Map 擴展）、AIDV-881（screenshotKey 安全再確認）
- 丟棄 0 筆
- 本輪 issue links：AIDV-914 Relates to AIDV-659（ADP-2 原始卡）；AIDV-916 Relates to AIDV-854（OnboardingFlow 同樣問題）
- 彙總留言：AIDV-102（待本步驟）
- 驗證基準：`origin/main` HEAD 5dfe7a9（2026-06-30）

— 智能助手 🤖
