# aidv-optimize 第 3 輪審計 — 2026-06-30

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
>
> **註**：2026-06-30 當日有兩次第 3 輪 harvest 並行落檔（不同模態組合、不同卡批次），兩份記錄均對 `origin/main` HEAD 驗證後保留為下方 A／B 兩段，互不覆蓋，合併為單一去重錨點檔。

---

## A 段 — harvest 批次 A（AIDV-792~802）

> **方法**：多代理 Workflow（4 模態 harvest → 2 視角對 origin/main 驗證＋對看板去重）。本段＝該輪完整記錄，亦為下一輪去重錨點。
> **規模**：harvest 41 候選（docs:15 jira:5 uiux:12 fullstack:9）→ 驗證後 **新建 11 卡｜路由 4 卡留言（合計 12 項）｜丟 ~20（已修/重複/已決策）**。
> **基準**：HEAD（`origin/main`，c96dd89，2026-06-30，含 AIDV-772 安全批次等 15 個新 commit）。

### A · 結論（一句話）

**本輪最有價值的發現是「`generate.ts checkStudioJob` fal.ai 例外靜默吞掉導致 job 永久卡在 processing（AIDV-792）」與「proStudio 語音路由未接 customBlocks 導致自訂語音 preset 完全無效（AIDV-793）」**；另新建 9 卡含 railway 部署流程缺 migration 自動化（AIDV-795，待議）、同意書驗證 N+1（AIDV-796）、worldbuilding mock API（AIDV-797，待議）、搜尋防重複（AIDV-798）、promptLibrary 全文索引（AIDV-799）、design token 漂移（AIDV-800）、LoRA log 靜默（AIDV-801）、realEarth N+1（AIDV-802）；~20 個候選經 HEAD 實證後丟棄；4 張既有卡收到路由留言。

### A · §1 本輪新建卡（已對 origin/main 實證）

| 卡 | 優先 | 設計門 | 標籤 | 一句話 |
|---|---|---|---|---|
| **AIDV-792** | High | auto-pass | opt-card, backend, error-handling | `generate.ts checkStudioJob` fal.ai 查詢例外靜默吞掉，job 永遠卡在 processing 無任何 log 或狀態更新。 |
| **AIDV-793** | High | auto-pass | opt-card, backend, feature-gap | proStudio 語音生成路由未查詢 customBlocks 表，`isCustom=true` 的語音 preset 永遠走 DEFAULT_EMOTION，使用者存的設定完全無效。 |
| **AIDV-794** | Medium | auto-pass | opt-card, backend, type-safety | `providerRouter.ts:23` `fallbackProviderIds: string[]` 無 compile-time 型別守衛，打錯 provider ID 到執行期才報錯，備用切換靜默失效。 |
| **AIDV-795** | Medium | 待議 | opt-card, tooling, 待議, decision | db:push/drizzle migrate 未納入 Railway 部署流程——每次部署需人工執行 migration；Dockerfile CMD 與 railway.toml 均無 preDeployCommand；待 Bruce 拍板選方案。 |
| **AIDV-796** | Medium | auto-pass | opt-card, backend, perf | `models.ts:348` 同意書驗證 for-of 內逐筆 await `getModelTrainingConsent`，N 模型＝N 次 DB roundtrip；改 `inArray()` 批次查詢降為 1 次。 |
| **AIDV-797** | Medium | 待議 | opt-card, backend, 待議, decision | `worldbuilding.ts:793` `getCompositionSuggestions` 回傳寫死 mock 字串，前端誤呈現為 AI 建議；每個使用者看到相同內容；待 Bruce 決定：真實 AI / 標示 mock / 下架。 |
| **AIDV-798** | Medium | auto-pass | opt-card, frontend, uiux | TeachingArchiveGrounding + RealEarthResearch 搜尋按鈕缺 `disabled={isPending}` 防重複提交；連點可送多重複請求。 |
| **AIDV-799** | Low | auto-pass | opt-card, backend, perf | `promptLibrary.ts:79` 對 title+content 雙欄用 `LIKE '%x%'` 全表掃描，無 FULLTEXT 或 B-Tree 索引，大資料量時搜尋效能差。 |
| **AIDV-800** | Low | auto-pass | opt-card, frontend, uiux | `FocusFlowMini.tsx:69` rgb() + `ThoughtIslandChain.tsx:84` STATUS_COLORS hex 硬編色彩繞過 design-kit token，主題切換時顏色不同步。 |
| **AIDV-801** | Low | auto-pass | opt-card, backend, error-handling | `loraTrainer.ts:371` `replicateTrainingStatus` 外層 catch 靜默忽略，LoRA 訓練狀態查詢失敗無 log，使用者只見 `null`。 |
| **AIDV-802** | Low | auto-pass | opt-card, backend, perf | `realEarth.ts:245` `getRelated` for-of 內逐筆 await DB 查詢，N+1 問題；改 `Promise.all` 或 `inArray()` 並行批次查詢。 |

### A · §2 路由回擁有卡（一軌一主，已留言）

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-613** | ① `CreditsUsagePanel.tsx:27` statsQ/balanceQ 缺 isError → 失敗靜默顯示「—」無重試；② `IntelBentoGrid.tsx:821` useInfiniteQuery 缺 isError → 空白畫面無重試；③ `GeneralSettingsPanel.tsx:33` getQ 缺 isLoading 骨架 → 載入期表單已展開可操作；④ `ContextSidecar.tsx:328` size-6 icon button 24px 命中區低於 44pt。已留言附 file:line。 |
| **AIDV-559** | ① `ShowcaseMasonry.tsx:324` framer-motion y+whileHover 未守 useReducedMotion；② `AdvancedPromptPanel.tsx:69` height 動畫未守 useReducedMotion；③ `StructuredBlocksEditor.tsx:178` height 動畫未守 useReducedMotion；④ `PageSidebar.tsx:162` h-7 w-7 折疊按鈕 28px 命中區低於 44pt。已留言。 |
| **AIDV-588** | 進度/狀態 UI 散落 5 處（Toast / BackgroundTasksDrawer / DirectorAI intent cards / ZenProgressOverlay / OrbTaskObservationStrip），DirectorAI 自建 jobId tracking 與 BackgroundTasksContext 並存冗餘訂閱。已留言附建議抽 `<GenerationStatus>` 統一元件。 |
| **AIDV-771** | `proStudio.ts:64` `chargeForFalTask()` 先扣點再送 fal，refund 路徑無 idempotency key/hold 保護——與此卡（createBackgroundJob 失敗→點數孤兒）為計費閉環的兩個薄弱環節，建議合併修復。已留言附 hold 方案。 |

### A · §3 丟棄（已修／重複／已決策，staleness 過濾的價值）

| 候選 | 原因（origin/main 實證） |
|---|---|
| prompt_library × digital_asset_library 無關聯表 | **已存在**：`schema.ts:1801` `promptAssets` 多對多中介表已實作（`prompt_library × digital_asset_library`）；dedupeRoute 與 stillReal 兩個代理均確認已解決。 |
| Drizzle migration 編號重複（0008/0033/0067） | **已路由/已修**：AIDV-17 已完成（孤兒 migration 補登記 journal）；重複編號問題已路由 AIDV-170；dedupeRoute 標 drop。 |
| fal 派發碎片化（imageStudio/videoStudio 各自定義 falQueueSubmit） | **已路由（上輪）**：AIDV-164（Backlog/待議）為現任擁有卡，不重複開卡。 |
| VideoStudio 未使用 useGenerationTask hook | **已路由（上輪）**：AIDV-71 歸屬（四 studio 複製問題已追蹤），不重複開卡。 |
| 三組事件總線並存 | **已路由（上輪）**：AIDV-675 歸屬（SSE 三匯流），已留言，不重複。 |
| costAnalytics.detectRetryChains 未接降級 | **已路由（上輪）**：AIDV-597 歸屬，已留言，不重複。 |
| backgroundJobs 無 TTL | **已路由（上輪）**：AIDV-655 歸屬，已留言，不重複。 |
| stripeWebhook TODO duplicate | **已覆蓋**：與 AIDV-167 路由（上輪已留言）完全重疊，dedupeRoute 標 drop。 |
| AIDV-772 batch 修掉的 SSRF/GDPR 候選 | **已修**：commit c96dd89 修復 SSRF url 驗證×2＋GDPR 抹除 8 表等；所有對應 doc-stale 候選丟棄。 |
| AIDV-714 修掉的 as any 前端候選 | **已修**：commit faadcb5 消除前端 API 結果解析 as any；相關候選丟棄。 |
| AIDV-560 修掉的前端靜默失敗候選 | **已修**：commit 69093ef 前端健壯性完整收斂 10 處；相關候選丟棄。 |
| 4-shell 重構前路徑（client/src/pages/*）的所有候選 | **doc-stale**：路徑在 origin/main 不存在，全部丟棄。 |

### A · §4 Phase 2 / 待 Bruce 拍板

- **待 Bruce 拍板（AIDV-795）**：Railway preDeployCommand 方案選擇——方案 A（`railway.toml [deploy] preDeployCommand`）vs 方案 B（Dockerfile ENTRYPOINT 腳本）；需討論 migration 失敗時的 FAIL_CLOSED 行為。
- **待 Bruce 拍板（AIDV-797）**：`getCompositionSuggestions` 三選一：① 改真實 AI 呼叫（Gemini/Director，需評估成本/延遲）② 在回傳值加 `isMock: true` 讓前端顯示「範例建議」③ 直接下架此功能入口。
- **Phase 2 人工走查**：AIDV-800（FocusFlowMini + ThoughtIslandChain token 替換後視覺等價需截圖對比）；AIDV-798（搜尋防重複的 Network 標籤確認）；AIDV-799（promptLibrary FULLTEXT 索引需在 staging DB 確認 EXPLAIN 改善）。

### A · 完成指標

- 新卡 11 張：AIDV-792~802
- 路由留言 4 卡：AIDV-613（4 項）、AIDV-559（4 項）、AIDV-588、AIDV-771
- 丟棄 ~20 個候選（已修/重複/已決策，經 origin/main 實證）
- 待議 2 張：AIDV-795、AIDV-797

---

## B 段 — harvest 批次 B（AIDV-788~790）

> **方法**：多代理並行 harvest（4 模態）→ 對 origin/main HEAD 驗證＋對看板去重 → 寫卡/路由/丟棄。
> **規模**：harvest 掃描 → 驗證後 **新建 3 卡｜路由 12｜丟 ~18（已修/重複/已決策）**。
> **基準**：HEAD（`origin/main`，2026-06-30，以 2026-06-29 第 2 輪為去重錨點）。

### B · 結論（一句話）

**本輪最重要的發現是兩個安全缺口：`videoProject`/`imageStudio` 的 4K 解析度無後端付費驗證（AIDV-788）、`/api/media/download` 代理路由無資產所有權驗證（AIDV-789，待 Bruce 決策）**；另建 AIDV-790（ConsentFormDialog canvas 無 a11y）；12 張既有卡收到路由留言（含新批次 a11y 缺口）；~18 個候選對 HEAD 實證後丟棄。

### B · §1 本輪新建卡（已對 origin/main HEAD 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-788** | High | opt-card, security, fullstack, auto-pass | `videoProject`/`imageStudio` router 的 4K 解析度選項（`z.enum(["720p","1080p","4K"])` / `z.enum(["0.5K","1K","2K","4K"])`）被後端 create/update mutation 直接接受，無 `isPaid` guard；前端 `outputSpecEntitlement` query 存在但只供 UI 讀取，API 可直接繞過，任意登入使用者均可提交 4K 任務。`server/routers/videoProject.ts:29`、`imageStudio.ts:829`。 |
| **AIDV-789** | High | opt-card, security, backend, 待議, decision | `/api/media/download`（`server/routes/download.ts:46-80`）僅驗 `req.user` 存在與 SSRF origin 白名單，無 DB 所有權查詢——任意登入使用者可下載他人媒體 URL。需 Bruce 拍板：UUID-as-capability 是否足夠，或補 DB 擁有者查詢。AIDV-623 修復的是 `requestDownloadByUrl` service layer，此代理路由層未被覆蓋。 |
| **AIDV-790** | Medium | opt-card, a11y, frontend, auto-pass | `ConsentFormDialog.tsx:486` 手寫簽名 `<canvas>` 無 `aria-label`、無 `role`、無 `tabIndex`，鍵盤使用者完全無法存取 LoRA 訓練同意流程。補 `aria-label="數位手寫簽名區域，請用滑鼠或觸控繪製您的簽名"` + `role="img"` + `tabIndex={0}` + 鍵盤替代說明文字即可（auto-pass）。 |

### B · §2 路由回擁有卡（一軌一主，已留言）

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-559** | 新批次 a11y 缺口：① `OutputSpecSelector.tsx:138` radio group 缺 role + 命中區 < 44px ② `CreationFlowBar.tsx:122` 步驟按鈕缺 `aria-current="step"` ③ `WorkflowStepper.tsx:151` 同上 ④ `LoraTrainer.tsx:2191` 撤回按鈕命中區 ~20px + 缺 aria-label ⑤ `VibeCardWizard.tsx:42` Framer Motion 動畫未守 useReducedMotion ⑥ `ShowcaseMasonry.tsx:323` 同上（y 位移 + hover 動效）。已留言附 file:line。 |
| **AIDV-190** | `server/routers/learnHub.ts:145,153` — `let videos/quizzes = [...SEED_*]` 仍為 in-memory；fix(AIDV-190) 只加了誠實 UI 訊息，未改持久化。已留言確認，不重開卡。 |
| **AIDV-167** | `server/routes/stripeWebhook.ts:166-255` — 5 個 handler（`handleCheckoutSessionCompleted`、`handleInvoicePaid`、`handleInvoicePaymentFailed`、subscription.updated、subscription.deleted）仍為空 TODO stub，對 HEAD 再次確認。已留言標 High 業務缺口。 |
| **AIDV-620** | `chargeForFalTask` 預扣點數與 fal submit 之間無原子交易保護；失敗時點數已扣但任務未執行，無補償路徑。已留言附建議。 |
| **AIDV-593** | `orbFeatureDiscovery.ts` `@deprecated always returns []` 確認仍在 HEAD；`ai.chat` brain slot 未讀取個人化資訊。已留言確認仍開放。 |
| **AIDV-654** | `WorldbuildingAgentExecutionPanel` dead/placeholder 按鈕確認仍在 HEAD。已留言確認追蹤。 |
| **AIDV-164** | 多個 router 各自宣告本地 `falQueueSubmit`，無統一派發層。已留言建議統一為 `falDispatchService.ts`。 |
| **AIDV-655** | `background_jobs` 無 TTL/FK/自動 history row 確認仍在 HEAD。已留言確認。 |
| **AIDV-675** | 三條 SSE 事件匯流（`generationEvents`/`orbChatProgress`/`orbTaskObserver`）各自維護 client Map，無統一 typed envelope；補充細節確認，待 Bruce 拍板架構。已留言。 |
| **AIDV-170** | `drizzle/` migration 編號 0008/0033/0067 疑似重複；`relations.ts` 僅 3 條 relation（80+ 表）。已留言補充確認指令。 |
| **AIDV-548** | `orbFeatureDiscovery.ts:276-528` stubs 再次確認仍在 HEAD；依賴 AIDV-19（pgvector halfvec/needs-key）。已留言確認 blocked 依賴關係。 |
| **AIDV-676** | `voiceCompiler.ts` SSML 無長度上限；`audioCompiler` schema 無 duration min/max 業務約束；Director 呼叫無 runtime zod parse。已留言附建議（互補 AIDV-631）。 |

### B · §3 丟棄（已修／重複／已決策，staleness 過濾的價值所在）

| 候選 | 原因（HEAD 實證） |
|---|---|
| `promptLibrary` search 只搜 title | **上輪已路由 AIDV-628**（escapeLikePattern 相關修復）；本輪確認為同檔案不同角度，但屬已有擁有卡涵蓋範圍，不重開卡。 |
| `orbLongTermMemory.ts` LIKE 搜尋品質 | **已路由 AIDV-548**（上輪）；本輪確認 blocked by AIDV-19，不重複留言。 |
| `audioCompiler.ts` 缺 schema 驗證 | **已存在**：`validateCompilerInput("audio", input, AudioCompilerInputSchema)` 在 `audioCompiler.ts:487`；非缺口，丟棄。 |
| `AppleDockItem` 缺 aria-label | **已存在**：`AppleDockItem.tsx:47` 已有 `aria-label={label}`，grep 確認，丟棄。 |
| `requestExport` dead 按鈕 | **已路由 AIDV-646**（上輪），不重複。 |
| `BackgroundTasksDrawer` tRPC+SSE 雙管線 | **已路由 AIDV-592**（上輪），不重複。 |
| `costAnalytics.detectRetryChains` 未連 orbCostGuard | **已路由 AIDV-597**（上輪），不重複。 |
| Director `knowledge_base` 漂移風險 | **已路由 AIDV-631**（上輪），不重複。 |
| `AIDV-650` `submitStudioJob` 缺 `costPoints` | **已路由 AIDV-650**（上輪），不重複。 |
| `CommandPalette` 缺 FocusTrap + aria | **已路由 AIDV-559**（上輪），本輪補批次，不重複開卡。 |
| `AmbientVideo` 自動播放缺 useReducedMotion | **已路由 AIDV-559**（上輪），不重複留言。 |
| migration 0085 語法錯誤 | **已修**：commit `949d98c`，上輪確認，丟棄。 |
| `skillRegistryService.ts` 未串 tRPC | **已建 AIDV-664**（上輪，待議），丟棄重複。 |
| `ConsistencyVault` 缺 isError guard | **已建 AIDV-674**（上輪，auto-pass），丟棄重複。 |
| `VoiceCompilerInput.emotionOverride` dead field | **已建 AIDV-677**（上輪），丟棄重複。 |
| `imageStudio` 26 個 `as any` | **已建 AIDV-678**（上輪），丟棄重複。 |
| `FrameTimeline.tsx` 四態設計 | **非適用**：上輪確認為上傳槽元件，per-slot 狀態即符需求，丟棄。 |
| `providerSnapshotJob.ts` fal/GCP Billing TODO | **低優先 roadmap 項**，無業務阻塞，歸下輪或人工排程，本輪丟棄不開卡。 |

### B · §4 Phase 2 / 待 Bruce

- **待 Bruce 拍板（AIDV-789）**：`/api/media/download` 代理路由需決策：① UUID-as-capability（UUID entropy 防猜）② 補 DB 擁有者查詢（明確授權）。建議最小介入：在 media 表加 `ownerUserId` 索引，加一行 `WHERE id = $assetId AND ownerUserId = $userId`。
- **待 Bruce 確認（AIDV-788）**：4K 後端 gate 修法——建議提取 `outputSpecEntitlement` 邏輯為 shared helper，在 create/update mutation 的 `ctx.session.user.isPaid` 未通過時拋 `UNAUTHORIZED`。
- **Phase 2 人工走查**：`VibeCardWizard` 與 `ShowcaseMasonry` 的 `useReducedMotion` 效果需真機確認；`ConsentFormDialog` canvas 鍵盤替代路徑需手動 Tab 測試（均屬 AIDV-559/AIDV-790 Phase 2 走查項）。

### B · 完成指標

- 新卡 3 張：AIDV-788（High，security/fullstack，auto-pass）、AIDV-789（High，security/backend，待議/needs-bruce）、AIDV-790（Medium，a11y/frontend，auto-pass）
- 路由留言 12 張：AIDV-559（新批次 a11y）、AIDV-190、AIDV-167（Stripe stubs 確認）、AIDV-620、AIDV-593、AIDV-654、AIDV-164、AIDV-655、AIDV-675、AIDV-170、AIDV-548、AIDV-676
- 丟棄 ~18 個候選（已修/重複/已決策，經 HEAD 實證）
- 本輪 issue link：AIDV-789 Relates to AIDV-623；AIDV-788 Relates to AIDV-674（pattern 相似）

---

— 智能助手 🤖
