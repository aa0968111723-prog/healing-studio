# aidv-optimize 第 2 輪審計 — 2026-06-26

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：活碼全端掃描（live-verified）+ Jira 看板去重 + git log 對 origin/main HEAD 確認是否已修
> **規模**：掃描候選 9 筆 → 驗證後 **新建 4 卡｜路由 0 筆｜丟 5 筆過時/已有**
> **基準**：origin/main HEAD `9fd7738`（M8 巨檔拆分 refactor(AIDV-71) 後）

---

## 結論（一句話）

**最有價值的發現是「emailService.ts 密碼重設功能完全不發信（isConfigured 硬編 false）」（AIDV-434，High）**，與「worldbuilding timeline frames 5 個 tRPC procedure DB 表已建但 router 仍回 mock」（AIDV-433，High）；Orb AI 大腦三組幽靈功能群與 costAnalytics 廢棄迴路各開 Medium 卡；5 筆候選（含 CI 缺失、hex 顏色、已有待議卡）經 HEAD 確認後丟棄。

---

## §1 本輪新建卡（已對 origin/main HEAD 實證）

| 卡 | 優先 | 標籤 | 一句話 | 設計門 |
|---|---|---|---|---|
| **AIDV-433** | High | opt-card, backend, data-loss, worldbuilding | `server/routers/worldbuilding.ts:617–735` 的 `listTimelineFrames`/`uploadTimelineFrame`/`deleteTimelineFrame`/`checkConsistency`/`listCompositions` 全回 mock/空陣列；migration `drizzle/0064_timeline_frames_and_compositions.sql` 兩張表已建但 router 從未接線，上傳的圖幀資料永久丟失 | 自動通過 |
| **AIDV-434** | High | opt-card, backend, security, 待議, decision | `server/services/auth/emailService.ts:constructor` `isConfigured=false` 硬編；`send()` 方法在任何真實發送邏輯前直接 return；密碼重設/安全通知/email 驗證全部靜默，使用者永遠收不到信 | **needs-bruce**（選服務商） |
| **AIDV-435** | Medium | opt-card, backend, ai-quality, 待議, decision | `orbClarificationEngine.ts:119` `identifyIntent` TODO stub 被 `orbConversationEnhancer.ts:92` 真實呼叫；`orbFeatureDiscovery.ts:286/480/437` recommendations/search/category 全回空；`orbLongTermMemory.ts:190` `semanticSearch` TODO 回空——被 `agentToolExecutor.ts` 真實路由 | Phase A 自動通過；Phase B needs-bruce |
| **AIDV-436** | Medium | opt-card, backend, cost-optimization, perf | `server/services/costAnalytics.ts` `detectRetryChains`(L692)/`calculateWasteCost`(L340)/`projectMonthlyCost`(L434) 只被 `apiUsage.ts` admin 面板使用，`grep` 確認生成決策路徑（proStudio/imageStudio/videoStudio/falDispatcher）零引用，廢棄迴路 | Phase A 自動通過；Phase B needs-bruce |

---

## §2 路由（0 筆）

本輪所有發現均無擁有卡可路由，全部開新卡。

---

## §3 丟棄（已修／重複，staleness 過濾的價值所在）

| 候選 | 原因（origin/main HEAD 實證） |
|---|---|
| CI workflow 缺失（2026-05-15 盤點 #5） | **已修**：`.github/workflows/pr-gate.yml` 存在，含 tsc/check:routes/check:navigation/vitest run 四道驗證門 |
| `AmbientOrb.tsx` 硬編 hex（#DFF7F1/#7FD9CF 等） | SVG 動畫藝術漸層，設計意圖為客製視覺效果，不屬於 design-kit token 替換範圍 |
| `LoginCosmicScene.tsx` 硬編 hex（宇宙漸層） | 宇宙動畫場景，設計意圖明確，與 design-kit token 無衝突 |
| Fal dispatcher 碎片化 | 已有 **AIDV-164**（Backlog/待議），不重複開卡 |
| agent_tasks INSERT RLS policy 缺失 | 已有 **AIDV-419**（Backlog），不重複開卡 |

---

## §4 待 Bruce 拍板（待議清單）

| 卡 | 需決策事項 |
|---|---|
| **AIDV-434** | 選定郵件服務商（SendGrid 免費方案建議）並提供 API Key；決定後工程師可自走接線（約 0.5 天） |
| **AIDV-435 Phase B** | orbFeatureDiscovery 推薦演算法：rules-based（依功能使用頻率排序）或 ML（需 embedding）；orbLongTermMemory 記憶壓縮策略 |
| **AIDV-436 Phase B** | costAnalytics 熔斷策略：月廢棄成本超 X% 是軟熔斷（強制免費 provider）還是只警告？閾值建議根據線上資料調校 |

---

## §5 Phase 2 / 人工走查項目

以下項目靜態掃描無法驗證，留 Bruce 人工或下輪深查：

- `orbClarificationEngine.ts` 的 `recordAnswer()` / `getUserAnswerPattern()` — 需真實 AI 回路實測確認效果
- `orbLongTermMemory.consolidate()` — 記憶合併策略需 domain 決策
- 信件外觀與送達率（AIDV-434 接線後）— 需真機 E2E 驗收

---

## §6 本輪脈絡備忘

**自上輪（2026-06-21）以來的大量完成**：AIDV-161/162/163（projectGateway/loadProject/spine）、AIDV-71（M8 routers.ts 9939→502行）、AIDV-420/329/344/323/331/353/332（多代理安全與容錯）等 30+ 張卡全數合入 main。本輪候選均已排除這些已完成工作，確認只對仍成立的缺口開卡。

— 智能助手 🤖
