# aidv-optimize 第 8 輪審計 — 2026-06-30（R8）

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：直接 code 分析（origin/main HEAD 3d5a63b）—— 聚焦 R7 基準（5dfe7a9）→ 3d5a63b 之間 4 commits 引入的新缺口，特別是 AIDV-915/916/889 修復（commit 3d5a63b）與 AIDV-901 drizzle relations 補齊（commit 4bbd6b2）的殘餘缺口，以及 AIDV-877 接線後的 pollDlq 調度缺口。
> **規模**：直接分析 → 驗證後 **新建 1 卡｜路由 2｜丟 0**。
> **基準**：HEAD（`origin/main`，2026-06-30 3d5a63b，以 R7 5dfe7a9 為去重錨點）。
> **聚焦**：R7 以來 4 個新 commit 引入的殘餘缺口——AIDV-877 pollDlq 未排程、AIDV-901 後仍缺 ~57 個周邊表 FK relations、useElementPicker overlay 缺 aria-hidden。

---

## 結論（一句話）

**本輪規模較小（4 commit diff），最有價值的發現是 AIDV-877 部分修復後的殘存缺口：`pollDlq()` 仍未在任何 scheduler 中呼叫（已路由 AIDV-877），DLQ 條目只進不出；另路由 AIDV-170 補充 drizzle/relations.ts 仍缺約 57 個周邊表 FK 聲明（包含 videoAnalytics、globalAuditLog、orbCostAttribution 等）；新建 AIDV-918（Low）修補 useElementPicker hover overlay 缺 aria-hidden="true" 的 a11y 缺口。**

---

## §1 本輪新建卡（已對 origin/main HEAD 3d5a63b 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-918** | Low | opt-card, a11y, frontend | `client/src/hooks/useElementPicker.ts:27-30` 用 `document.createElement("div")` 建 hover 高亮 overlay 後直接 `body.appendChild(hl)`，缺 `hl.setAttribute("aria-hidden", "true")`；螢幕閱讀器 DOM 遍歷時可能碰到無標籤裝飾層（WCAG 2.1 SC 1.1.1）。修法：加一行 `setAttribute`。**Relates to AIDV-915**（同檔已修 reduced-motion）。 |

---

## §2 路由留言

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-877** | commit `b5f958a` 已在 `agentWorkflowRouter.ts:54` 接上 `insertDlqEntry`，但 `pollDlq()` 在 HEAD `3d5a63b` 仍完全沒有排程呼叫（`git grep pollDlq server/**/*.ts` 只回傳 agentDlq.ts 自身）；DLQ 條目永遠堆積不被處理，建議在 `server/_core/index.ts` 加定期排程（60s）；已留言。 |
| **AIDV-170** | AIDV-901（已完成）補了 48 個主要表 relations，但 `drizzle/schema.ts` 共 105 個 export，仍有約 57 個周邊表缺 FK relations 聲明（含 userId FK 的重要表：`videoAnalytics`、`globalAuditLog`、`orbCostAttribution`、`apiKeys`、`emailVerificationTokens`、`specializedAgentInteractions`、`orbScheduledJobs` 等）；AIDV-170 描述已包含「補 relations.ts 關聯」，建議同步覆蓋；已留言。 |

---

## §3 已確認仍開放的 R7 卡（無需新卡）

| 卡 | 狀態 | 確認說明 |
|---|---|---|
| **AIDV-913** | Backlog | `QuickFeedbackButton.handleSubmit:100` 的 `await create.mutateAsync(...)` 在 HEAD 3d5a63b 仍無 try/catch；無修復 commit |
| **AIDV-914** | Backlog | `studio.generateAudio`（:1581）、`studio.generateSfx`（:1667）、`studio.generate3D`（:1249）在 HEAD 3d5a63b 仍走舊 `dispatchFalQueueTask + awaitFalForOrb` 路徑，未用 `runFalWithRecovery`；無修復 commit |

---

## §4 丟棄

本輪無丟棄候選（所有候選均為 5dfe7a9→3d5a63b 4 commits 引入/殘留的缺口，已在 HEAD 實證）。

---

## §5 Phase 2 / 待 Bruce

- **AIDV-877 pollDlq 優先**：DLQ 接線 commit b5f958a 已讓條目寫入，但排程步驟遺漏；建議下輪 autodev 優先在 `server/_core/index.ts` 加 `setInterval(() => pollDlq(), 60_000)` 或等效排程。
- **AIDV-913/914 為 Medium 可開工卡**：無需 Bruce 拍板，可直接進 sprint。
- **Phase 2 人工走查**：AIDV-918 需在 VoiceOver/NVDA 環境確認 hover overlay 不被 AT 播報。

---

## 完成指標

- 新卡 1 張：AIDV-918（Low, a11y/frontend, auto-pass）
- 路由留言 2 筆：AIDV-877（pollDlq 未排程）、AIDV-170（drizzle relations 周邊表缺口）
- 丟棄 0 筆
- 本輪 issue links：AIDV-918 Relates to AIDV-915（同檔已修 reduced-motion）
- 彙總留言：AIDV-102（待本步驟）
- 驗證基準：`origin/main` HEAD 3d5a63b（2026-06-30）

— 智能助手 🤖
