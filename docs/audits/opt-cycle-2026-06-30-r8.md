# aidv-optimize 第 8 輪審計 — 2026-06-30（R8）

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：4 模態直接 code 分析（origin/main HEAD `de13de0`）—— 聚焦 R7 基準（5dfe7a9）→ de13de0 之間 2 commits 引入的異動（AIDV-803 ⌘K 去重 + lora-trainer 路徑修正；AIDV-770 rawBody 捕獲），以及全域 TODO/FIXME 掃描、Jira 去重後的新鮮度確認。
> **規模**：4 模態 harvest → verify+dedupe → **新建 0 卡｜路由 2｜丟 13**
> **基準**：HEAD（`origin/main`，2026-06-30 de13de0，以 R7 5dfe7a9 為去重錨點）

---

## 結論（一句話）

**本輪最重要的發現是 AIDV-167（Stripe webhook handler stubs）的緊急度提升**——AIDV-770 rawBody 修復已完成，Stripe 事件現在能正確送達並通過簽章驗證，但 5 個事件處理函式仍全為空 stub，付費/取消/升降級完全不寫 DB；另路由 AIDV-170 更新重複遷移統計（從 3 對擴大為 6 對）；本輪新建 0 卡，全部候選通過 Jira 去重後歸入既有卡或丟棄。

---

## §1 本輪新建卡

**無**。本輪所有已驗證發現均已有對應 Backlog 卡，不重複新建。

---

## §2 路由留言

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-167** | AIDV-770 rawBody 修復已完成（commit b0ed81d），Stripe 事件現在真的送到 handler 但 5 個分支仍 console.log stub（`server/routes/stripeWebhook.ts:166-255`）。緊急度從「連通知都驗不了」升級為「通知收到但什麼事都沒做」；`userSubscriptions` 表已存在、seed.ts 有完整 insert 範本，建議升 High 排入下一 sprint。 |
| **AIDV-170** | 活碼掃描（109 個遷移）確認重複數從原卡 3 對（0008/0033/0067）擴大為 **6 對**（+0080/0081/0082）。修法時需同時涵蓋全部 6 對。 |

---

## §3 丟棄清單

| 候選 | dropReason |
|---|---|
| Stripe webhook stubs 新建卡 | AIDV-167 已在 Backlog，描述完整，不重複開卡 |
| ProviderHealthProbe 401/403 | AIDV-886 在進行中，已被追蹤 |
| agentScopeGuard 未接線 | AIDV-879 在 Backlog |
| worldbuilding.getCompositionSuggestions mock | AIDV-847 在 Backlog |
| agentCreatorQuota 多副本問題 | AIDV-906 + AIDV-923 在 Backlog |
| RAG/Pinecone 決策 | AIDV-907 在 Backlog |
| SSE 三總線並存 | AIDV-675 在 Backlog |
| orbLongTermMemory semantic search TODO | 屬 AIDV-907 Pinecone 決策範疇，不另開卡 |
| ActiveProjectContextPanel "即將推出" | 設計意圖的 pending feature placeholder，非缺口 |
| browser-audit-findings.md 所有項目 | 審計日期早於 4-shell 重構，路徑與品牌均已過時（doc-stale） |
| AIDV-913/914/915/916 | 本輪確認全部已完成 |
| ⌘K 功能（AIDV-803 後） | AIDV-803 移除的是重複 handler，CommandPalette.tsx:76 主 handler 健在，無新問題 |
| stripeWebhook.ts 原 5 handler 已修 rawBody | AIDV-770 只修 rawBody 捕獲；handler 實作缺口屬 AIDV-167，非新發現 |

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 拍板（AIDV-167）**：Stripe webhook handler 實作需確認訂閱方案 planId 命名、email 服務選型（SendGrid/Resend）、冪等性策略。建議以 Stripe event.id 做 idempotency key。
- **待 Bruce 拍板（AIDV-906/923）**：agentCreatorQuota 跨副本共享方案（Redis / Supabase / Fly.io single instance）需在確認多副本部署架構後決定。

---

## 完成指標

- 新卡 0 張（乾淨的一輪：都有主卡）
- 路由留言 2 筆：AIDV-167（urgency upgrade）、AIDV-170（migration count update 3→6 pairs）
- 丟棄 13 筆（全部附 dropReason）
- 彙總留言：AIDV-102（待本步驟）
- 驗證基準：`origin/main` HEAD de13de0（2026-06-30）

— 智能助手 🤖
