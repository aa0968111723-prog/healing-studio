# aidv-optimize 第 8 輪審計 — 2026-06-30（R8）

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：直接 code 分析（origin/main HEAD 49f8506）—— 聚焦 R7 基準（5dfe7a9）→ 49f8506 之間 14 commits 引入的新缺口，涵蓋 AIDV-877/878 DLQ/Quota 接線、AIDV-884/885 IDOR 修復、AIDV-887 freellmapi tools guard、deploy boot fix（9b44637）。
> **規模**：直接分析 → 驗證後 **新建 0 卡｜路由 4｜丟 多筆**。
> **基準**：HEAD（`origin/main`，2026-06-30 49f8506，以 R7 5dfe7a9 為去重錨點）。
> **聚焦**：14 commits 主要為既有卡的修復（AIDV-877~916），R8 聚焦「修復後的剩餘缺口」與「新引入代碼的隱性問題」。

---

## 結論（一句話）

**本輪 14 commits 絕大多數是既有卡的修復，無法產生大量新卡；四項路由發現為：① AIDV-877 DLQ 寫入已完成但 `pollDlq()` 監控排程仍缺、② AIDV-912 與 AIDV-887 重複追蹤同一 freellmapi 問題（已修復，AIDV-912 可關閉）、③ `orbAutoDriverInFlight` Map 也是 process-local（與 AIDV-906 同根）、④ AIDV-887 測試缺正向迴歸防護；所有 drop 均有明確已追蹤卡或屬推測性邊界情境。**

---

## §1 本輪新建卡

本輪無新建卡（所有發現均可路由到既有卡）。

---

## §2 路由留言（4 筆）

| 路由到 | 發現摘要 |
|---|---|
| **AIDV-877** | `server/services/agentDlq.ts:81` `pollDlq()` 從未被任何 production 代碼調用（grep 僅回傳自身定義）；DLQ 寫入側（`insertDlqEntry` via b5f958a）已完成，但積壓計數與 escalate 告警仍無能見度。建議接入 `startScheduledMaintenanceJobs()` 週期排程或 `/api/health/detail`。 |
| **AIDV-912** | commit 49f8506（`fix(AIDV-887)`）已在 `llm.ts:1997` 加入 `engineConfig.supportsToolCalling !== false` 守門，並附 `llm-tool-capability-guard.test.ts` 測試；AIDV-912「進行中」追蹤同一問題，建議關閉。 |
| **AIDV-906** | `server/routers/_aiHelpers.ts:29` `orbAutoDriverInFlight = new Map<string, number>()` 也是 process-local；`orbScheduler.ts` 在各副本獨立輪詢下可能雙重 dispatch 同一任務。Redis/DB-backed 方案（AIDV-906 修法）可同時覆蓋此去重問題。 |
| **AIDV-887** | `llm-tool-capability-guard.test.ts` 缺少正向迴歸測試（標準引擎 WITH tools → payload 應含 tools），若未來判斷邏輯被錯誤改動（如 `!== true`）現有測試無法捕捉。Low 優先，建議補一個 `it("正常引擎送出 tools")` case。 |

---

## §3 丟棄（均有明確理由）

| 候選 | 丟棄原因 |
|---|---|
| Stripe webhook TODO stubs | 已覆蓋：AIDV-167（路由留言 opt-cycle-2026-06-30 已留）|
| orbLongTermMemory 語義搜尋 TODO | 已覆蓋：AIDV-907（RAG 後端未決策）；現有 LIKE 搜尋可用，非硬缺口 |
| `bootReady` 無超時 | 推測性：`runMigrations` 通常有連線池超時；即使卡住，Railway 會在 healthcheck 窗口後自動重啟容器（fail-closed 行為已知） |
| freellmapi 串流 tools gap | 無證據：llm.ts 的串流路徑也已通過 `engineConfig.supportsToolCalling !== false` 守門 |
| `agentCreatorQuota` test fail-open | 已知設計：`tryAcquireCreatorSlot` 在 `NODE_ENV=test` 無條件回傳 true，agent-creator-quota.test.ts 已文件化此行為 |

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 確認（AIDV-906 修法範圍）**：AIDV-906 決策後（Redis vs DB-backed Map），建議同時覆蓋 `orbAutoDriverInFlight` 去重邏輯，避免分批修兩個相同模式的 Map。
- **待 Bruce 關注（AIDV-912 關閉）**：AIDV-912 為 R56 自動產生的 automated 卡，需人工確認關閉（或 autodev 在下次跑到時自動轉欄）。
- **AIDV-877 剩餘工作**：`pollDlq()` 排程接線為 Low 優先，可在下一輪 autodev 撿取；無安全風險，純監控能見度。

---

## 完成指標

- 新卡 0 張
- 路由留言 4 筆：AIDV-877（pollDlq 排程）、AIDV-912（關閉建議）、AIDV-906（orbAutoDriverInFlight 補充）、AIDV-887（測試迴歸防護）
- 丟棄 5 筆：Stripe webhook、semantic search、bootReady timeout、streaming tools gap、test fail-open
- 彙總留言：AIDV-102（待本步驟）
- 驗證基準：`origin/main` HEAD 49f8506（2026-06-30）

— 智能助手 🤖
