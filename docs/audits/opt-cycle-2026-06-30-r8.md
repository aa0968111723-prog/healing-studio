# aidv-optimize 第 8 輪審計 — 2026-06-30（R8）

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：直接 code 分析（origin/main HEAD 6df03b1）—— 聚焦 R7 基準（5dfe7a9）→ 6df03b1 之間 8 commits 引入的新缺口，特別是 AIDV-856（PREFER_CHEAP_MODELS）、AIDV-857（GenerationEngineChip）、AIDV-124（orbBudgetGuard）等新功能的實作缺口。
> **規模**：直接分析 → 驗證後 **新建 3 卡｜路由 0｜丟 0**。
> **基準**：HEAD（`origin/main`，2026-06-30 6df03b1，以 R7 5dfe7a9 為去重錨點）。
> **聚焦**：R7→R8 新 commits 帶入的缺口：PREFER_CHEAP_MODELS premium 分支未實作、GenerationEngineChip 誤導性預設、orbBudgetGuard 無快取 DB 查詢。

---

## 結論（一句話）

**本輪最重要的發現是 AIDV-938（Medium）：`PREFER_CHEAP_MODELS=premium` 靜默降至 economy 模式（比 balanced 還差），原因是 `getActiveDefaultBrains()` 只有二分檢查；次要為 AIDV-939（Low）：`GenerationEngineChip` 預設 `"healthy"` 導致載入中/查詢失敗時誤顯綠燈；AIDV-940（Low）：`enforceMonthlyBudgetGate()` 啟用時每次 LLM 請求發 DB SUM 查詢、無 TTL 快取；路由 0，丟棄 0。**

---

## §1 本輪新建卡（已對 origin/main HEAD 6df03b1 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-938** | Medium | opt-card, backend, cost, 待議, decision | `server/middleware/brainContext.ts:209,219` `getActiveDefaultBrains()` / `getActiveDefaultEngines()` 只做 `=== "balanced"` 二分；設 `PREFER_CHEAP_MODELS=premium` 靜默跑 economy（gemini-flash + flux/schnell），AIDV-856 原規格表的 premium 分支（kling-pro / suno-v4 / opus-4.7）從未實作，且無任何 warn。待 Bruce 拍板 premium 模型清單後補第三分支。 |
| **AIDV-939** | Low | opt-card, frontend, uiux | `client/src/shells/video/console/CreationFlowBar.tsx:66` `const status = data?.status ?? "healthy"` — 對比相鄰 `AiBrainChip` 用 `"offline"` 安全預設，本 chip 在載入中或 `retry: false` 查詢失敗時誤顯綠燈「生成引擎·線上」。修法：改 `"down"` 並加 `isLoading` 骨架態。 |
| **AIDV-940** | Low | opt-card, backend, perf | `server/services/orbBudgetGuard.ts:77` `enforceMonthlyBudgetGate()` 啟用後每次 `ai.chat`/`director.chat` 均打 `SELECT SUM(totalCostUsd) FROM cost_aggregations WHERE date >= monthStart`，無 TTL 快取。`cost_aggregations` 雖小（≤31 筆/月），但仍是額外 DB 往返。修法：加 60s in-memory cache，參考 `providerHealth.ts` 的 `healthCache` 模式。flag 預設 OFF，低風險。 |

---

## §2 路由留言

本輪無路由（三筆發現均無現有擁有卡對應；AIDV-856/857/124 均已標 完成）。

---

## §3 丟棄

本輪無丟棄候選（所有發現均為 5dfe7a9→6df03b1 新引入缺口，已在 HEAD 實證）。

---

## §4 Phase 2 / 待 Bruce

- **待 Bruce 拍板（AIDV-938 premium 模型）**：方案 A（補 premium 分支，照 AIDV-856 規格 kling-pro/suno-v4/opus-4.7）vs 方案 B（移除 env 說明中的 premium，只保留 economy/balanced）。
- **Phase 2 人工走查**：AIDV-939 需在慢速網路模擬 `providerSystemStatus` 延遲，確認初始載入不顯示誤導綠燈；AIDV-940 需在 `ENABLE_ORB_BUDGET_GUARD=true` 環境壓測確認快取命中率。

---

## 完成指標

- 新卡 3 張：AIDV-938（Medium, backend/cost, needs-bruce）、AIDV-939（Low, frontend/uiux, auto-pass）、AIDV-940（Low, backend/perf, auto-pass）
- 路由留言 0 筆
- 丟棄 0 筆
- 彙總留言：AIDV-102（已完成）
- 驗證基準：`origin/main` HEAD 6df03b1（2026-06-30）

— 智能助手 🤖
