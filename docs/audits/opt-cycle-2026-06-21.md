# aidv-optimize 第 1 輪審計 — 2026-06-21

> **系統**：`/aidv-optimize`（任務卡迭代優化系統，執行者 智能助手 🤖）
> **方法**：多代理 Workflow（4 模態 harvest → 2 視角對 HEAD 驗證＋對看板去重）。本檔＝該輪完整記錄，亦為下一輪去重錨點。
> **規模**：harvest 67 候選（docs:42 jira:8 uiux:6 fullstack:11）→ 驗證後 **新建 4 卡｜路由 ~24｜丟 5（已修/重複）**。
> **基準**：HEAD（worktree reverent-blackwell-946cbe）。Workflow 全量結果：`tasks/wian8w4ky.output`。

---

## 結論（一句話）

**最有價值的發現是「`spine/projectGateway.ts` 的 `as any` 造成一整串 tsc 抓不到的靜默失敗」**（含一個同帳號跨專案資料混入），已開成 [AIDV-161]/[AIDV-162] 兩張 High 卡；其餘前端健壯性與後端派發碎片化各開一卡，UI a11y／大腦模型 SSOT 兩群路由回擁有卡；報告中宣稱已修者（0075 junction、Director temperature）經 HEAD 實證確認已修，未重複產卡。

---

## §1 本輪新建卡（已對 HEAD 實證）

| 卡 | 優先 | 標籤 | 一句話 |
|---|---|---|---|
| **AIDV-161** | High | opt-card, frontend, data-boundary | `loadProject` 跨專案混入：`worldStoryboard.list` 不吃輸入、回該使用者全部分鏡（server `worldStoryboard.ts:119` 無 input；client `projectGateway.ts:113` 送的 projectId 被忽略）。改用 `listByWorld`。 |
| **AIDV-162** | High | opt-card, frontend, type-safety | `projectGateway.ts:90` `getTrpcClient() as unknown as any` → 5 個方法 input/output 形狀錯誤靜默失敗（saveVaultState／saveCharacterEdit／ingestStoryboard 寫不進、loadProject worldbuilding.get 永遠 null、recompilePacket 永遠本地回退）。移 any、用具名型別。 |
| **AIDV-163** | Medium | opt-card, frontend | 健壯性 4 連：先 toast.success 再 await 的競態＋無 isPending 去重、空 catch 吞錯、`director.createPlanningMilestones` N+1 逐筆 await、`ProductionPackagePreview` 死碼 onClick。 |
| **AIDV-164** | Medium | opt-card, backend, 待議 | Fal 派發碎片化：image/audio/video router 各包 `falQueueSubmit`（盤點 A.8 #1 🔴），統一走 `falDispatcher`；附 background_jobs/generation_history 無 FK／無 TTL。碰生成關鍵路徑→待 Bruce。 |

## §2 路由回擁有卡（一軌一主，已留言或記此）

| 群 | 路由到 | 內容 |
|---|---|---|
| a11y/對比 | **AIDV-118**（已留言）| 6 icon 缺 aria-label、10 framer 無 reduced-motion、命中區<44pt、error 當 empty、低不透明灰字對比。 |
| 大腦/模型 SSOT | **AIDV-104**（已留言）| FALLBACK_CHAINS 無 compile-time 檢查、brain upsert 缺 enum 白名單、缺一致性測試、fallback 鏈分散兩處。 |
| 載入態統一 | AIDV-92 | skeleton→PanelLoading、統一 `<GenerationStatus/>`、面板 any-cast 收斂（維護性）。 |
| 預扣款/回退 | AIDV-160 | chargeForFalTask 先扣後送→改 hold/pending；mock 在 prod 回退鎖 provider 決策。 |
| 影片管線 | AIDV-18 | M3 兩表 video_generation_sessions／video_segment_jobs（P4）。 |
| 向量遷移 | AIDV-19 | pgvector halfvec(3072)+HNSW（decision-gated）；Supabase parity 亦歸此。 |
| /social | AIDV-96 | /social 實 UI（重用 video cockpit、零新表，P5）。 |
| 提示詞↔素材 | AIDV-8 | 0075 junction 驗證雙向讀寫＋backfill 生效（非重建表）。 |
| Director 狀態機 | AIDV-22 | multi-step chain 改 xstate。 |
| 孤兒頁清理 | AIDV-133 | 8 個未掛路由頁，確認無深連結/analytics 後刪。 |
| 提示詞庫搜尋 | （後端小）| `promptLibrary.list` search 只 LIKE title，未含 content。 |
| 資安續跑 | AIDV-158 | C2 六維（auth/idor/sql/ssrf/secrets/dos）續跑。 |
| UX 子卡 | AIDV-157 | 錯誤訊息白話化＋送出後捲到最新訊息。 |
| provenance | AIDV-122 | AI 輸出來源標示拆獨立子卡。 |
| 大型重構 | AIDV-71 | 抽 useGenerationTask hook（四 studio ~19.6K LOC 複製）。 |

## §3 丟棄（已修／重複，**staleness 過濾的價值所在**）

| 候選 | 原因（HEAD 實證） |
|---|---|
| 提示詞庫↔素材 junction 缺失 | **已修**：`drizzle/0075_prompt_assets.sql` 存在、`postGenActions.ts:36/248/365` 已寫 junction（與 PR #864 相符）。剩驗證歸 AIDV-8，不重建表。 |
| Director temperature 0.7→0.4 | **已修並 push main/Railway**；報告自承「防回歸對照非新優化」。 |
| Auth 維持自建 JWT | **已決策**：AIDV-28 decision-resolved，無新可交付。 |
| Supabase parity 金鑰未到不切換 | 與 **AIDV-19**（blocked/needs-key）完全重疊，歸該卡。 |
| ZIP 匯出 dead button 等 7 項舊稽核 | 多為 4-shell 重構前 `client/src/pages/*` 路徑，需逐一對新殼確認；本輪未產卡，留待人工或下輪對碼後處置。 |

## §4 Phase 2 / 待 Bruce

- **待 Bruce 拍板**：AIDV-164（Fal 派發碰生成關鍵路徑）、後端 P3/P4（M2/M3 表、HF/Gemini adapter、halfvec 維度、drizzle relations + 重複 migration 號）皆 decision/needs-key，已路由不擅自動工。
- **Phase 2 人工走查**：a11y 對比實測、命中區真機量測（AIDV-118 軌）。

— 智能助手 🤖
