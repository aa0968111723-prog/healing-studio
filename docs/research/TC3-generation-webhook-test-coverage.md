# TC3 — 生成/webhook/背景任務測試覆蓋
- 產生日期:2026-07-03
- 依據 commit:812f6fdb
- 稽核範圍:generate/webhook/background job/dispatch 相關 *.test.ts + 對照 EH/IN/RC 發現

> 方法論:先用 Glob/Grep 找出實際存在的 `*.test.ts(x)`/`__tests__` 測試檔並逐一讀取內容確認「測了什麼」,再回頭比對 `docs/research/EH*.md`(錯誤處理失效模式)、`IN8-event-bg-task-wiring.md`(事件/背景任務接線)、`RC0/RC1-*.md`(並發競態)、`W3/W7-*-deepdive.md`(generate.ts / webhook 安全網逐行深挖)的已確認發現。不臆測「應該有測試」——每一列的「有無測試」都基於實際讀到的測試檔內容,測試檔存在但斷言方向不對/只驗證表層的案例會標註「untested」或「partial」並說明原因,而非僅憑檔名判斷。已知既有測試(deduct/refund/atomicClaimJobRefund 並發測試、videoCatalogConsistency.test、agent-tool-executor.test 的 fallback vs requireConfirmation 缺口)依任務指示採納為前提,未在本次重新驗證範圍內深挖其實作細節,僅引用其結論。

---

## 1. 逐項核對表:關鍵路徑 × 覆蓋狀態 × 對應發現 × 回歸風險 × 建議測試

| # | 關鍵路徑(檔案:行號) | 對應發現 | 覆蓋狀態 | 證據(實際測試檔:行號) | 回歸風險(若 untested) | 建議測試 |
|---|---|---|---|---|---|---|
| 1 | `server/routes/webhookFal.ts:204-217`(單路徑終態守門:job 已 `completed/failed/cancelled` 則短路) | RC0/RC1(job 終態覆蓋,單路徑面) | **tested** | `server/routes/__tests__/webhookFal.test.ts:503-553`(AIDV-158):重送已 completed/failed 的 job → 不寫 DB、不重跑 post-gen、不重覆退款 | — | 已覆蓋,可作範本 |
| 2 | `server/routes/webhookSuno.ts:170-183`(同款終態守門) | RC0/RC1 | **tested** | `server/routes/__tests__/webhookSuno.test.ts:241-289`(AIDV-590):遲到的 `first`/`text` 中間回呼在 completed/failed 後不覆寫 | — | 已覆蓋 |
| 3 | `server/routes/webhookReplicate.ts:111-120`(同款終態守門) | RC0/RC1 | **tested** | `server/routes/__tests__/webhookReplicate.test.ts:168-216`(AIDV-610):ready 後收 failed、failed 後收 succeeded 皆不覆寫 | — | 已覆蓋 |
| 4 | `server/db.ts:2141-2148`(`updateBackgroundJob` 無條件 `UPDATE ... WHERE id=?`,無 `WHERE status` CAS 守衛)——webhook / `checkStudioJob` 輪詢 / `staleJobChecker` **三條路徑之間**的競態 | **RC0 §44-59、RC1 發現 2**(P0・高):webhook 剛寫入 `completed+resultUrl`,若 staleJobChecker 在稍早已把該 job 讀進 stuck 快照,稍後仍會用舊快照把它打回 `queued`/`failed`,抹掉成品連結 | **untested**(第 1-3 列的守門測試都只驗證「單一路徑重送同一事件」的 replay 情境,mock 的 `updateBackgroundJob` 是裸 `vi.fn()`,不模擬真實 DB 的無 WHERE 守衛行為;`staleJobChecker.test.ts` 的 `mockGetStuck`/`mockUpdate` 也是各自獨立 mock,兩者從未在同一測試案例中交錯執行) | 任何人修 `updateBackgroundJob` 加東西都不會觸發回歸測試失敗;修 CAS 守衛後也無法用現有測試自動證明修好;目前三條完成路徑(webhook/輪詢/staleJobChecker)之間真正的交錯覆蓋(而非各自獨立的終態守門)完全沒有測試覆蓋 | 新增整合測試(建議 `server/db.updateBackgroundJob.cas.test.ts` 或擴充 `staleJobChecker.test.ts`):模擬「staleJobChecker 讀到 job 為 processing 且逾時」與「webhook 在 staleJobChecker 寫回之前已把同一 job 標記 completed」的交錯順序,斷言最終狀態應保持 `completed`(而非被 `staleJobChecker` 的舊快照覆寫回 `queued`/`failed`);可先對 `updateBackgroundJob` 補上 `WHERE status IN (...)` 條件式更新,再用此測試驗證 |
| 5 | `server/routes/webhookFal.ts:166`(`res.status(200)` 先送出)→ 之後 `updateBackgroundJob`/`runPostGenForJob`/`refundJobIfBilled` 才執行,任何一步拋錯只落入最外層 `catch(err){console.error}`(:359-361) | **EH1**(swallowed errors):驗證通過即回 200,供應商不會重送;DB 暫時故障時任務卡住、資產未持久化、退款未觸發 | **untested** | 對 `webhookFal.test.ts`/`webhookSuno.test.ts`/`webhookReplicate.test.ts` 三檔全文 Grep `mockRejectedValue`/`throw` 於 mock 實作中皆**零命中**——目前所有測試案例的 `updateBackgroundJobMock`/`getBackgroundJobMock`/`runPostGenForJobMock` 永遠 `mockResolvedValue`,從未模擬過這些呼叫拋錯後的行為 | 若 DB 在 webhook 處理期間短暫故障,目前完全沒有測試驗證:(a) 是否有任何告警/重試,(b) job 是否卡在 processing 且對使用者不可見,(c) 是否會造成孤兒扣款(已扣點但無法退款,因為狀態更新本身就失敗了) | 新增測試:`updateBackgroundJobMock.mockRejectedValueOnce(new Error("db down"))` 後送 webhook payload,斷言 `res.status` 仍是 200(已送出)、`console.error` 被呼叫(或未來補上的告警機制被觸發)、且能證明現況「job 永遠卡在 processing,無自動恢復」——需搭配 staleJobChecker 是否能撿回這類 job 一併驗證(目前 staleJobChecker 只認 `status='processing' AND updatedAt<cutoff`,若 DB 故障導致 `updateBackgroundJob` 整個沒寫入,`updatedAt` 不會更新,理論上仍能被撿到,但**未經測試驗證這條救援路徑對此情境成立**) |
| 6 | `server/routes/webhookFal.ts:268/303/338`(`void refundJobIfBilled(jobId)`/`void runPostGenForJob(jobId)` fire-and-forget,無 `.catch`) | **EH3 發現 2**(HIGH):`void` 呼叫且無 `.catch`,函式內部有未受 try 保護的 DB 呼叫,DB 抖動時的 unhandled rejection 在多任務併發下有觸發 storm 閾值風險 | **untested** | 同上,三個 webhook 測試檔皆無任何案例讓 `runPostGenForJobMock`/`refundJobIfBilledMock` reject——無法驗證「這兩個 fire-and-forget 呼叫失敗時,是否會變成未捕捉的 promise rejection」 | 與 #5 同根因但性質不同:這裡即使 webhook handler 自身的 try/catch 完整,`void` 呼叫的 reject 本來就不會被同步 try/catch 接住,需要獨立測試驗證 | 新增測試:`refundJobIfBilledMock.mockRejectedValueOnce(...)`,監聽 `process.on("unhandledRejection")` 於測試範圍內,驗證是否真的產生未捕捉拒絕(重現問題)、或驗證修復後(補 `.catch`)不再產生 |
| 7 | `server/routers/generate.ts:1536-1539`(`jobStatus`:`db.getBackgroundJob(input.jobId)` 無 owner 檢查) | **W3 C1**(P0・IDOR):任何登入者換 `jobId` 即可讀他人 prompt/resultUrl/費用;同檔姐妹 procedure `checkStudioJob`(:2176-2182)有正確 `job.userId !== ctx.user.id` 檢查,`jobStatus` 是漏做 | **untested,且有高誤判風險** | 全庫 Grep `caller.generate.jobStatus`/`generate\.jobStatus` 於 `*.test.ts*` **零命中**。唯一命中「jobStatus」關鍵字的 `server/_core/videoIdorConvergence.test.ts:82-89`(AIDV-244)測的是**另一個不同路由** `server/routers/videoStudio.ts` 的 `jobStatus`(有 `getBackgroundJobByRequestId`+`FORBIDDEN` 守門且已測),與 `generate.ts` 的 `jobStatus` 完全不同函式——容易誤判「jobStatus 已經測過了」 | 這是本次稽核發現的**最容易被誤判為已覆蓋**的缺口:檔名/函式名相同但實作路由不同,`videoStudio.jobStatus` 有測試+有防護,`generate.jobStatus` 零測試+零防護,兩者不可混為一談;任何人修 `generate.ts` 不會被任何測試攔下,C1 IDOR 修復後也無法自動驗證 | 新增 `server/routers/__tests__/generateJobStatus.test.ts`:比照 `videoIdorConvergence.test.ts` 或 TC2 已列的 `history-ownership-idor.test.ts` 寫法,以非 owner 的 `ctx.user.id` 呼叫 `generate.jobStatus({jobId})`,驗證應 `FORBIDDEN`/回 `null`(修復前此測試會失敗,可當回歸測試骨架) |
| 8 | `server/routers/generate.ts:342-1534`(`multimodal` 同步 mutation,image/video/audio/voice 四區塊各自在 fal dispatch 失敗/無 URL 時呼叫 `refundUserPoints` 後 `throw`,又被最外層 `catch`(:1491-1533)**無條件**再退一次) | **W3 C3**(Critical):雙重退款,可被重複觸發刷點數 | **untested** | `server/phase3-audit.test.ts:128-215`、`server/healing.test.ts`、`server/phase5-audit.test.ts`、`server/phase3a-visual.test.ts`、`server/atomic-deduction.test.ts` 內所有 `caller.generate.multimodal(...)` 呼叫全部是 zod 輸入驗證測試(空 prompt、非法 enum、溫度範圍等 `rejects.toThrow()`),**沒有任何一個案例讓 fal dispatch 失敗後檢查 `refundUserPoints` 被呼叫幾次** | 每一次因 fal.ai dispatch 失敗或「回傳但無 URL」導致 `multimodal` 失敗,使用者都被多退一次點數;此缺口目前完全沒有測試防線,也無法自動驗證未來的修復(移除內層退款或加 CAS 旗標)是否生效 | mock `dispatchImageGeneration`(或對應 image/video/audio/voice 分支的 dispatch 函式)回傳失敗,呼叫 `multimodal`,斷言 `db.refundUserPoints` 被呼叫**恰好一次**(而非兩次);四個模態(image/video/audio/voice)的失敗分支建議都各補一條 |
| 9 | `server/routers/proStudio.ts`(`checkAudioStatus` 家族,~20 個非 Suno fal 模型端點,`falQueueSubmit` 從未呼叫 `createBackgroundJob`) | **W7 發現 0**(P0・本輪核心):webhook 有到、簽章/token 都驗證通過,但因找不到任何 `backgroundJobs` row 可綁定,`webhookFal.ts:191-196` 靜默丟棄,`refundJobIfBilled` 從未被呼叫;`checkAudioStatus` 的 `FAILED` 分支(輪詢)同樣無退款呼叫;是「webhook 安全網結構性補不到」的一族端點,非併發競態而是 100% 必發生 | **untested,且有誤導性弱測試**(見下) | 全庫 Grep `checkAudioStatus`/`falQueueSubmit` 於 `*.test.ts` 找不到任何 server 端測試檔案針對 `proStudio.ts` 本身(`find . -iname "*prostudio*"` 只找到三個 client 端 `tests/unit/client/pages/ProStudio.*.test.tsx`,皆為前端表單/送出邏輯,不觸及 webhook/退款);唯一相關的 `server/points-billing-audit.test.ts:28-54` 只是**逐字計數**斷言:`refundUserPoints(ctx.user.id` 出現次數 ≥ `chargeForFalTask(ctx.user.id` 出現次數——這只能證明「檔案裡某處寫了退款呼叫」(通常是 `falQueueSubmit` 本身送出失敗的同步 catch),**完全無法區分**「同步送出失敗有退款」與「非同步任務最終 FAILED/逾時卻無退款」這兩種情境,是一個容易讓人誤以為「已經稽核過退款覆蓋率」的表層測試 | 這是本次稽核發現**最容易被現有測試掩蓋**的 P0 缺口:`points-billing-audit.test.ts` 綠燈不代表 W7 發現 0 的漏洞已被覆蓋或修復,兩者衡量的是完全不同的東西 | 新增 `server/routers/__tests__/proStudioAudioRefund.test.ts`:呼叫 `checkAudioStatus` 家族任一端點(如 sonauto/ace-step),mock `falQueueClient` 回傳 `FAILED`,斷言目前**確實**不會呼叫 `refundJobIfBilled`/`refundUserPoints`(先把現況釘住當回歸測試骨架),待修復(補 `createBackgroundJob` 綁定)後再反轉斷言為「應該退款」 |
| 10 | `server/services/postGenActions.ts:52-181`(`refundJobIfBilled` + `atomicClaimJobRefund` CAS 鎖) | RC0 發現 6 佐證(已知健康路徑) | **tested**(已知,依任務指示採納) | `server/services/postGenActions.refund.test.ts:103-149`:兩條/三條併發失敗路徑同打一個 job,`refundUserPoints` 只呼叫一次;`refundRestoreFailed` 補寫路徑也有覆蓋(:150-166 附近) | — | 已覆蓋,可作為 #4/#9 補測試時的並發測試寫法範本 |
| 11 | `server/routers/generate.ts`/`director.ts`/`proStudio.ts` 的 `createBackgroundJob` 建立失敗→孤兒扣點退款守衛(AIDV-771/620) | EH0/EH2 提及的孤兒扣點類缺口的既有修復 | **tested**(已知,依任務指示採納) | `server/aidv-771-orphan-refund.test.ts`(全檔,4 個 it,涵蓋 generate 同步/非同步+director 三個計費站點的 deduct→try→create→catch→refund→throw 結構不變量)、`server/aidv-620-suno-orphan-refund.test.ts`(proStudio Suno 音樂路徑同款守衛) | — | 已覆蓋;注意這兩份都是**原始碼結構檢查**(字串位置斷言),不是執行期整合測試——若未來重構打散這些函式的程式碼佈局(即使邏輯不變),測試可能誤報失敗,屬已知取捨 |
| 12 | `server/services/refundStatus.ts`(`deriveJobRefundStatus`/`getJobRefundStatuses`) | 支撐 RC0 發現 6、IN8 §2 的退款狀態契約 | **tested** | `server/services/refundStatus.test.ts`(全檔,含 none/not_refunded/partial/full 全矩陣、超退 clamp、`refundRestoreFailed` 降級、director 新舊任務形狀);`server/routers/__tests__/credits.refundStatus.test.ts`(router 層授權/隔離/上限/DB 錯誤 fallback) | — | 已覆蓋,測試品質高,可作範本 |
| 13 | `server/routers/generate.ts:2153-2168`(`submitStudioJob` 建 job 時 `resultJson` 不含 `costPoints`) | **IN8 F2**(high・dead-seam 延伸 B-19):此類任務(ImageStudio/ProStudio/VideoStudio 的「先呼模型 mutation、後補登記」流程)`refundJobIfBilled` 因 `costPoints` 缺失而恆定短路,退款徽章 UI 也恆定不顯示任何警示——使用者對「扣了錢但沒退」拿不到任何信號 | **partial(誤導性)** | `server/routers/__tests__/credits.refundStatus.test.ts:106-114` 有一條案例明確針對「`submitStudioJob` 登錄型」的 `resultJson`(僅 `requestId`/`modelId`/`studioType`,無 `costPoints`)斷言回傳 `refundStatus:"none"`,測試名稱與註解(「前端不顯示徽章」)把**現況的靜默降級行為當作預期正確結果**釘住,而非驗證「`submitStudioJob` 這條路徑是否應該要有 `costPoints`」這個更上游的問題 | 這條測試會通過,容易讓人誤以為「submitStudioJob 的退款顯示邏輯已測試覆蓋」,但實際上只驗證了`deriveJobRefundStatus` 對「缺 `costPoints`」輸入的**純函式行為**(這本身是對的、面向舊式無計費任務),完全沒有觸及 IN8 F2 真正關心的問題:這類任務在寫入端當下,呼叫方是否已經扣點、若已扣點卻不寫 `costPoints`,使用者將永久收不到退款/警示信號 | 需要一條寫入端測試(而非只測衍生函式):在 `submitStudioJob` 呼叫端(ImageStudio/ProStudio 等)確認扣點時機與金額後,驗證 `resultJson` 是否確實寫入對應 `costPoints`(若目前設計是「completion 後才記帳、不預先扣點」,則需要另一條測試證明這條任務類型**不會**產生孤兒扣點,而非現狀「零證據」) |
| 14 | `server/services/webhookDispatcher.ts`(對外創作者 webhook 通知,`dispatchWebhookEvent`) | W7 §五(死碼/契約澄清):與計費安全網是不同系統,已核對接線正確 | **tested** | `server/routers/__tests__/webhookDispatcher.test.ts:34-100+`:無訂閱時 skip、簽章 POST、SSRF URL 被擋、非 2xx 重試並記錄 delivery history | — | 已覆蓋,非本次未測缺口的重點,列出避免誤判 |
| 15 | `server/fal-queue-dispatcher.test.ts`/`server/fal-queue-awaiter.test.ts`(`dispatchFalQueueTask` fallback chain、`awaitFalQueueResult`/`extractFalMediaUrl` URL 抽取) | 支撐 W7 §四注入/SSRF 檢查的底層工具 | **tested** | 兩檔共 16 個 it,涵蓋 fallback chain 降級、4xx 不重試/5xx 走 fallback、多種 payload envelope(nested video/images/data/output)URL 抽取、逾時回 pending、`FAL_API_KEY` 缺失防呆 | — | 已覆蓋,測試品質高 |
| 16 | `server/services/loraTrainer.ts`/`server/routers/loraTrainer.ts`(LoRA/Fal 訓練啟動走 `import(...).then(...)`,僅內層 catch) | **EH3 發現 1**(HIGH):import 本身失敗會變無主 unhandled rejection,backgroundJob 卡在 queued/processing 永不轉 failed | **untested** | Grep `loraTrainer`/`submitReplicateTraining` 於 `*.test.ts`,唯一命中的 `server/orb-train-lora-tool.test.ts` 測的是 orb agent tool 包裝層(`studio.trainLora`,`:76-224`:建 model/job row、路由到 fal/replicate runner、approval gate、輸入驗證),**不觸及** `import(...).then()` 這段動態載入失敗處理路徑本身 | 若 `import(...)` 因模組解析錯誤/build 產物問題失敗,目前完全沒有測試能證明「該 job 是否最終被標記 failed 並退款」,也無法防止此問題被靜默引入 | 新增測試:mock 動態 `import()` 讓其 reject,驗證 backgroundJob 最終狀態(理想上應轉 `failed` 並觸發退款,而非永久卡在 `queued`/`processing`);若目前確實無此救援機制,測試應先釘住現況(卡住)作為回歸基準 |

---

## 2. 已有良好測試(negative results——不要重找 bug,這些路徑已被驗證健康)

| 測試檔:行號 | 涵蓋內容 |
|---|---|
| `server/routes/__tests__/webhookFal.test.ts`(全檔,555 行) | fal webhook 的 jobId 解析(query string / request_id 反查)、OK/ERROR/IN_PROGRESS 三態分流、nested payload 相容(payload.payload/Kling video)、無 URL 時改標 failed 並退款、AIDV-158 capability token 401/放行、終態守門 replay 防護——覆蓋面是本次稽核中最完整的單一測試檔 |
| `server/routes/__tests__/webhookSuno.test.ts` | complete/text 階段分流、meta 保留、無 audio URL 標 failed 並退款、AIDV-615 token 認證、AIDV-590 終態守門 |
| `server/routes/__tests__/webhookReplicate.test.ts` | succeeded/failed/canceled 三態、中間態不覆寫、AIDV-610 終態守門 |
| `server/services/postGenActions.refund.test.ts` + `server/services/__tests__/postGenActions.test.ts` | `refundJobIfBilled`/`atomicClaimJobRefund` CAS 並發安全(2/3 條路徑同打一 job 只退一次)、`runPostGenForJob` 冪等短路、`doPostGenComplete` 對 DB 錯誤的 swallow-and-continue 設計、prompt library upsert-by-content 大量邊角案例 |
| `server/services/refundStatus.test.ts` + `server/routers/__tests__/credits.refundStatus.test.ts` | `deriveJobRefundStatus` 全矩陣(none/not_refunded/partial/full/超退 clamp/`refundRestoreFailed` 降級)、`getJobRefundStatuses` 授權隔離/上限/DB 錯誤 fallback、claim-then-refund 原始碼結構不變量(generate.ts/proStudio.ts/director.ts/postGenActions.ts 四站點) |
| `server/aidv-771-orphan-refund.test.ts` + `server/aidv-620-suno-orphan-refund.test.ts` | `createBackgroundJob` 建立失敗→孤兒扣點退款守衛,涵蓋 generate 同步/非同步、director 多代理、proStudio Suno 音樂四個計費站點 |
| `server/jobs/staleJobChecker.test.ts` | retryCount 0→1→2→3(標 failed)遞增邏輯、多 jobType 各自獨立掃描、`resultJson` 合併保留其他欄位——僅限單路徑隔離邏輯,不含與 webhook 交錯的競態(見第 1 節 #4) |
| `server/routers/__tests__/webhookDispatcher.test.ts` | 對外創作者 webhook 通知:訂閱比對、HMAC 簽章、SSRF URL 阻擋、重試+delivery history |
| `server/fal-queue-dispatcher.test.ts` + `server/fal-queue-awaiter.test.ts` | fal 佇列送出的 fallback chain 降級、4xx/5xx 分流、多種 provider payload envelope 的 URL 抽取、逾時/缺 API key 防呆 |

### 已知既有並發/回歸測試(依任務說明採納,未在本次重新驗證範圍內深挖,僅列供交叉參考)
- `deduct`/`refund`/`atomicClaimJobRefund` 有並發回歸測試(W5),與本次第 1 節 #10 交叉確認一致。
- `videoCatalogConsistency.test`(影片類一致性)。
- `agent-tool-executor.test` 覆蓋 fallback 路徑,但**未蓋 `requireConfirmation`**——本次稽核在 dispatch 相關範圍內額外發現的姐妹缺口是第 1 節 #6(`webhookFal.ts` 的 `void` fire-and-forget 呼叫無 `.catch`),兩者都是「非同步/確認閘的邊界路徑缺測試」同類問題,建議合併規劃補測試。

---

## 3. 重點標記:頂級確認發現中「untested」的優先順序

1. **W7 發現 0**(proStudio ~20 個 `checkAudioStatus` 端點,webhook 安全網結構性補不到,見 #9)—— P0、100% 必發生(非競態)、且現有 `points-billing-audit.test.ts` 是會誤導人的表層計數測試,優先度最高。
2. **W3 C1**(`generate.ts` `jobStatus` 零 owner 檢查 IDOR,見 #7)—— P0,且與已測試的 `videoStudio.jobStatus` 同名不同函式,是最容易被誤判「已覆蓋」的缺口,零測試零防護。
3. **W3 C3**(`generate.ts` `multimodal` 雙重退款,見 #8)—— Critical,可被重複觸發刷點數,現有 `multimodal` 測試全部只測輸入驗證,完全沒有退款次數斷言。
4. **RC0/RC1**(`updateBackgroundJob` 無 CAS 守衛,三路徑交錯覆蓋終態,見 #4)—— High,個別路徑的終態守門都測了、但唯獨「路徑之間交錯」這個真正的競態場景沒人測過。
5. **EH1/EH3**(webhook 先回 200 後才寫 DB,失敗被外層 catch 吞掉 / `void` fire-and-forget 無 `.catch`,見 #5、#6)—— High,三個 webhook 測試檔沒有任何一個案例模擬 DB/下游呼叫失敗。
6. **IN8 F2**(`submitStudioJob` 不寫 `costPoints`,見 #13)—— High,現有測試不是零覆蓋,而是把現況(靜默無信號)測試成"預期行為",閱讀測試檔案的人容易誤判此問題已解決。
7. **EH3 發現 1**(LoRA `import().then()` 無外層 catch,見 #16)—— High,零測試,唯一同名測試檔測的是完全不同的呼叫層(agent tool 包裝,非動態 import 失敗處理)。

---

## 4. 需再查(尚未在本次範圍內完整驗證)

- `server/routers/director.ts` 的 `executeGenerationTask`/`pollGenerationTask` 對 fal 派工失敗的退款路徑,`refundStatus.test.ts` 已用**原始碼結構檢查**驗證 claim-then-refund 順序不變量,但**未見執行期整合測試**(mock fal 派工失敗後實際跑一次 procedure、斷言退款金額與次數)——是否有等價的執行期測試補在別處,**需再查**。
- `server/services/teachingArchiveIngest.ts`/`teachingArchiveIngestionWorker.ts`(`background_jobs.jobType='teaching_archive_ingestion'`)与 `server/routers/models.ts`(`model_training` job)是否有各自的失敗/終態測試覆蓋,不在本次「generate/webhook/dispatch」核心範圍內,**需再查**。
- `server/unifiedSseRoute.ts` + `server/services/sseRouter.ts`(`UNIFIED_SSE_ROUTER` 旗標開啟後的路徑)目前預設 OFF,是否有對應測試覆蓋旗標開啟後的行為,**需再查**(IN8 已確認旗標本身兩端一致,但本次未深入其測試覆蓋)。
- `client/src/contexts/BackgroundTasksContext.test.tsx`(前端)是否涵蓋 IN8 F1(`BackgroundTasksDrawer` 從未掛載)、F3(`activeJobs` 未過濾 `jobType`)、F4(`SSEFallbackBanner` 掛載範圍)這幾個前端接線缺口,因屬前端範疇且超出本次「server 端 generate/webhook/background job/dispatch」核心焦點,**未展開,需再查**。
